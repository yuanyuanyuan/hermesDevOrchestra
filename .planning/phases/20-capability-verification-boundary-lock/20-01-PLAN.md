---
phase: 20-capability-verification-boundary-lock
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md
  - .planning/phases/20-capability-verification-boundary-lock/20-VERIFICATION.md
  - .planning/phases/19-hermes-workflow-design/DESIGN.md
  - .planning/phases/19-hermes-workflow-design/REQUIREMENTS.md
  - .planning/phases/19-hermes-workflow-design/WORKFLOW-EXPLAINED.md
  - .planning/phases/19-hermes-workflow-design/WORKFLOW-ASCII-DIAGRAMS.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - VFY-01
  - VFY-02
must_haves:
  truths:
    - "Phase 20 produces a capability-verification matrix before any phase 19 claim writeback."
    - "Every phase 19 official-claim row ends with a stable verdict: verified, unsupported, or local-extension."
    - "If a capability is locally runnable, the matrix stores the exact command, exit code, and key output under the local Hermes runtime anchor."
    - "Gateway, Dispatcher, and Hooks are allowed to use hybrid evidence, but hybrid rows are never mislabeled as pure runtime verification."
    - "Every unsupported or downgraded official claim gets a backlog entry in .planning/ROADMAP.md before VFY-01 and VFY-02 are marked complete."
  artifacts:
    - path: ".planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md"
      provides: "Audit entrypoint for all official capability claims referenced by phase 19"
      contains: "verdict:"
    - path: ".planning/phases/20-capability-verification-boundary-lock/20-VERIFICATION.md"
      provides: "Phase 20 evidence summary and milestone closeout readiness"
      contains: "Ready for $gsd-complete-milestone"
    - path: ".planning/phases/19-hermes-workflow-design/DESIGN.md"
      provides: "Appendix A and design claims reclassified from settled matrix verdicts"
      contains: "Capability verification status"
    - path: ".planning/phases/19-hermes-workflow-design/REQUIREMENTS.md"
      provides: "R1/R2 downstream reclassification notes for failed official claims"
      contains: "Phase 0 verification outcome"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog entrypoint for unsupported or downgraded official claims"
      contains: "Phase 20 carry-forward"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VFY-01 and VFY-02 completion traceability after evidence"
      contains: "| VFY-01 | Phase 20 | Complete |"
  key_links:
    - from: ".planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md"
      to: ".planning/phases/19-hermes-workflow-design/DESIGN.md"
      via: "Appendix A and design claims inherit settled matrix verdicts"
      pattern: "Capability verification status"
    - from: ".planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md"
      to: ".planning/phases/19-hermes-workflow-design/REQUIREMENTS.md"
      via: "failed official claims are reclassified to local requirements"
      pattern: "Phase 0 verification outcome"
    - from: ".planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md"
      to: ".planning/ROADMAP.md"
      via: "unsupported rows create explicit backlog entrypoints"
      pattern: "Phase 20 carry-forward"
---

<objective>
Execute Phase 20 as a matrix-first verification pass that locks the Hermes official boundary before implementation.

Purpose: satisfy VFY-01 and VFY-02 by proving which phase 19 official claims are truly supported by the current local Hermes runtime, isolating hybrid/doc-only evidence where local closure is impractical, and automatically reclassifying failed official claims into backlog-backed local work.
Output: a settled capability-verification matrix, phase 19 claim writeback, roadmap backlog carry-forwards, and Phase 20 verification evidence ready for milestone closeout.
</objective>

<execution_context>
Use the GSD execute-plan workflow. Keep all mutating Hermes probes isolated to `/tmp` via temporary `HERMES_HOME` unless a task explicitly requires read-only inspection of the user's live installation state.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/20-capability-verification-boundary-lock/20-CONTEXT.md
@.planning/phases/20-capability-verification-boundary-lock/20-DISCUSSION-LOG.md
@.planning/phases/20-capability-verification-boundary-lock/20-RESEARCH.md
@.planning/phases/20-capability-verification-boundary-lock/20-VALIDATION.md
@.planning/phases/19-hermes-workflow-design/DESIGN.md
@.planning/phases/19-hermes-workflow-design/REQUIREMENTS.md
@.planning/phases/19-hermes-workflow-design/WORKFLOW-EXPLAINED.md
@.planning/phases/19-hermes-workflow-design/WORKFLOW-ASCII-DIAGRAMS.md
@reference/hermes-docs-index/SKILL.md
@reference/hermes-docs-index/hermes_docs_index.json

<interfaces>
Required verdict set:

```text
verified
unsupported
local-extension
```

Required evidence classes:

```text
runtime
hybrid
doc-only
```

Required runtime anchor:

```text
Hermes Agent v0.13.0 (2026.5.7)
```

Required official doc URLs to appear in the matrix when relevant:

```text
https://hermes-agent.nousresearch.com/docs/reference/cli-commands
https://hermes-agent.nousresearch.com/docs/reference/profile-commands
https://hermes-agent.nousresearch.com/docs/user-guide/features/kanban
https://hermes-agent.nousresearch.com/docs/user-guide/features/hooks
https://hermes-agent.nousresearch.com/docs/reference/tools-reference
https://hermes-agent.nousresearch.com/docs/reference/toolsets-reference
```

Required matrix fields:

```text
claim_id
capability_area
phase19_ref
official_source
runtime_anchor
local_feasible
evidence_class
command_or_probe
exit_code
key_output
verdict
writeback_target
backlog_ref
```
 </interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the capability-verification matrix skeleton from phase 19 claims</name>
  <files>.planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md</files>
  <read_first>
    Read 20-CONTEXT.md decisions D-20-01 through D-20-06, 20-RESEARCH.md findings 1 through 6, phase 19 DESIGN Appendix A, all `[Hermes 官方]` markers in WORKFLOW-EXPLAINED.md, and R1/R2 in phase 19 REQUIREMENTS.md.
  </read_first>
  <action>
    Create `20-CAPABILITY-MATRIX.md` as the audit entrypoint for all official claims before any downstream doc edits. Group rows into:

1. Core gating rows: Kanban, Profile, Dispatcher, Curator, Memory, Gateway, Hooks
2. Supporting rows: `session_search`, `terminal`, `clarify`, toolsets, `skill_manage`, `approvals.mode`

    For every row, populate at least:

```text
claim_id
capability_area
phase19_ref
official_source
runtime_anchor
local_feasible
evidence_class
command_or_probe
verdict
writeback_target
backlog_ref
```

    Leave `exit_code`, `key_output`, and final verdict details as placeholders where later tasks will fill evidence. Do not modify phase 19 documents yet.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
f=.planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md
for needle in \
  "Kanban" \
  "Profile" \
  "Dispatcher" \
  "Curator" \
  "Memory" \
  "Gateway" \
  "Hooks" \
  "skill_manage" \
  "session_search" \
  "terminal" \
  "clarify" \
  "approvals.mode"; do
  rg -F "$needle" "$f" >/dev/null
done
rg -F "Hermes Agent v0.13.0 (2026.5.7)" "$f" >/dev/null
'
    </automated>
  </verify>
  <acceptance_criteria>
    The matrix exists, covers the roadmap core areas plus the supporting phase 19 claims, and contains the required schema fields. No phase 19 or roadmap writeback has happened yet.
  </acceptance_criteria>
  <done>Phase 20 has a stable audit surface before any verdict consolidation or document rewrite.</done>
</task>

<task type="auto">
  <name>Task 2: Capture runtime evidence for locally runnable capability rows</name>
  <files>.planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md</files>
  <read_first>
    Read Task 1 matrix skeleton, 20-RESEARCH.md validation architecture, and 20-VALIDATION.md Task 20-01-02 commands. Review local baseline probes from `hermes --version`, `hermes status`, `hermes tools list`, `hermes sessions stats`, `hermes curator status`, and `hermes memory status`.
  </read_first>
  <action>
    Fill runtime evidence for rows that can and should be exercised locally. Default all mutating probes to a temporary home:

```bash
export HERMES_HOME=/tmp/hermes-phase20-home
export HERMES_KANBAN_BOARD=phase20-matrix
```

    Minimum target set:

- Kanban board/task/link/comment/show lifecycle
- Profile list/create/show/use
- Curator status and at least one safe stateful curator probe in temp home
- Memory status and built-in/external-provider boundary
- Tools/toolsets visibility proving `terminal`, `clarify`, and `session_search` exist
- Sessions store visibility

    For each locally runnable row, record the exact command, exit code, key output fragment, and final evidence class `runtime` or `hybrid`. If a row unexpectedly cannot be closed locally, downgrade it to `hybrid` and explain why in the row notes instead of forcing a false pass.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
f=.planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md
for needle in \
  "evidence class: runtime" \
  "exit code:" \
  "key output:" \
  "hermes kanban" \
  "hermes profile" \
  "hermes curator" \
  "hermes memory" \
  "hermes tools" \
  "hermes sessions"; do
  rg -F "$needle" "$f" >/dev/null
done
'
    </automated>
  </verify>
  <acceptance_criteria>
    Every locally runnable row now contains exact probe commands, exit codes, and key outputs under the local runtime anchor. Mutating evidence was collected in a temp home, not in the user's live Hermes state.
  </acceptance_criteria>
  <done>The matrix has hard runtime evidence for the low-risk, locally closable capability rows.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve hybrid/doc-only rows and classify failed official claims</name>
  <files>.planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md, .planning/ROADMAP.md</files>
  <read_first>
    Read the settled runtime rows from Task 2, the official-doc URLs listed in 20-RESEARCH.md, and 20-CONTEXT.md decisions D-20-01 through D-20-06. Re-read phase 19 claims around Dispatcher, Gateway, Hooks, `approvals.mode`, and `skill_manage`.
  </read_first>
  <action>
    Resolve the rows that cannot be treated as straightforward local runtime verification:

- Dispatcher
- Gateway
- Hooks
- `approvals.mode`
- `skill_manage`

    Use the Hermes docs index workflow first, then the official URLs, and only then local read-only probes where appropriate. Mark each row with:

- `evidence class: hybrid` when CLI/runtime and docs both contribute
- `evidence class: doc-only` only when local closure is clearly impractical

    Any official claim that cannot meet the Phase 20 standard must be assigned:

- `verdict: unsupported`, or
- `verdict: local-extension`

    Then create a concrete backlog item in `.planning/ROADMAP.md` with a `Phase 20 carry-forward` marker and a back-reference to the matrix row. Do not update phase 19 docs yet.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
f=.planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md
for needle in \
  "evidence class: hybrid" \
  "evidence class: doc-only" \
  "https://hermes-agent.nousresearch.com/docs/user-guide/features/hooks" \
  "https://hermes-agent.nousresearch.com/docs/user-guide/features/kanban" \
  "https://hermes-agent.nousresearch.com/docs/reference/cli-commands" \
  "verdict: unsupported" \
  "verdict: local-extension"; do
  rg -F "$needle" "$f" >/dev/null
done
rg -F "Phase 20 carry-forward" .planning/ROADMAP.md >/dev/null
'
    </automated>
  </verify>
  <acceptance_criteria>
    Hybrid/doc-only rows are clearly labeled, and every failed or downgraded official claim now has an explicit roadmap backlog entrypoint. No failed claim remains buried only in matrix notes.
  </acceptance_criteria>
  <done>The matrix fully expresses official boundary verdicts, including unsupported/downgraded claims and their carry-forward work.</done>
</task>

<task type="auto">
  <name>Task 4: Write settled verdicts back into phase 19 and close VFY traceability</name>
  <files>.planning/phases/19-hermes-workflow-design/DESIGN.md, .planning/phases/19-hermes-workflow-design/REQUIREMENTS.md, .planning/phases/19-hermes-workflow-design/WORKFLOW-EXPLAINED.md, .planning/phases/19-hermes-workflow-design/WORKFLOW-ASCII-DIAGRAMS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/20-capability-verification-boundary-lock/20-VERIFICATION.md</files>
  <read_first>
    Read the settled `20-CAPABILITY-MATRIX.md`, phase 19 Appendix A, all `[Hermes 官方]` labels, and 20-VALIDATION.md Task 20-01-04 checks.
  </read_first>
  <action>
    Apply matrix verdicts to the phase 19 source package and close Phase 20 traceability:

- Update phase 19 Appendix A so every official claim has an explicit post-Phase-20 status
- Update phase 19 REQUIREMENTS R1/R2 downstream notes so failed official claims are reclassified instead of implied away
- Update WORKFLOW-EXPLAINED.md and WORKFLOW-ASCII-DIAGRAMS.md where needed so `[Hermes 官方]` labels no longer overclaim unsupported coverage
- Update `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` only after evidence is complete
- Create `20-VERIFICATION.md` summarizing the matrix outcome, writeback scope, backlog entries, and milestone closeout readiness

    Then run the final gate:

```bash
rtk make test
```
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
rg -F "verified" .planning/phases/19-hermes-workflow-design/DESIGN.md >/dev/null
rg -F "local-extension" .planning/phases/19-hermes-workflow-design/DESIGN.md >/dev/null
rg -F "unsupported" .planning/phases/19-hermes-workflow-design/DESIGN.md >/dev/null
rg -F "Phase 0 verification outcome" .planning/phases/19-hermes-workflow-design/REQUIREMENTS.md >/dev/null
test -f .planning/phases/20-capability-verification-boundary-lock/20-VERIFICATION.md
'
    </automated>
    <automated>
rtk make test
    </automated>
  </verify>
  <acceptance_criteria>
    Phase 19 claims now match the settled matrix verdicts, VFY-01/VFY-02 can be marked complete, and Phase 20 verification evidence explicitly states readiness for milestone closeout.
  </acceptance_criteria>
  <done>Phase 20 has locked the official boundary, updated downstream planning docs, and produced closeout evidence.</done>
</task>

</tasks>
