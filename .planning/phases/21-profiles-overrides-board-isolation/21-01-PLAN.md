---
phase: 21-profiles-overrides-board-isolation
plan: "01"
type: execute
wave: 1
depends_on:
  - 20-01
files_modified:
  - docs/orchestra/hermes/profile-distribution/
  - .hermes/profiles/
  - docs/orchestra/scripts/setup.sh
  - docs/orchestra/scripts/bin/orch-init
  - docs/orchestra/scripts/bin/orch-start
  - docs/orchestra/scripts/bin/orch-status
  - docs/orchestra/scripts/bin/orch-profile-sync
  - docs/orchestra/scripts/lib/orch-common.sh
  - docs/orchestra/scripts/tests/test-install-probe.sh
  - docs/orchestra/scripts/tests/test-init-start-status.sh
  - docs/orchestra/scripts/tests/test-profile-packaging.sh
  - docs/orchestra/scripts/tests/test-project-isolation.sh
  - docs/orchestra/README.md
  - docs/orchestra/WORKFLOW.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/phases/19-hermes-workflow-design/DESIGN.md
  - .planning/phases/21-profiles-overrides-board-isolation/21-VERIFICATION.md
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - FLOW-02
  - MEM-01
must_haves:
  truths:
    - "Phase 21 produces a checked-in canonical profile catalog covering 8 active roles and 3 reserved roles."
    - "The canonical runtime reviewer slug is `reviewer`; `tech-reviewer` is no longer emitted as a generated profile directory."
    - "Project overrides merge `model`, `toolsets.enabled/disabled`, and SOUL fragments into `.hermes/projects/{project_slug}/` without mutating project customizations into `~/.hermes/profiles/`."
    - "A single `project_slug` derives board slug, workspace root, override directory, memory namespace, and task/log/run prefixes."
    - "Learnings default to `project:{project_slug}` and only orchestrator or explicit user `cross-project` intent may request global promotion."
    - "Two different project slugs generate isolated runtime outputs with no board/profile/memory bleed."
  artifacts:
    - path: "docs/orchestra/hermes/profile-distribution/distribution.yaml"
      provides: "Canonical profile catalog manifest for active and reserved workflow roles"
      contains: "reviewer"
    - path: "docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml"
      provides: "Canonical reviewer runtime contract replacing design-time `tech-reviewer` drift"
      contains: "toolsets:"
    - path: ".hermes/profiles/README.md"
      provides: "Project override contract defining `.override.yaml` and `.project.md` inputs"
      contains: ".override.yaml"
    - path: "docs/orchestra/scripts/bin/orch-profile-sync"
      provides: "Assembler that compiles base profiles and project overrides into project-scoped Hermes runtime output"
      contains: ".hermes/projects/"
    - path: "docs/orchestra/scripts/tests/test-profile-packaging.sh"
      provides: "Proof that project override merge and SOUL assembly work without global pollution"
      contains: "SOUL order"
    - path: "docs/orchestra/scripts/tests/test-project-isolation.sh"
      provides: "Proof that two project slugs do not share board/workspace/profile/memory state"
      contains: "memory_namespace"
    - path: ".planning/phases/21-profiles-overrides-board-isolation/21-VERIFICATION.md"
      provides: "Phase 21 evidence summary and inherited-blocker classification"
      contains: "Ready for $gsd-execute-phase closeout"
  key_links:
    - from: "docs/orchestra/hermes/profile-distribution/"
      to: ".hermes/profiles/"
      via: "canonical base profiles are compiled with repo-local overrides"
      pattern: ".override.yaml"
    - from: "docs/orchestra/scripts/bin/orch-profile-sync"
      to: "docs/orchestra/scripts/bin/orch-init"
      via: "project initialization triggers project-scoped Hermes runtime assembly"
      pattern: ".hermes/projects/{project_slug}/"
    - from: "docs/orchestra/scripts/lib/orch-common.sh"
      to: "docs/orchestra/scripts/tests/test-project-isolation.sh"
      via: "shared slug derivation drives board/workspace/memory checks"
      pattern: "project:{project_slug}"
    - from: ".planning/phases/19-hermes-workflow-design/DESIGN.md"
      to: "docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml"
      via: "Phase 19 role design is normalized into executable runtime profile artifacts"
      pattern: "reviewer"
---

<objective>
Execute Phase 21 as the packaging-and-isolation layer for the Hermes-native workflow MVP.

Purpose: satisfy PROF-01, PROF-02, FLOW-02, and MEM-01 by materializing the workflow profile catalog, compiling project-specific overrides into isolated project runtimes, and locking the naming contract that Phase 22 routing will depend on.
Output: checked-in profile catalog sources, project override contract, project runtime assembly helper, isolation tests, and Phase 21 verification evidence ready for milestone progression.
</objective>

<execution_context>
Use the GSD execute-plan workflow. Keep Phase 21 narrow: deliver profile packaging, override merge, and project isolation contracts only. Do not implement routing logic, risk policy enforcement, worker lifecycle recovery, or observability collection in this phase.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/21-profiles-overrides-board-isolation/21-CONTEXT.md
@.planning/phases/21-profiles-overrides-board-isolation/21-DISCUSSION-LOG.md
@.planning/phases/21-profiles-overrides-board-isolation/21-RESEARCH.md
@.planning/phases/21-profiles-overrides-board-isolation/21-VALIDATION.md
@.planning/phases/20-capability-verification-boundary-lock/20-CAPABILITY-MATRIX.md
@.planning/phases/20-capability-verification-boundary-lock/20-VERIFICATION.md
@.planning/phases/19-hermes-workflow-design/DESIGN.md
@.planning/phases/19-hermes-workflow-design/REQUIREMENTS.md
@.planning/phases/19-hermes-workflow-design/WORKFLOW-EXPLAINED.md
@.planning/phases/19-hermes-workflow-design/WORKFLOW-ASCII-DIAGRAMS.md
@reference/hermes-docs-index/SKILL.md
@reference/hermes-docs-index/hermes_docs_index.json
@docs/orchestra/scripts/setup.sh
@docs/orchestra/scripts/bin/orch-init
@docs/orchestra/scripts/bin/orch-start
@docs/orchestra/scripts/bin/orch-status
@docs/orchestra/scripts/lib/orch-common.sh
@docs/orchestra/README.md
@docs/orchestra/WORKFLOW.md

<interfaces>
Required canonical role slugs:

```text
pm
orchestrator
researcher
implementer
reviewer
qa-tester
devops-engineer
sre-observer
pm-researcher
product-designer
growth-marketer
```

Required locked mappings:

```text
board = {project_slug}
workspace root = .hermes/projects/{project_slug}/
override dir = {repo}/.hermes/profiles/
memory namespace = project:{project_slug}
SOUL assembly order = global -> project -> role
```

Required override files:

```text
.hermes/profiles/{role}.override.yaml
.hermes/profiles/{role}.project.md
```

Required official references for planning rationale:

```text
https://hermes-agent.nousresearch.com/docs/reference/profile-commands
https://hermes-agent.nousresearch.com/docs/user-guide/profiles
https://hermes-agent.nousresearch.com/docs/user-guide/profile-distributions
https://hermes-agent.nousresearch.com/docs/guides/use-soul-with-hermes
https://hermes-agent.nousresearch.com/docs/user-guide/features/personality
https://hermes-agent.nousresearch.com/docs/user-guide/features/kanban
https://hermes-agent.nousresearch.com/docs/user-guide/features/memory
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical workflow profile catalog and lock runtime role names</name>
  <files>docs/orchestra/hermes/profile-distribution/, .planning/phases/19-hermes-workflow-design/DESIGN.md</files>
  <read_first>
    Read 21-CONTEXT.md decisions D-21-01 through D-21-14, 21-RESEARCH.md findings 1 through 5, DESIGN.md sections 3.2 through 3.7, and the Phase 20 matrix row proving the official profile surface. Also review the official references for profiles and profile distributions.
  </read_first>
  <action>
    Create a checked-in canonical catalog under `docs/orchestra/hermes/profile-distribution/` with 11 role directories. Every role must have `config.yaml` and `SOUL.md`. Normalize the runtime role name to `reviewer`; `tech-reviewer` may remain only as a compatibility note in planning docs, not as a generated profile directory.

    Active roles:

```text
pm
orchestrator
researcher
implementer
reviewer
qa-tester
devops-engineer
sre-observer
```

    Reserved roles:

```text
pm-researcher
product-designer
growth-marketer
```

    Reserved roles must stay disabled with `toolsets.enabled: []` and `model: none`. Update the relevant Phase 19 design text only as needed to eliminate the `tech-reviewer` runtime naming ambiguity. Do not generate project-specific runtime output yet.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
root=docs/orchestra/hermes/profile-distribution
test -f "$root/distribution.yaml"
for role in \
  pm orchestrator researcher implementer reviewer qa-tester devops-engineer sre-observer \
  pm-researcher product-designer growth-marketer; do
  test -f "$root/profiles/$role/config.yaml"
  test -f "$root/profiles/$role/SOUL.md"
done
test ! -e "$root/profiles/tech-reviewer"
for reserved in pm-researcher product-designer growth-marketer; do
  rg -F "enabled: []" "$root/profiles/$reserved/config.yaml" >/dev/null
  rg -F "model: none" "$root/profiles/$reserved/config.yaml" >/dev/null
done
'
    </automated>
  </verify>
  <acceptance_criteria>
    The repository contains a canonical 11-role profile catalog, runtime naming is normalized to `reviewer`, and reserved roles are statically disabled. No project-specific generated home is committed in this task.
  </acceptance_criteria>
  <done>PROF-02 has a reviewable runtime catalog instead of a design-only table.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the project override contract and project-home assembly helper</name>
  <files>.hermes/profiles/, docs/orchestra/scripts/bin/orch-profile-sync, docs/orchestra/scripts/lib/orch-common.sh, docs/orchestra/scripts/setup.sh</files>
  <read_first>
    Read Task 1 catalog output, 21-RESEARCH.md findings 2, 3, and 6, 21-VALIDATION.md Task 21-01-02, and DESIGN.md section 3.6. Reconfirm the locked merge semantics from 21-CONTEXT.md.
  </read_first>
  <action>
    Add the project override contract under `.hermes/profiles/` and implement a helper named `orch-profile-sync` that assembles canonical base profiles plus project overrides into `.hermes/projects/{project_slug}/`.

    Required merge behavior:

```text
model: project override replaces base
toolsets.enabled/disabled: merged sets with project override priority
SOUL: global -> project -> role
```

    Required override inputs:

```text
.hermes/profiles/{role}.override.yaml
.hermes/profiles/{role}.project.md
```

    The helper must compile project-scoped runtime output without writing project-specific customizations into `~/.hermes/profiles/`. If setup/install scripts need adjustment so the helper is available after installation, update them in the minimal way necessary.
  </action>
  <verify>
    <automated>
rtk docs/orchestra/scripts/tests/test-profile-packaging.sh
    </automated>
  </verify>
  <acceptance_criteria>
    Base profile sources and project overrides compile into a generated project runtime tree, SOUL assembly order is deterministic, and project customizations do not leak into the user's global Hermes profile directory.
  </acceptance_criteria>
  <done>PROF-01 is backed by executable merge logic instead of a prose-only contract.</done>
</task>

<task type="auto">
  <name>Task 3: Wire `project_slug` isolation into orchestra metadata and helper flows</name>
  <files>docs/orchestra/scripts/bin/orch-init, docs/orchestra/scripts/bin/orch-start, docs/orchestra/scripts/bin/orch-status, docs/orchestra/scripts/lib/orch-common.sh, docs/orchestra/scripts/tests/test-init-start-status.sh, docs/orchestra/scripts/tests/test-project-isolation.sh</files>
  <read_first>
    Read 21-CONTEXT.md decisions D-21-04 through D-21-14, 21-RESEARCH.md findings 1, 7, and 8, current `orch-init`/`orch-start`/`orch-status`, and 21-VALIDATION.md Task 21-01-03.
  </read_first>
  <action>
    Extend orchestra project metadata so every initialized project records the locked isolation mapping:

```text
project_slug
board_slug
workspace_root
override_dir
memory_namespace
```

    `orch-init` must create `.hermes/projects/{project_slug}/` and `.hermes/profiles/` when missing, persist the isolation metadata, and trigger or prepare the project-home assembly flow. `orch-start` and `orch-status` must surface the same derived values consistently. Keep this task scoped to naming/isolation plumbing; do not add dispatcher logic, backpressure, or memory-promotion workflow behavior here.
  </action>
  <verify>
    <automated>
rtk docs/orchestra/scripts/tests/test-project-isolation.sh
    </automated>
  </verify>
  <acceptance_criteria>
    Two different project slugs generate distinct workspace roots, board slugs, memory namespaces, and assembled runtime outputs. The shared contract is readable from stable project metadata instead of repeated string concatenation.
  </acceptance_criteria>
  <done>FLOW-02 and MEM-01 now have concrete slug-derived isolation plumbing.</done>
</task>

<task type="auto">
  <name>Task 4: Update docs, traceability, and Phase 21 verification evidence</name>
  <files>docs/orchestra/README.md, docs/orchestra/WORKFLOW.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/phases/21-profiles-overrides-board-isolation/21-VERIFICATION.md</files>
  <read_first>
    Read the diffs from Tasks 1 through 3, 21-VALIDATION.md Task 21-01-04, and Phase 20's inherited blocker note in STATE.md / 20-VERIFICATION.md.
  </read_first>
  <action>
    Update user-facing docs and planning traceability to reflect the implemented Phase 21 contract: canonical role names, profile catalog location, override file contract, project workspace root, and memory namespace rule. Write `21-VERIFICATION.md` with the final evidence summary, and explicitly classify any residual `rtk make test` failure as either a new Phase 21 regression or the inherited `upstream-status` pin mismatch from Phase 20.

    Only after the targeted Phase 21 tests pass may you mark:

```text
PROF-01
PROF-02
FLOW-02
MEM-01
```

    complete in planning traceability.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
rg -F "PROF-01" .planning/REQUIREMENTS.md >/dev/null
rg -F "PROF-02" .planning/REQUIREMENTS.md >/dev/null
rg -F "FLOW-02" .planning/REQUIREMENTS.md >/dev/null
rg -F "MEM-01" .planning/REQUIREMENTS.md >/dev/null
rg -F "reviewer" docs/orchestra/README.md docs/orchestra/WORKFLOW.md >/dev/null
test -f .planning/phases/21-profiles-overrides-board-isolation/21-VERIFICATION.md
'

rtk make test
    </automated>
  </verify>
  <acceptance_criteria>
    Docs and planning state describe the same isolation contract as the implementation, Phase 21 verification evidence exists, and residual repo-gate failures are accurately attributed.
  </acceptance_criteria>
  <done>Phase 21 is documented, traceable, and ready to hand off to Phase 22 without naming or isolation drift.</done>
</task>

</tasks>
