---
phase: 13
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - AGENTS.md
  - CLAUDE.md
  - .planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - MIGR-01
---

# Phase 13 Plan 01: Evidence Audit & Discoverability

<objective>
Generate a repository state snapshot and complete path-reference inventory, create an explicit root-directory index (`README.md`), and append Dev Orchestra navigation to `AGENTS.md` and `CLAUDE.md` without overwriting existing managed blocks.
</objective>

<review_feedback_integrated>
- `13-EVIDENCE.md` must include both a readable summary and the complete raw match table for `docs/hermes-dev-orchestra` references.
- Category values must cover actual referenced path types, including `scripts/setup.sh`; do not force unmatched paths into the original four-category set.
- `AGENTS.md` must list all actual `orch-*` helper entrypoints present in `docs/hermes-dev-orchestra/scripts/bin/`, including `orch-bus-loop` and `orch-verify`.
- L3/L4 wording must describe the real blocking path: `escalation.md` / `claude-decision.md` -> `orch-bus-loop` -> pending decision -> `orch-decisions` / `orch-approve` / `orch-reject`; `orch-risk-check` is a classifier/helper, not the blocking mechanism itself.
- Verification must account for pre-existing dirty worktree state instead of claiming the whole repository diff only contains Phase 13 files.
</review_feedback_integrated>

<must_haves>
- `13-EVIDENCE.md` exists with repository snapshot, pre-existing worktree attribution, reference summary, and complete path-reference inventory for every current `rg -n "docs/hermes-dev-orchestra" --type md --type sh --type json` match.
- Root `README.md` exists as a lightweight landing page with navigation links to enhancement-layer docs.
- `AGENTS.md` has a `## Hermes Dev Orchestra` section appended after all GSD managed blocks.
- `AGENTS.md` lists all 11 actual helper entrypoints: `orch-init`, `orch-start`, `orch-stop`, `orch-status`, `orch-bus-loop`, `orch-risk-check`, `orch-audit`, `orch-decisions`, `orch-approve`, `orch-reject`, `orch-verify`.
- `CLAUDE.md` references `AGENTS.md` (Dev Orchestra section) and `.planning/SPEC.md` as canonical authorities.
</must_haves>

<threat_model>
This phase is documentation/planning work only; it does not execute runtime helpers or modify production state.

Threats and mitigations:
- **Managed-block overwrite risk (MEDIUM):** appending to `AGENTS.md` could accidentally alter GSD managed blocks. Mitigate by reading `AGENTS.md` first, appending only after `<!-- GSD:profile-end -->`, and verifying all existing `<!-- GSD:* -->` markers remain present.
- **Misleading safety authority wording (MEDIUM):** describing `orch-risk-check` as the L3/L4 blocker would misdocument the system. Mitigate by naming `escalation.md`, `claude-decision.md`, `orch-bus-loop`, pending decisions, `orch-decisions`, `orch-approve`, and `orch-reject` as the actual blocking/decision path.
- **Unrelated dirty-worktree confusion (LOW):** existing staged/untracked files may be mistaken for Phase 13 outputs. Mitigate by recording the baseline `git status --short --branch` before edits and adding an attribution table in `13-EVIDENCE.md`.
- **Scope creep (LOW):** this phase must not migrate paths or change runtime scripts. If execution would require moving `docs/hermes-dev-orchestra/` or editing `docs/hermes-dev-orchestra/scripts/*`, stop and defer to Phase 14+.
</threat_model>

<tasks>

### Task 1 - Generate 13-EVIDENCE.md

<type>execute</type>

<read_first>
- `.planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md` (if it already exists, preserve any prior evidence unless intentionally regenerating the file)
- `.planning/phases/13-evidence-audit-and-discoverability/13-CONTEXT.md` (D-13-07 through D-13-09 for evidence format)
- `.planning/phases/13-evidence-audit-and-discoverability/13-REVIEWS.md` (review feedback on inventory scale and category coverage)
- `.planning/ROADMAP.md` (Phase 13 success criteria)
- `.planning/REQUIREMENTS.md` (DISC-01, DISC-02, MIGR-01)
</read_first>

<action>
Create `.planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md`.

Run and record these commands at execution time:

1. `git branch --show-current`
2. `git rev-parse HEAD`
3. `git status --short --branch`
4. `git log --oneline -5`
5. `rg -n "docs/hermes-dev-orchestra" --type md --type sh --type json`

Before editing other files, capture the current `git status --short --branch` output as the worktree baseline. In `13-EVIDENCE.md`, include this structure:

```markdown
# Phase 13 Evidence: Repository State & Path References

**Generated:** YYYY-MM-DD

## Repository Snapshot

- **Branch:** {branch}
- **Commit:** {sha}
- **Status at audit start:**
  ```
  {git status --short --branch output}
  ```
- **Recent commits:**
  ```
  {git log --oneline -5 output}
  ```

## Pre-existing Worktree Attribution

| Status | Path | Attribution |
|--------|------|-------------|
| A | .planning/BACKLOG-COLLABORATIVE-PLANNING.md | Existing staged planning artifact; not created by Task 1 |
| M | .planning/STATE.md | Existing planning state update; preserve and update only if recording planning completion |
| A | .planning/phases/13-evidence-audit-and-discoverability/13-CONTEXT.md | Existing Phase 13 context artifact |
| A | .planning/phases/13-evidence-audit-and-discoverability/13-DISCUSSION-LOG.md | Existing Phase 13 discussion artifact |
| A | gsd_commands_reference.md | Existing GSD command reference artifact |

## Path Reference Summary

- **Search command:** `rg -n "docs/hermes-dev-orchestra" --type md --type sh --type json`
- **Total matches:** {line count from rg output}

| Referenced Path | Category | Match Count |
|-----------------|----------|-------------|
| {unique referenced path or prefix} | {category} | {count} |

## Path Reference Inventory

| File | Line | Referenced Path | Context Snippet | Category |
|------|------|-----------------|-----------------|----------|
```

For the `Path Reference Inventory` table, include one row for every line returned by `rg -n "docs/hermes-dev-orchestra" --type md --type sh --type json`. The current pre-plan probe found 43 matches; do not hard-code that number during execution. Recompute it and make the `Total matches` value equal the number of inventory rows.

Use these category values based on the referenced path, not the source file containing the reference:

- `scripts-bin` for `docs/hermes-dev-orchestra/scripts/bin/*`
- `scripts-lib` for `docs/hermes-dev-orchestra/scripts/lib/*`
- `scripts-setup` for `docs/hermes-dev-orchestra/scripts/setup.sh`
- `scripts-tests` for `docs/hermes-dev-orchestra/scripts/tests/*`
- `skills` for `docs/hermes-dev-orchestra/skills/*`
- `docs` for `docs/hermes-dev-orchestra/README.md`, `docs/hermes-dev-orchestra/WORKFLOW.md`, `docs/hermes-dev-orchestra/hermes/SOUL.md`, and `docs/COVERAGE-MATRIX.md`
- `other` only if a current match does not fit the categories above; if used, add a note explaining the path

Escape pipe characters in context snippets as `\|` so the Markdown table remains valid.
</action>

<acceptance_criteria>
- `13-EVIDENCE.md` exists at `.planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md`
- File contains `## Repository Snapshot`
- File contains `## Pre-existing Worktree Attribution`
- File contains `## Path Reference Summary`
- File contains `## Path Reference Inventory`
- Inventory table header is exactly `| File | Line | Referenced Path | Context Snippet | Category |`
- File contains the literal search command ``rg -n "docs/hermes-dev-orchestra" --type md --type sh --type json``
- Inventory rows include at least one `scripts-bin` reference and at least one `docs` reference
- If `scripts/setup.sh` appears in the `rg` output, its row is categorized as `scripts-setup`
- No inventory row uses category values outside `scripts-bin`, `scripts-lib`, `scripts-setup`, `scripts-tests`, `skills`, `docs`, `other`
</acceptance_criteria>

---

### Task 2 - Create Root README.md

<type>execute</type>

<read_first>
- `README.md` (if it already exists, preserve unrelated content or stop for guidance before replacing)
- `docs/hermes-dev-orchestra/README.md` (first 20 lines for project description)
- `docs/hermes-dev-orchestra/WORKFLOW.md` (navigation target)
- `.planning/ROADMAP.md` (for current v1.2 status context)
</read_first>

<action>
Create `/data/hermes/README.md` with this exact structure:

```markdown
# Hermes Dev Orchestra

> **Status:** v1.2 migration in progress (2026-04-28)
>
> This repository is transitioning from v1.1 upstream integration to v1.2 normalization.
> See `.planning/ROADMAP.md` for current phase tracking.

## What This Is

Hermes Dev Orchestra is a single-developer, multi-project AI development orchestration system.
It coordinates Hermes Agent, Claude Code CLI, and Codex CLI through a per-project file bus.

## Documentation

| Document | Description |
|----------|-------------|
| [`docs/hermes-dev-orchestra/README.md`](docs/hermes-dev-orchestra/README.md) | Product behavior baseline and architecture |
| [`docs/hermes-dev-orchestra/WORKFLOW.md`](docs/hermes-dev-orchestra/WORKFLOW.md) | Installation and usage guide |
| [`AGENTS.md`](AGENTS.md) | Agent rules and role boundaries |
| [`.planning/SPEC.md`](.planning/SPEC.md) | Canonical specification |
| [`.planning/ROADMAP.md`](.planning/ROADMAP.md) | Development roadmap and phase tracking |

## Quick Navigation

- **Setup**: See `docs/hermes-dev-orchestra/WORKFLOW.md`
- **Agent rules**: See `AGENTS.md`
- **Specs**: See `.planning/SPEC.md`
```

Keep the root README lightweight. Do not include install commands, `setup.sh` invocation, quick-start steps, or detailed usage instructions.
</action>

<acceptance_criteria>
- `README.md` exists at repository root (`/data/hermes/README.md`)
- Contains `> **Status:** v1.2 migration in progress`
- Contains `[docs/hermes-dev-orchestra/README.md](docs/hermes-dev-orchestra/README.md)`
- Contains `[docs/hermes-dev-orchestra/WORKFLOW.md](docs/hermes-dev-orchestra/WORKFLOW.md)`
- Contains `[AGENTS.md](AGENTS.md)`
- Contains `[.planning/SPEC.md](.planning/SPEC.md)`
- Does not contain `bash docs/hermes-dev-orchestra/scripts/setup.sh`
- Does not contain `## Quick Start`
- Does not contain `## Installation`
</acceptance_criteria>

---

### Task 3 - Append Dev Orchestra Section to AGENTS.md

<type>execute</type>

<read_first>
- `AGENTS.md` (verify existing GSD managed block structure and delimiter pattern)
- `docs/hermes-dev-orchestra/scripts/setup.sh` (helper list source of truth)
- `docs/hermes-dev-orchestra/README.md` (documented helper and L3/L4 flow wording)
- `docs/hermes-dev-orchestra/scripts/bin/orch-bus-loop` (actual L3/L4 pending-decision behavior)
- `.planning/SPEC.md` (AUTH-03 and RISK-03 authority rules)
</read_first>

<action>
Append the following section to the end of `/data/hermes/AGENTS.md`, after all existing content and after `<!-- GSD:profile-end -->`:

```markdown
<!-- hermes-dev-orchestra-start -->
## Hermes Dev Orchestra

Project-specific rules for the Hermes Dev Orchestra adaptation layer.
These complement (not replace) the Architecture section above.

### Package Boundary

- This repository is an **adapter layer**, not a standalone runtime.
- Local entrypoints are limited to `orch-*` helpers: `orch-init`, `orch-start`, `orch-stop`, `orch-status`, `orch-bus-loop`, `orch-risk-check`, `orch-audit`, `orch-decisions`, `orch-approve`, `orch-reject`, `orch-verify`.
- Spec authority lives in `docs/hermes-dev-orchestra/`; `.planning/SPEC.md` is canonical for planning artifacts.

### Agent Role Boundary

- **Hermes** must not auto-approve L3/L4 escalations. Blocking flows through `escalation.md` or high-risk `claude-decision.md`, `orch-bus-loop`, pending decisions, and explicit user action via `orch-decisions`, `orch-approve`, or `orch-reject`.
- **Claude** must not modify upstream `NousResearch/hermes-agent` core code.
- **Codex** must not modify `~/.hermes-orchestra/rules.json`.
- `orch-risk-check` is a risk classifier/helper; it is not a replacement for the L3/L4 blocking and user-decision flow.

### Directory Navigation

| Directory | Purpose |
|-----------|---------|
| `docs/hermes-dev-orchestra/` | Product behavior baseline, SOUL, skills, scripts |
| `.planning/SPEC.md` | Canonical specification |
| `.planning/STATE.md` | Project state and decisions |
<!-- hermes-dev-orchestra-end -->
```

Do not edit, move, reformat, or delete any existing `<!-- GSD:* -->` managed block.
</action>

<acceptance_criteria>
- `AGENTS.md` contains `<!-- hermes-dev-orchestra-start -->`
- `AGENTS.md` contains `<!-- hermes-dev-orchestra-end -->`
- `AGENTS.md` contains `## Hermes Dev Orchestra`
- New section appears after `<!-- GSD:profile-end -->`
- New section contains all 11 helper names: `orch-init`, `orch-start`, `orch-stop`, `orch-status`, `orch-bus-loop`, `orch-risk-check`, `orch-audit`, `orch-decisions`, `orch-approve`, `orch-reject`, `orch-verify`
- New section contains `must not auto-approve L3/L4 escalations`
- New section contains `orch-risk-check` and `risk classifier/helper`
- Existing `<!-- GSD:project-start`, `<!-- GSD:stack-start`, `<!-- GSD:conventions-start`, `<!-- GSD:architecture-start`, `<!-- GSD:skills-start`, `<!-- GSD:workflow-start`, and `<!-- GSD:profile-start` markers remain present
</acceptance_criteria>

---

### Task 4 - Update CLAUDE.md with Cross-References

<type>execute</type>

<read_first>
- `CLAUDE.md` (existing content to find insertion point)
- `AGENTS.md` (after Task 3, verify Dev Orchestra section exists)
- `.planning/SPEC.md` (canonical specification target)
</read_first>

<action>
Append the following section to the end of `/data/hermes/CLAUDE.md`:

```markdown
## Hermes Dev Orchestra References

- Agent rules and boundaries: See `AGENTS.md` -> `## Hermes Dev Orchestra`
- Canonical specification: See `.planning/SPEC.md`
```

If `CLAUDE.md` already ends with a horizontal rule (`---`), insert the section before the final `---`.
</action>

<acceptance_criteria>
- `CLAUDE.md` contains `## Hermes Dev Orchestra References`
- `CLAUDE.md` contains `AGENTS.md`
- `CLAUDE.md` contains `.planning/SPEC.md`
- `CLAUDE.md` does not duplicate the full content of `AGENTS.md`
- `CLAUDE.md` does not duplicate the full content of `.planning/SPEC.md`
</acceptance_criteria>

---

### Task 5 - Verify Deliverables and Diff Scope

<type>execute</type>

<read_first>
- `README.md`
- `AGENTS.md`
- `CLAUDE.md`
- `.planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md`
- `.planning/phases/13-evidence-audit-and-discoverability/13-CONTEXT.md`
</read_first>

<action>
Run the verification commands below after Tasks 1-4 complete:

1. `test -f README.md && echo "OK: README.md exists" || echo "FAIL: README.md missing"`
2. `grep -q "hermes-dev-orchestra-start" AGENTS.md && echo "OK: AGENTS.md delimiter found" || echo "FAIL: AGENTS.md delimiter missing"`
3. `grep -q "orch-bus-loop" AGENTS.md && grep -q "orch-verify" AGENTS.md && echo "OK: AGENTS.md helper list complete" || echo "FAIL: AGENTS.md helper list incomplete"`
4. `grep -q "must not auto-approve L3/L4 escalations" AGENTS.md && echo "OK: AGENTS.md L3/L4 wording found" || echo "FAIL: AGENTS.md L3/L4 wording missing"`
5. `grep -q "Hermes Dev Orchestra References" CLAUDE.md && echo "OK: CLAUDE.md reference found" || echo "FAIL: CLAUDE.md reference missing"`
6. `test -f .planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md && echo "OK: 13-EVIDENCE.md exists" || echo "FAIL: 13-EVIDENCE.md missing"`
7. `grep -q "## Pre-existing Worktree Attribution" .planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md && echo "OK: worktree baseline documented" || echo "FAIL: worktree baseline missing"`
8. `grep -q "scripts-setup" .planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md && echo "OK: scripts-setup category found" || echo "WARN: no scripts-setup category found"`
9. `git diff -- README.md AGENTS.md CLAUDE.md .planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md`

Use the final `git diff -- ...` command to review only Phase 13 deliverable files. Do not require unrelated pre-existing staged files to disappear.
</action>

<acceptance_criteria>
- Verification command 1 prints `OK: README.md exists`
- Verification command 2 prints `OK: AGENTS.md delimiter found`
- Verification command 3 prints `OK: AGENTS.md helper list complete`
- Verification command 4 prints `OK: AGENTS.md L3/L4 wording found`
- Verification command 5 prints `OK: CLAUDE.md reference found`
- Verification command 6 prints `OK: 13-EVIDENCE.md exists`
- Verification command 7 prints `OK: worktree baseline documented`
- Final diff review is scoped to `README.md`, `AGENTS.md`, `CLAUDE.md`, and `13-EVIDENCE.md`
</acceptance_criteria>

</tasks>

<verification>
After all tasks complete, run:

1. `test -f README.md && echo "OK: README.md exists" || echo "FAIL: README.md missing"`
2. `grep -q "hermes-dev-orchestra-start" AGENTS.md && echo "OK: AGENTS.md delimiter found" || echo "FAIL: AGENTS.md delimiter missing"`
3. `grep -q "orch-bus-loop" AGENTS.md && grep -q "orch-verify" AGENTS.md && echo "OK: AGENTS.md helper list complete" || echo "FAIL: AGENTS.md helper list incomplete"`
4. `grep -q "must not auto-approve L3/L4 escalations" AGENTS.md && echo "OK: AGENTS.md L3/L4 wording found" || echo "FAIL: AGENTS.md L3/L4 wording missing"`
5. `grep -q "Hermes Dev Orchestra References" CLAUDE.md && echo "OK: CLAUDE.md reference found" || echo "FAIL: CLAUDE.md reference missing"`
6. `test -f .planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md && echo "OK: 13-EVIDENCE.md exists" || echo "FAIL: 13-EVIDENCE.md missing"`
7. `grep -q "## Path Reference Inventory" .planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md && echo "OK: inventory heading found" || echo "FAIL: inventory heading missing"`
</verification>

<success_criteria>
- [ ] `README.md` is a lightweight landing page with status banner and navigation links.
- [ ] `AGENTS.md` retains all existing GSD managed blocks and has appended Dev Orchestra section.
- [ ] `AGENTS.md` lists all 11 actual `orch-*` helper entrypoints, including `orch-bus-loop` and `orch-verify`.
- [ ] `AGENTS.md` states that L3/L4 escalations must not be auto-approved and names the pending-decision/user-action flow.
- [ ] `CLAUDE.md` points to `AGENTS.md` and `.planning/SPEC.md` without duplicating content.
- [ ] `13-EVIDENCE.md` contains repository snapshot, pre-existing worktree attribution, path-reference summary, and complete inventory.
- [ ] All five tasks pass their acceptance criteria.
- [ ] Final diff review is scoped to Phase 13 deliverables instead of being invalidated by pre-existing dirty worktree changes.
</success_criteria>
