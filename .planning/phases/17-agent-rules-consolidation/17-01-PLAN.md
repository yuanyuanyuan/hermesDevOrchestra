---
phase: 17-agent-rules-consolidation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
must_haves:
  truths:
    - "AGENTS.md preserves existing GSD managed sections and contains exactly one delimited Dev Orchestra block."
    - "AGENTS.md states the Dev Orchestra Package Boundary and Agent Role Boundary with the current orch-* helper surface."
    - "AGENTS.md keeps L3/L4 no-auto-approval wording intact."
    - "CLAUDE.md points to AGENTS.md and .planning/SPEC.md without duplicating the Dev Orchestra rule set."
    - "Static agent-rule checks and rtk make test both pass."
  artifacts:
    - path: "AGENTS.md"
      provides: "Agent rule surface with managed sections, Dev Orchestra boundaries, helper list, and L3/L4 wording"
      contains: "<!-- hermes-dev-orchestra-start -->"
    - path: "CLAUDE.md"
      provides: "Claude-facing authority pointer file"
      contains: "Hermes Dev Orchestra References"
  key_links:
    - from: "AGENTS.md"
      to: "docs/orchestra/scripts/bin/orch-*"
      via: "static helper inventory comparison"
      pattern: "find docs/orchestra/scripts/bin -maxdepth 1 -type f -name orch-*"
    - from: "AGENTS.md"
      to: "specs/commands.md"
      via: "helper names must appear in both surfaces"
      pattern: "grep -Fq \"$cmd\" AGENTS.md && grep -Fq \"$cmd\" specs/commands.md"
    - from: "AGENTS.md"
      to: "specs/risk-decisions.md"
      via: "L3/L4 no-auto-approval wording check"
      pattern: "no timeout-based or fallback auto-approval path"
    - from: "CLAUDE.md"
      to: ".planning/SPEC.md"
      via: "authority pointer"
      pattern: "grep -Fq .planning/SPEC.md CLAUDE.md"
---

<objective>
Verify and, only if required by failing checks, repair the Phase 17 agent-rule convergence surface.

Purpose: Satisfy AGNT-01 and AGNT-02 without broad rewrites, preserving existing managed sections and keeping `CLAUDE.md` pointer-only per D-17-01 through D-17-10.
Output: Passing static agent-rule checks, passing `rtk make test`, and a Phase 17 summary describing whether source edits were needed.
</objective>

<execution_context>
@/home/stark/.codex/get-shit-done/workflows/execute-plan.md
@/home/stark/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/SPEC.md
@.planning/phases/17-agent-rules-consolidation/17-CONTEXT.md
@.planning/phases/17-agent-rules-consolidation/17-RESEARCH.md
@.planning/phases/17-agent-rules-consolidation/17-VALIDATION.md
@.planning/phases/17-agent-rules-consolidation/17-PATTERNS.md
@.planning/phases/13-evidence-audit-and-discoverability/13-01-SUMMARY.md
@.planning/phases/16-makefile-dev-workflow/16-01-SUMMARY.md
@AGENTS.md
@CLAUDE.md
@Makefile
@specs/commands.md
@specs/risk-decisions.md

<interfaces>
<!-- Current fixed surfaces the executor must preserve. -->

AGENTS.md required Dev Orchestra delimiters:
```markdown
<!-- hermes-dev-orchestra-start -->
<!-- hermes-dev-orchestra-end -->
```

Current required local helper surface:
```text
orch-approve orch-audit orch-bus-loop orch-decisions orch-init orch-reject orch-risk-check orch-start orch-status orch-stop orch-verify
```

CLAUDE.md required pointer section:
```markdown
## Hermes Dev Orchestra References

- Agent rules and boundaries: See `AGENTS.md` -> `## Hermes Dev Orchestra`
- Canonical specification: See `.planning/SPEC.md`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run static agent-rule convergence checks</name>
  <files>AGENTS.md, CLAUDE.md, Makefile, specs/commands.md, specs/risk-decisions.md, docs/orchestra/scripts/bin/orch-*</files>
  <read_first>
    Read `AGENTS.md`, `CLAUDE.md`, `Makefile`, `specs/commands.md`, `specs/risk-decisions.md`, and list `docs/orchestra/scripts/bin/orch-*`. Also read `17-CONTEXT.md` decisions D-17-01 through D-17-10 before interpreting failures.
  </read_first>
  <action>
    Run the exact static check below before editing anything. This implements D-17-02, D-17-09, and D-17-10. If it passes, record that `AGENTS.md` and `CLAUDE.md` need no source edits. If it fails, capture the failing assertion and continue to Task 2; do not create `docs/orchestra/scripts/tests/test-agent-rules.sh` unless an existing project maintainer has already added it outside this plan.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
for needle in "<!-- GSD:project-start" "<!-- GSD:project-end -->" "<!-- GSD:stack-start" "<!-- GSD:stack-end -->" "<!-- GSD:workflow-start" "<!-- GSD:workflow-end -->" "<!-- GSD:profile-start -->" "<!-- GSD:profile-end -->"; do
  grep -Fq "$needle" AGENTS.md
done
[ "$(grep -Fc "<!-- hermes-dev-orchestra-start -->" AGENTS.md)" = "1" ]
[ "$(grep -Fc "<!-- hermes-dev-orchestra-end -->" AGENTS.md)" = "1" ]
grep -Fq "### Package Boundary" AGENTS.md
grep -Fq "### Agent Role Boundary" AGENTS.md
grep -Fq "This repository is an **adapter layer**, not a standalone runtime." AGENTS.md
grep -Fq "must not auto-approve L3/L4 escalations" AGENTS.md
grep -Fq "Codex** must not modify \`~/.hermes-orchestra/rules.json\`" AGENTS.md
grep -Fq "Claude** must not modify upstream \`NousResearch/hermes-agent\` core code" AGENTS.md
expected="orch-approve orch-audit orch-bus-loop orch-decisions orch-init orch-reject orch-risk-check orch-start orch-status orch-stop orch-verify"
actual="$(find docs/orchestra/scripts/bin -maxdepth 1 -type f -name "orch-*" -printf "%f\n" | sort | paste -sd " " -)"
[ "$actual" = "$expected" ] || { printf "helper mismatch\nexpected: %s\nactual: %s\n" "$expected" "$actual"; exit 1; }
for cmd in $expected; do
  test -x "docs/orchestra/scripts/bin/$cmd"
  grep -Fq "$cmd" AGENTS.md
  grep -Fq "$cmd" specs/commands.md
done
grep -Fq "L3 and L4 decisions have no timeout-based or fallback auto-approval path" specs/risk-decisions.md
grep -Fq "Hermes Dev Orchestra References" CLAUDE.md
grep -Fq "See \`AGENTS.md\` -> \`## Hermes Dev Orchestra\`" CLAUDE.md
grep -Fq "See \`.planning/SPEC.md\`" CLAUDE.md
! grep -Fq "Local entrypoints are limited to" CLAUDE.md
! grep -Fq "must not auto-approve L3/L4 escalations" CLAUDE.md
'
    </automated>
  </verify>
  <acceptance_criteria>
    The command exits 0, or its failing line identifies a concrete missing marker, heading, helper, L3/L4 phrase, or `CLAUDE.md` pointer. No file is modified during this task. The actual helper inventory is exactly `orch-approve orch-audit orch-bus-loop orch-decisions orch-init orch-reject orch-risk-check orch-start orch-status orch-stop orch-verify`.
  </acceptance_criteria>
  <done>Static rule convergence is proven green, or the exact source-edit gap is known before any patch is attempted.</done>
</task>

<task type="auto">
  <name>Task 2: Patch only exact AGENTS.md or CLAUDE.md gaps</name>
  <files>AGENTS.md, CLAUDE.md</files>
  <read_first>
    Re-read only the relevant failing section from `AGENTS.md` or `CLAUDE.md` identified by Task 1. Re-read `17-CONTEXT.md` decisions D-17-03 through D-17-08 before applying any patch.
  </read_first>
  <action>
    If Task 1 passed, do not edit `AGENTS.md` or `CLAUDE.md`; record "no source edit required" in the summary. If Task 1 failed because `AGENTS.md` is missing Dev Orchestra boundary content, patch only inside the `<!-- hermes-dev-orchestra-start -->` / `<!-- hermes-dev-orchestra-end -->` block per D-17-03, D-17-04, and D-17-05. Required `AGENTS.md` values are: heading `### Package Boundary`; heading `### Agent Role Boundary`; helper list `orch-init`, `orch-start`, `orch-stop`, `orch-status`, `orch-bus-loop`, `orch-risk-check`, `orch-audit`, `orch-decisions`, `orch-approve`, `orch-reject`, `orch-verify`; phrase `must not auto-approve L3/L4 escalations`; phrase `Claude** must not modify upstream \`NousResearch/hermes-agent\` core code`; phrase `Codex** must not modify \`~/.hermes-orchestra/rules.json\``. If Task 1 failed because `CLAUDE.md` is missing authority pointers, patch only the pointer section per D-17-06, D-17-07, and D-17-08 using the exact two bullets shown in the context interfaces block. Do not copy Package Boundary, Agent Role Boundary, helper inventory, or L3/L4 rule prose into `CLAUDE.md`. Do not modify `Makefile`, `specs/*.md`, `docs/orchestra/scripts/bin/*`, `docs/orchestra/config/rules.json`, `~/.hermes-orchestra/rules.json`, or upstream Hermes Agent core.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
grep -Fq "<!-- GSD:profile-end -->" AGENTS.md
[ "$(grep -Fc "<!-- hermes-dev-orchestra-start -->" AGENTS.md)" = "1" ]
[ "$(grep -Fc "<!-- hermes-dev-orchestra-end -->" AGENTS.md)" = "1" ]
grep -Fq "### Package Boundary" AGENTS.md
grep -Fq "### Agent Role Boundary" AGENTS.md
grep -Fq "must not auto-approve L3/L4 escalations" AGENTS.md
grep -Fq "Hermes Dev Orchestra References" CLAUDE.md
grep -Fq "See \`AGENTS.md\` -> \`## Hermes Dev Orchestra\`" CLAUDE.md
grep -Fq "See \`.planning/SPEC.md\`" CLAUDE.md
! grep -Fq "Local entrypoints are limited to" CLAUDE.md
! git diff --name-only -- Makefile specs/commands.md specs/risk-decisions.md docs/orchestra/scripts/bin docs/orchestra/config/rules.json .planning/upstream/hermes-agent-pin.json | grep -q .
'
    </automated>
  </verify>
  <acceptance_criteria>
    `AGENTS.md` contains exactly one Dev Orchestra block and all required boundary strings. `CLAUDE.md` contains `Hermes Dev Orchestra References`, `AGENTS.md`, and `.planning/SPEC.md`, and does not contain duplicated Package Boundary or L3/L4 rule prose. `git diff --name-only -- Makefile specs/commands.md specs/risk-decisions.md docs/orchestra/scripts/bin docs/orchestra/config/rules.json .planning/upstream/hermes-agent-pin.json` prints no paths.
  </acceptance_criteria>
  <done>Either no source edit was needed, or only the exact missing `AGENTS.md` / `CLAUDE.md` content was patched and static checks are green.</done>
</task>

<task type="auto">
  <name>Task 3: Run aggregate verification and write execution summary</name>
  <files>AGENTS.md, CLAUDE.md</files>
  <read_first>
    Read `Makefile`, `17-VALIDATION.md`, `AGENTS.md`, and `CLAUDE.md`. If Task 2 made edits, review the diff for only `AGENTS.md` and `CLAUDE.md` before running the full gate.
  </read_first>
  <action>
    Run the full Phase 17 verification gate per D-17-09. Use `rtk make test` from the repository root. Then review `git diff -- AGENTS.md CLAUDE.md` and create `.planning/phases/17-agent-rules-consolidation/17-01-SUMMARY.md` from the standard summary template, explicitly stating whether Task 2 made no source edits or patched exact gaps. The summary must include the static check command result and `rtk make test` result.
  </action>
  <verify>
    <automated>rtk make test</automated>
    <automated>rtk bash -lc 'set -euo pipefail; git diff --name-only -- AGENTS.md CLAUDE.md; ! git diff --name-only -- docs/orchestra/scripts/bin docs/orchestra/config/rules.json .planning/upstream/hermes-agent-pin.json | grep -q .'</automated>
  </verify>
  <acceptance_criteria>
    `rtk make test` exits 0. The final diff is limited to `AGENTS.md` and/or `CLAUDE.md` when a real Phase 17 gap was patched; if Task 1 passed, `git diff --name-only -- AGENTS.md CLAUDE.md` prints no paths. No diff exists under `docs/orchestra/scripts/bin`, `docs/orchestra/config/rules.json`, or `.planning/upstream/hermes-agent-pin.json`. `.planning/phases/17-agent-rules-consolidation/17-01-SUMMARY.md` exists and records AGNT-01 and AGNT-02 verification.
  </acceptance_criteria>
  <done>Static checks pass, `rtk make test` passes, and the Phase 17 summary documents the exact outcome.</done>
</task>

</tasks>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 17 | Append/consolidate Dev Orchestra rules in `AGENTS.md` without overwriting managed sections | 17-01 | COVERED | Tasks 1-3 verify and conditionally patch the root instruction surfaces. |
| REQ | AGNT-01 | `AGENTS.md` has Package Boundary and Agent Role Boundary without managed-section damage | 17-01 | COVERED | Tasks 1 and 2 check markers, delimiters, headings, helper list, and L3/L4 wording. |
| REQ | AGNT-02 | `CLAUDE.md` points to `AGENTS.md` and `.planning/SPEC.md` without duplicating all rules | 17-01 | COVERED | Tasks 1 and 2 enforce pointer-only content. |
| RESEARCH | Static checks | Managed markers, delimiters, headings, helper surface, L3/L4 wording, and `CLAUDE.md` pointers | 17-01 | COVERED | Task 1 uses the exact inline command recommended by research. |
| RESEARCH | Verification gate | Full local gate must be `rtk make test` | 17-01 | COVERED | Task 3 runs `rtk make test`. |
| RESEARCH | No persistent agent-rules test required | Prefer inline static checks unless a real need appears | 17-01 | COVERED | Task 1 explicitly avoids adding `test-agent-rules.sh`. |
| RESEARCH | Authority boundaries | Do not touch `~/.hermes-orchestra/rules.json` or upstream Hermes Agent core | 17-01 | COVERED | Task 2 action prohibits those mutations; Task 3 checks no repo-side rules/core-adjacent diff. |
| CONTEXT | D-17-01 | Phase is verification and necessary small fixes, not broad rewrite | 17-01 | COVERED | Objective and Task 2 patch rules. |
| CONTEXT | D-17-02 | Verification-only is valid if current files pass | 17-01 | COVERED | Task 2 has an explicit no-edit path. |
| CONTEXT | D-17-03 | Keep Dev Orchestra block concise and delimited | 17-01 | COVERED | Task 2 patches only inside existing delimiters. |
| CONTEXT | D-17-04 | Patch only real gaps | 17-01 | COVERED | Task 1 must fail before Task 2 edits. |
| CONTEXT | D-17-05 | Do not duplicate `.planning/SPEC.md` in `AGENTS.md` | 17-01 | COVERED | Task 2 limits required `AGENTS.md` values to boundary summary strings. |
| CONTEXT | D-17-06 | Keep `CLAUDE.md` pointer-only | 17-01 | COVERED | Task 1 negative checks and Task 2 action. |
| CONTEXT | D-17-07 | `CLAUDE.md` references `AGENTS.md` and `.planning/SPEC.md` | 17-01 | COVERED | Task 1 and Task 2 verify exact bullets. |
| CONTEXT | D-17-08 | Do not copy full rule set into `CLAUDE.md` | 17-01 | COVERED | Task 1 negative checks and Task 2 prohibition. |
| CONTEXT | D-17-09 | Verification includes static checks plus `make test` | 17-01 | COVERED | Task 1 static checks and Task 3 `rtk make test`. |
| CONTEXT | D-17-10 | Static checks cover markers, delimiters, headings, helper list, L3/L4 wording, and `CLAUDE.md` pointers | 17-01 | COVERED | Task 1 command covers each item. |
| CONTEXT | Deferred Ideas | None | 17-01 | NOT APPLICABLE | No deferred idea is planned. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root agent docs -> agent behavior | `AGENTS.md` and `CLAUDE.md` guide future agents, so wording drift can change authority behavior. |
| Derived specs -> root docs | `specs/commands.md` and `specs/risk-decisions.md` are projections used to detect command and risk wording drift. |
| Repository adapter -> user/runtime state | Phase 17 must not mutate `~/.hermes-orchestra/rules.json` or upstream Hermes Agent core. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `AGENTS.md` managed sections | mitigate | Task 1 checks GSD managed markers and Dev Orchestra delimiters before any edit; Task 2 patches only inside the Dev Orchestra block when needed. |
| T-17-02 | Tampering | `CLAUDE.md` authority surface | mitigate | Task 1 verifies pointer-only references and negative-checks duplicated rule prose; Task 2 only restores exact authority bullets. |
| T-17-03 | Elevation of Privilege | L3/L4 decision wording | mitigate | Task 1 checks `AGENTS.md` no-auto-approval wording and `specs/risk-decisions.md` no timeout/fallback auto-approval wording; Task 3 runs risk tests through `rtk make test`. |
| T-17-04 | Tampering | `orch-*` helper command surface | mitigate | Task 1 compares actual executable helper inventory with `AGENTS.md` and `specs/commands.md`. |
| T-17-05 | Tampering | `~/.hermes-orchestra/rules.json` and upstream Hermes Agent core | mitigate | Task 2 forbids these mutations; Task 3 checks no repo-side diff under helper scripts, rules config, or upstream pin manifest. |
| T-17-06 | Repudiation | Phase verification evidence | mitigate | Task 3 writes `17-01-SUMMARY.md` with static check and `rtk make test` results. |
</threat_model>

<verification>
Overall phase checks:

```bash
rtk bash -lc 'set -euo pipefail
for needle in "<!-- GSD:project-start" "<!-- GSD:workflow-end -->" "<!-- hermes-dev-orchestra-start -->" "<!-- hermes-dev-orchestra-end -->" "### Package Boundary" "### Agent Role Boundary" "must not auto-approve L3/L4 escalations"; do grep -Fq "$needle" AGENTS.md; done
for needle in "Hermes Dev Orchestra References" "AGENTS.md" ".planning/SPEC.md"; do grep -Fq "$needle" CLAUDE.md; done
'
rtk make test
```
</verification>

<success_criteria>
- `AGENTS.md` preserves GSD managed sections and exactly one Dev Orchestra block.
- `AGENTS.md` includes Package Boundary, Agent Role Boundary, all 11 current `orch-*` helpers, L3/L4 no-auto-approval wording, upstream core protection, and `~/.hermes-orchestra/rules.json` protection.
- `CLAUDE.md` points to `AGENTS.md` and `.planning/SPEC.md` and does not duplicate Dev Orchestra boundary rules.
- `rtk make test` passes.
- `.planning/phases/17-agent-rules-consolidation/17-01-SUMMARY.md` exists with AGNT-01 and AGNT-02 verification evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-rules-consolidation/17-01-SUMMARY.md`.
</output>
