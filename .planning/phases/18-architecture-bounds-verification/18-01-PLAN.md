---
phase: 18-architecture-bounds-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/SPEC.md
  - specs/file-bus.md
  - docs/orchestra/README.md
  - docs/orchestra/WORKFLOW.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/18-architecture-bounds-verification/18-VERIFICATION.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "All core bus surfaces state that fixed Runtime bus filenames represent one active task slot per project."
    - "All core bus surfaces state that fixed Runtime bus filenames are not a per-project multi-task parallel execution protocol."
    - "Same-project parallelism is marked out of scope for v1.2 and future design areas are named."
    - "\"10x\" is limited to lower coordination overhead across multiple projects for one developer, not same-project parallel Codex execution, team-scale concurrency, or AI-factory throughput."
    - "Static drift checks, rtk make test, and Phase 13-18 traceability checks pass before ARCH-01 and ARCH-02 are marked complete."
  artifacts:
    - path: ".planning/SPEC.md"
      provides: "Canonical architecture boundary for file bus, same-project parallelism, and 10x scope"
      contains: "fixed Runtime bus filenames represent one active task slot per project"
    - path: "specs/file-bus.md"
      provides: "Derived file-bus contract with single active task slot boundary"
      contains: "not a per-project multi-task parallel execution protocol"
    - path: "docs/orchestra/README.md"
      provides: "Human-facing file bus and 10x boundary projection"
      contains: "\"10x\" means lower coordination overhead across multiple projects for one developer"
    - path: "docs/orchestra/WORKFLOW.md"
      provides: "Workflow projection of single-slot Runtime bus semantics"
      contains: "Same-project parallelism is out of scope for v1.2."
    - path: ".planning/REQUIREMENTS.md"
      provides: "ARCH-01 and ARCH-02 completion traceability after verification"
      contains: "| ARCH-01 | Phase 18 | Complete |"
    - path: ".planning/phases/18-architecture-bounds-verification/18-VERIFICATION.md"
      provides: "Phase 18 evidence and milestone closeout readiness"
      contains: "Ready for $gsd-complete-milestone"
  key_links:
    - from: ".planning/SPEC.md"
      to: "specs/file-bus.md"
      via: "fixed bus boundary mirrored from canonical spec into derived spec"
      pattern: "fixed Runtime bus filenames represent one active task slot per project"
    - from: "specs/file-bus.md"
      to: "docs/orchestra/README.md"
      via: "human projection carries same single-slot warning"
      pattern: "not a per-project multi-task parallel execution protocol"
    - from: ".planning/SPEC.md"
      to: "docs/orchestra/WORKFLOW.md"
      via: "future-work boundary appears in workflow projection"
      pattern: "Same-project parallelism is out of scope for v1.2."
    - from: ".planning/PROJECT.md"
      to: "docs/orchestra/README.md"
      via: "10x pressure boundary appears in project and user-facing docs"
      pattern: "\"10x\" means lower coordination overhead across multiple projects for one developer"
---

<objective>
Clarify Phase 18 architecture bounds and close v1.2 verification.

Purpose: satisfy ARCH-01 and ARCH-02 by making the fixed Runtime bus single-slot limitation, future same-project parallelism boundary, and "10x" pressure boundary explicit across canonical, derived, and user-facing docs. Then run milestone closeout checks and record Phase 18 verification evidence.
Output: updated documentation, completed ARCH-01/ARCH-02 traceability, and Phase 18 verification evidence ready for $gsd-complete-milestone.
</objective>

<execution_context>
Use the GSD execute-plan workflow and standard summary template from the local get-shit-done installation. Keep all changes focused on documentation and planning evidence unless a verification failure proves a real runtime drift bug.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/SPEC.md
@.planning/phases/18-architecture-bounds-verification/18-CONTEXT.md
@.planning/phases/18-architecture-bounds-verification/18-RESEARCH.md
@.planning/phases/18-architecture-bounds-verification/18-VALIDATION.md
@.planning/phases/18-architecture-bounds-verification/18-PATTERNS.md
@specs/file-bus.md
@docs/orchestra/README.md
@docs/orchestra/WORKFLOW.md
@docs/COVERAGE-MATRIX.md
@docs/orchestra/scripts/bin/orch-bus-loop
@docs/orchestra/scripts/lib/orch-common.sh
@docs/orchestra/scripts/tests/test-file-bus.sh
@Makefile

<interfaces>
Required exact fixed-bus phrase:

```text
fixed Runtime bus filenames represent one active task slot per project
```

Required exact negative-bus phrase:

```text
not a per-project multi-task parallel execution protocol
```

Required exact future-work phrase:

```text
Same-project parallelism is out of scope for v1.2.
```

Required future design areas:

```text
JSONL/event bus semantics
per-task file namespaces
per-task locks
worktrees or per-task branches
merge/review arbitration
```

Required exact 10x phrase:

```text
"10x" means lower coordination overhead across multiple projects for one developer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document fixed Runtime bus single active slot</name>
  <files>.planning/SPEC.md, specs/file-bus.md, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md</files>
  <read_first>
    Read .planning/SPEC.md BUS-01 through BUS-06, specs/file-bus.md Contract, docs/orchestra/README.md per-project file bus section, docs/orchestra/WORKFLOW.md file bus sections, and 18-CONTEXT.md decisions D-18-01 through D-18-03. Also inspect docs/orchestra/scripts/bin/orch-bus-loop and docs/orchestra/scripts/lib/orch-common.sh only as evidence for current fixed-file behavior.
  </read_first>
  <action>
    Add a concise boundary note to each of .planning/SPEC.md, specs/file-bus.md, docs/orchestra/README.md, and docs/orchestra/WORKFLOW.md. Each note must contain both exact phrases:

```text
fixed Runtime bus filenames represent one active task slot per project
not a per-project multi-task parallel execution protocol
```

    Each note must name the current fixed Runtime bus files:

```text
task.md, codex-question.md, claude-decision.md, escalation.md, codex-result.md, review-result.md
```

    The meaning must be: these files describe the current active task slot for a project; queued or appended tasks can exist in todo/state layers, but the Runtime bus does not represent multiple simultaneously active tasks inside the same project. Do not change runtime scripts, add per-task file namespaces, or alter file ownership tables except for explanatory wording.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
for f in .planning/SPEC.md specs/file-bus.md docs/orchestra/README.md docs/orchestra/WORKFLOW.md; do
  grep -Fq "fixed Runtime bus filenames represent one active task slot per project" "$f"
  grep -Fq "not a per-project multi-task parallel execution protocol" "$f"
  grep -Fq "task.md, codex-question.md, claude-decision.md, escalation.md, codex-result.md, review-result.md" "$f"
done
'
    </automated>
    <automated>
rtk bash -lc 'set -euo pipefail
! git diff --name-only -- docs/orchestra/scripts/bin docs/orchestra/scripts/lib docs/orchestra/scripts/tests | grep -q .
'
    </automated>
  </verify>
  <acceptance_criteria>
    All four core surfaces contain the exact fixed-bus phrase, exact negative-bus phrase, and the full fixed-file list. No files under docs/orchestra/scripts/bin, docs/orchestra/scripts/lib, or docs/orchestra/scripts/tests are changed by this task.
  </acceptance_criteria>
  <done>ARCH-01 has explicit single-slot Runtime bus wording in canonical, derived, and user-facing surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Document future same-project parallelism and 10x boundary</name>
  <files>.planning/SPEC.md, specs/file-bus.md, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md, .planning/PROJECT.md</files>
  <read_first>
    Read .planning/SPEC.md scope section and MULTI-06, .planning/PROJECT.md What This Is and Out of Scope, specs/file-bus.md Contract, docs/orchestra/README.md overview and multi-project sections, docs/orchestra/WORKFLOW.md setup and execution sections, and 18-CONTEXT.md decisions D-18-04 through D-18-09.
  </read_first>
  <action>
    Add short boundary wording without designing v2. At minimum, the repository must contain these exact strings after the patch:

```text
Same-project parallelism is out of scope for v1.2.
JSONL/event bus semantics
per-task file namespaces
per-task locks
worktrees or per-task branches
merge/review arbitration
"10x" means lower coordination overhead across multiple projects for one developer
does not promise same-project parallel Codex execution
team-scale concurrency
AI-factory throughput
```

    Place the future-work boundary in .planning/SPEC.md and specs/file-bus.md, and include concise user-facing versions in docs/orchestra/README.md and docs/orchestra/WORKFLOW.md. Place the 10x boundary in .planning/PROJECT.md and at least one canonical or user-facing architecture surface. Do not add new command contracts, schemas, task namespace designs, lock protocols, worktree setup instructions, or merge algorithms.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
for needle in \
  "Same-project parallelism is out of scope for v1.2." \
  "JSONL/event bus semantics" \
  "per-task file namespaces" \
  "per-task locks" \
  "worktrees or per-task branches" \
  "merge/review arbitration" \
  "\"10x\" means lower coordination overhead across multiple projects for one developer" \
  "does not promise same-project parallel Codex execution" \
  "team-scale concurrency" \
  "AI-factory throughput"; do
  rg -F "$needle" .planning/SPEC.md specs/file-bus.md docs/orchestra/README.md docs/orchestra/WORKFLOW.md .planning/PROJECT.md >/dev/null
done
'
    </automated>
    <automated>
rtk bash -lc 'set -euo pipefail
! rg -n "per-task namespace schema|task namespace schema|merge algorithm|worktree setup command|same-project multi-task implementation" .planning/SPEC.md specs/file-bus.md docs/orchestra/README.md docs/orchestra/WORKFLOW.md .planning/PROJECT.md
'
    </automated>
  </verify>
  <acceptance_criteria>
    The future-work phrase, all five future design areas, the 10x limiting phrase, and the negative same-project/team/AI-factory wording are grep-verifiable. The patch does not introduce v2 implementation details such as schemas, commands, algorithms, or new runtime protocols.
  </acceptance_criteria>
  <done>ARCH-01 future parallelism boundary and ARCH-02 10x pressure boundary are explicit and scoped.</done>
</task>

<task type="auto">
  <name>Task 3: Run closeout verification and update v1.2 traceability</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/phases/18-architecture-bounds-verification/18-VERIFICATION.md</files>
  <read_first>
    Read .planning/REQUIREMENTS.md ARCH-01, ARCH-02, and v1.2 Traceability; .planning/ROADMAP.md Phase 18 and Progress; .planning/PROJECT.md Current State; 18-VALIDATION.md; and the diffs from Tasks 1 and 2.
  </read_first>
  <action>
    Run the full closeout gate before marking requirements complete:

```bash
rtk bash -lc 'set -euo pipefail
for f in .planning/SPEC.md specs/file-bus.md docs/orchestra/README.md docs/orchestra/WORKFLOW.md; do
  grep -Fq "fixed Runtime bus filenames represent one active task slot per project" "$f"
  grep -Fq "not a per-project multi-task parallel execution protocol" "$f"
done
for needle in \
  "Same-project parallelism is out of scope for v1.2." \
  "JSONL/event bus semantics" \
  "per-task file namespaces" \
  "per-task locks" \
  "worktrees or per-task branches" \
  "merge/review arbitration" \
  "\"10x\" means lower coordination overhead across multiple projects for one developer" \
  "does not promise same-project parallel Codex execution"; do
  rg -F "$needle" .planning/SPEC.md specs/file-bus.md docs/orchestra/README.md docs/orchestra/WORKFLOW.md .planning/PROJECT.md >/dev/null
done
'

rtk make test
```

    Only after both commands exit 0, update .planning/REQUIREMENTS.md so ARCH-01 and ARCH-02 are checked and annotated as validated in Phase 18, and update v1.2 Traceability rows to Complete. Update .planning/ROADMAP.md so Phase 18 is complete with 1/1 plans and the v1.2 progress table shows Phase 18 Complete. Update .planning/PROJECT.md Current State to say v1.2 Phase 18 clarified architecture bounds and the milestone is ready for completion. Create 18-VERIFICATION.md with the command evidence, ARCH-01/ARCH-02 mapping, Phase 13-18 traceability review, and this exact line:

```text
Ready for $gsd-complete-milestone
```
  </action>
  <verify>
    <automated>rtk make test</automated>
    <automated>
rtk bash -lc 'set -euo pipefail
grep -Fq "[x] **ARCH-01**" .planning/REQUIREMENTS.md
grep -Fq "[x] **ARCH-02**" .planning/REQUIREMENTS.md
grep -Fq "| ARCH-01 | Phase 18 | Complete |" .planning/REQUIREMENTS.md
grep -Fq "| ARCH-02 | Phase 18 | Complete |" .planning/REQUIREMENTS.md
grep -Fq "- [x] **Phase 18: Architecture Bounds & Verification**" .planning/ROADMAP.md
grep -Fq "| 18. Architecture Bounds & Verification | v1.2 | 1/1 | Complete" .planning/ROADMAP.md
grep -Fq "Ready for $gsd-complete-milestone" .planning/phases/18-architecture-bounds-verification/18-VERIFICATION.md
grep -Fq "ARCH-01" .planning/phases/18-architecture-bounds-verification/18-VERIFICATION.md
grep -Fq "ARCH-02" .planning/phases/18-architecture-bounds-verification/18-VERIFICATION.md
'
    </automated>
  </verify>
  <acceptance_criteria>
    Static drift checks and rtk make test exit 0 before requirement status changes. .planning/REQUIREMENTS.md marks ARCH-01 and ARCH-02 checked and Complete in traceability. .planning/ROADMAP.md marks Phase 18 complete with 1/1 plans. 18-VERIFICATION.md contains the closeout command evidence, ARCH-01, ARCH-02, and Ready for $gsd-complete-milestone.
  </acceptance_criteria>
  <done>v1.2 traceability and Phase 18 verification evidence are ready for milestone completion.</done>
</task>

</tasks>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 18 | Clarify 10x pressure boundary and finish milestone acceptance. | 18-01 | COVERED | Tasks 1-3 update docs, verify, and close traceability. |
| REQ | ARCH-01 | Fixed file-name file bus means single active task; same-project parallelism needs separate design. | 18-01 T1, T2 | COVERED | Task 1 covers single-slot Runtime bus; Task 2 covers future design boundary. |
| REQ | ARCH-02 | 10x promise is single-developer multi-project, each project one active task. | 18-01 T2, T3 | COVERED | Task 2 adds limiting wording; Task 3 verifies and marks complete. |
| CONTEXT | D-18-01 | State fixed-file limitation in SPEC, file-bus spec, README, and WORKFLOW. | 18-01 T1 | COVERED | Four-surface grep enforces it. |
| CONTEXT | D-18-02 | Fixed bus filenames represent current active task slot. | 18-01 T1 | COVERED | Exact phrase required in all four surfaces. |
| CONTEXT | D-18-03 | Queued work may exist outside Runtime bus, but no simultaneous same-project Runtime tasks. | 18-01 T1 | COVERED | Task action requires this meaning. |
| CONTEXT | D-18-04 | Same-project multi-task parallelism is out of scope for v1.2. | 18-01 T2 | COVERED | Exact future-work phrase required. |
| CONTEXT | D-18-05 | Name future design areas. | 18-01 T2 | COVERED | All five design areas are grep targets. |
| CONTEXT | D-18-06 | Do not spec full v2 parallel execution design. | 18-01 T2 | COVERED | Negative grep rejects schemas, commands, algorithms, and implementation wording. |
| CONTEXT | D-18-07 | 10x means lower coordination overhead across projects for one developer. | 18-01 T2 | COVERED | Exact 10x phrase required. |
| CONTEXT | D-18-08 | v1.2 does not promise same-project parallel Codex execution. | 18-01 T2 | COVERED | Negative phrase required. |
| CONTEXT | D-18-09 | v1.2 does not promise team-scale or AI-factory high concurrency. | 18-01 T2 | COVERED | team-scale concurrency and AI-factory throughput required. |
| CONTEXT | D-18-10 | Include complete closeout gate. | 18-01 T3 | COVERED | Static checks, rtk make test, traceability, verification, and complete-milestone readiness are required. |
| CONTEXT | D-18-11 | Check canonical and projection surfaces for drift. | 18-01 T1, T2, T3 | COVERED | Grep spans canonical, derived, README, and WORKFLOW. |
| RESEARCH | Fixed Runtime bus evidence | Runtime scripts and tests use a fixed file set. | 18-01 T1 | COVERED | Runtime sources are read as evidence; scripts are not patched unless drift is proven. |
| RESEARCH | Validation Architecture | Static checks plus rtk make test and traceability checks. | 18-01 T3 | COVERED | Commands are embedded in task verify and overall verification. |
| VALIDATION | Nyquist sampling | Each task has automated verification and feedback latency below 30 seconds for static checks. | 18-01 | COVERED | 18-VALIDATION.md maps all tasks. |
| PATTERNS | Minimal docs-only patch | Follow Phase 17 verification-first pattern and avoid runtime changes. | 18-01 | COVERED | Tasks explicitly forbid script changes unless verification failure proves drift. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical spec to derived/user docs | .planning/SPEC.md is authoritative, while specs/file-bus.md and docs/orchestra/* are projections that can drift. |
| Runtime bus wording to user expectations | Fixed file names may be mistaken for multiple parallel task lanes if not bounded. |
| Future-work notes to v1.2 scope | Mentioning v2 parallelism can accidentally become a v1.2 implementation promise. |
| Milestone traceability to evidence | Requirements must not be marked complete before checks pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Spoofing | File bus docs | mitigate | Task 1 requires exact wording that fixed Runtime bus filenames represent one active task slot per project. |
| T-18-02 | Tampering | Derived/user docs | mitigate | Task 1 checks canonical, derived, README, and WORKFLOW surfaces together. |
| T-18-03 | Elevation of Privilege | Same-project parallelism expectations | mitigate | Task 2 marks same-project parallelism out of scope and lists future design areas without adding implementation. |
| T-18-04 | Repudiation | 10x claim | mitigate | Task 2 requires explicit negative wording for same-project Codex parallelism, team-scale concurrency, and AI-factory throughput. |
| T-18-05 | Tampering | Runtime scripts | mitigate | Tasks 1 and 2 prohibit script changes unless a verification failure proves real drift. |
| T-18-06 | Repudiation | Milestone closeout | mitigate | Task 3 writes Phase 18 verification evidence and only marks ARCH-01/ARCH-02 complete after checks pass. |
</threat_model>

<verification>
Overall phase checks:

```bash
rtk bash -lc 'set -euo pipefail
for f in .planning/SPEC.md specs/file-bus.md docs/orchestra/README.md docs/orchestra/WORKFLOW.md; do
  grep -Fq "fixed Runtime bus filenames represent one active task slot per project" "$f"
  grep -Fq "not a per-project multi-task parallel execution protocol" "$f"
  grep -Fq "task.md, codex-question.md, claude-decision.md, escalation.md, codex-result.md, review-result.md" "$f"
done
for needle in \
  "Same-project parallelism is out of scope for v1.2." \
  "JSONL/event bus semantics" \
  "per-task file namespaces" \
  "per-task locks" \
  "worktrees or per-task branches" \
  "merge/review arbitration" \
  "\"10x\" means lower coordination overhead across multiple projects for one developer" \
  "does not promise same-project parallel Codex execution"; do
  rg -F "$needle" .planning/SPEC.md specs/file-bus.md docs/orchestra/README.md docs/orchestra/WORKFLOW.md .planning/PROJECT.md >/dev/null
done
'

rtk make test

rtk bash -lc 'set -euo pipefail
grep -Fq "| ARCH-01 | Phase 18 | Complete |" .planning/REQUIREMENTS.md
grep -Fq "| ARCH-02 | Phase 18 | Complete |" .planning/REQUIREMENTS.md
grep -Fq "Ready for $gsd-complete-milestone" .planning/phases/18-architecture-bounds-verification/18-VERIFICATION.md
'
```
</verification>

<dependency_graph>
| Task | Needs | Creates | Wave |
|------|-------|---------|------|
| Task 1 | Phase 18 context, file bus docs, runtime evidence | Single-slot bus boundary wording | 1 |
| Task 2 | Task 1 context, scope docs | Future parallelism and 10x boundary wording | 1 |
| Task 3 | Tasks 1 and 2 passing checks | Traceability updates and Phase 18 verification evidence | 1 |

Single-plan execution is intentional because the phase is a milestone closeout documentation and verification pass with one cohesive artifact set.
</dependency_graph>
