---
phase: 14-migration-submodule-adr
plan: "01"
title: Migration and Upstream Pin ADR
type: execute
wave: 1
depends_on:
  - 13-evidence-audit-and-discoverability
files_modified:
  - docs/hermes-dev-orchestra/**
  - docs/orchestra/**
  - README.md
  - AGENTS.md
  - docs/COVERAGE-MATRIX.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/DIRECTION-CORRECTION.md
  - .planning/adr/ADR-001-upstream-pin.md
  - .planning/upstream/hermes-agent-pin.json
  - docs/orchestra/README.md
  - docs/orchestra/WORKFLOW.md
  - docs/orchestra/scripts/setup.sh
  - docs/orchestra/scripts/bin/orch-*
  - docs/orchestra/scripts/lib/orch-common.sh
  - docs/orchestra/scripts/tests/*.sh
  - docs/orchestra/scripts/tests/lib/assert.sh
  - .planning/phases/14-migration-submodule-adr/14-01-SUMMARY.md
autonomous: true
requirements:
  - MIGR-02
  - UPST-01
  - UPST-02
user_setup: []
must_haves:
  truths:
    - "The tracked Dev Orchestra package is moved from docs/hermes-dev-orchestra/ to docs/orchestra/ using git mv per D-14-01 and D-14-02."
    - "No supported compatibility shim, duplicate directory, symlink, or active pointer remains at docs/hermes-dev-orchestra/ per D-14-03."
    - "Actionable current files have zero docs/hermes-dev-orchestra references; historical planning artifacts are reviewed separately as audit-only per D-14-04."
    - "Migrated shell helpers and tests still parse and the migrated smoke runner passes per MIGR-02 and D-14-11."
    - "A repo-local JSON manifest records the upstream hermes-agent pin with the Phase 9 commit and probe evidence per D-14-06 and D-14-07."
    - "ADR-001 compares installer/probe pin, git submodule, manifest pin, and vendor snapshot, accepts manifest pin, and states UPST-02 is not applicable per D-14-05 through D-14-10."
    - "No .gitmodules file or gitlink mode 160000 is introduced because submodule is not selected."
  artifacts:
    - path: "docs/orchestra/README.md"
      provides: "Moved reader-facing product baseline"
    - path: "docs/orchestra/WORKFLOW.md"
      provides: "Moved install and usage guide with docs/orchestra setup command"
    - path: "docs/orchestra/scripts/tests/run-all.sh"
      provides: "Migrated smoke test runner"
    - path: ".planning/upstream/hermes-agent-pin.json"
      provides: "Machine-readable upstream manifest pin"
      contains: "\"commit\": \"023b1bff11c2a01a435f1956a0e2ac1773a065f3\""
    - path: ".planning/adr/ADR-001-upstream-pin.md"
      provides: "Accepted upstream pin strategy ADR"
      contains: "UPST-02 is not applicable"
    - path: ".planning/phases/14-migration-submodule-adr/14-01-SUMMARY.md"
      provides: "Execution evidence, audit-only residual review, and command results"
  key_links:
    - from: "README.md, AGENTS.md, docs/COVERAGE-MATRIX.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/DIRECTION-CORRECTION.md"
      to: "docs/orchestra"
      via: "literal path references"
      pattern: "docs/orchestra"
    - from: "docs/orchestra/scripts/tests/*.sh"
      to: "docs/orchestra/scripts/bin/orch-* and docs/orchestra/scripts/lib/orch-common.sh"
      via: "REPO_ROOT path literals"
      pattern: "REPO_ROOT.*/docs/orchestra/scripts"
    - from: ".planning/adr/ADR-001-upstream-pin.md"
      to: ".planning/upstream/hermes-agent-pin.json"
      via: "manifest path reference and strategy decision"
      pattern: ".planning/upstream/hermes-agent-pin.json"
    - from: ".planning/upstream/hermes-agent-pin.json"
      to: "Phase 16 make upstream-status"
      via: "stable JSON pointers /pin/commit and /pin/observed_version"
      pattern: "\"pin\""
---

<objective>
Execute the locked Phase 14 migration and upstream pin decision in one wave.

Purpose: move the Dev Orchestra adapter package to `docs/orchestra/` without old-path compatibility, then make the upstream `NousResearch/hermes-agent` pin decision machine-readable and documented for downstream workflow tooling.

Output: migrated docs/scripts, updated active references, `.planning/upstream/hermes-agent-pin.json`, `.planning/adr/ADR-001-upstream-pin.md`, and `.planning/phases/14-migration-submodule-adr/14-01-SUMMARY.md`.
</objective>

<execution_context>
@/home/stark/.codex/get-shit-done/workflows/execute-plan.md
@/home/stark/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/DIRECTION-CORRECTION.md
@.planning/phases/14-migration-submodule-adr/14-CONTEXT.md
@.planning/phases/14-migration-submodule-adr/14-RESEARCH.md
@.planning/phases/14-migration-submodule-adr/14-VALIDATION.md
@.planning/phases/14-migration-submodule-adr/14-PATTERNS.md
@.planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md
@.planning/phases/13-evidence-audit-and-discoverability/13-01-SUMMARY.md
@.planning/phases/09-upstream-hermes-agent-baseline/09-01-SUMMARY.md

<interfaces>
Key contracts for execution:

- Phase 14 locked migration target: `docs/hermes-dev-orchestra/` -> `docs/orchestra/`.
- Physical move command required by D-14-02 and MIGR-02: `git mv docs/hermes-dev-orchestra docs/orchestra`.
- Strict active old-path gate:
  `! rg -n "docs/hermes-dev-orchestra" README.md AGENTS.md CLAUDE.md docs .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/DIRECTION-CORRECTION.md`
- Audit-only broad old-path review:
  `rg --hidden -n "docs/hermes-dev-orchestra" --glob '!/.git/*' --glob '!*.zip'`
- Manifest path selected for this plan: `.planning/upstream/hermes-agent-pin.json`.
- ADR path locked by D-14-09: `.planning/adr/ADR-001-upstream-pin.md`.
- `CLAUDE.md` currently has no `docs/hermes-dev-orchestra` match; verify it, but do not edit it unless that precondition changes during execution.
</interfaces>
</context>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Based on Phase 13 evidence, decide/execute directory migration and write upstream pin ADR | 14-01 | COVERED | Tasks 1-3 deliver migration, manifest, ADR, and verification. |
| REQ | MIGR-02 | Directory migration must use `git mv`; after migration old actionable path references are resolved and tests pass | 14-01 | COVERED | Task 1 runs dry-run and real `git mv`, updates active references; Task 3 runs strict grep and smoke tests. |
| REQ | UPST-01 | ADR compares installer/probe pin, git submodule, manifest pin, and vendor snapshot | 14-01 | COVERED | Task 2 creates ADR with exact option labels and accepted manifest pin decision. |
| REQ | UPST-02 | If submodule is selected, verify only `.gitmodules` and gitlink staging | 14-01 | COVERED | Manifest pin is selected; Task 2 marks UPST-02 not applicable and Task 3 asserts no `.gitmodules` or gitlink. |
| RESEARCH | - | Use actionable zero gate plus audit-only residual review | 14-01 | COVERED | Tasks 1 and 3 separate active files from historical planning artifacts. |
| RESEARCH | - | Use `.planning/upstream/hermes-agent-pin.json` as repo-local machine-readable pin | 14-01 | COVERED | Task 2 creates JSON with required contract and validation. |
| RESEARCH | - | Run shell syntax checks and migrated smoke runner | 14-01 | COVERED | Task 3 runs `bash -n` over migrated scripts and `bash docs/orchestra/scripts/tests/run-all.sh`. |
| RESEARCH | - | Avoid accidental submodule artifacts | 14-01 | COVERED | Tasks 2 and 3 reject `.gitmodules` and mode 160000 gitlinks. |
| CONTEXT | D-14-01 | Execute directory migration to `docs/orchestra/` | 14-01 | COVERED | Task 1. |
| CONTEXT | D-14-02 | Use `git mv docs/hermes-dev-orchestra docs/orchestra` | 14-01 | COVERED | Task 1. |
| CONTEXT | D-14-03 | No compatibility shim, duplicate directory, symlink, or supported pointer | 14-01 | COVERED | Task 1 acceptance and Task 3 checks. |
| CONTEXT | D-14-04 | Update active references; historical artifacts only if audit-only | 14-01 | COVERED | Tasks 1 and 3. |
| CONTEXT | D-14-05 | ADR compares four strategies | 14-01 | COVERED | Task 2. |
| CONTEXT | D-14-06 | Select manifest pin | 14-01 | COVERED | Task 2. |
| CONTEXT | D-14-07 | Manifest is repo-local, machine-readable, and records pin evidence/update procedure | 14-01 | COVERED | Task 2. |
| CONTEXT | D-14-08 | Explain git submodule is not selected for adapter-layer repo | 14-01 | COVERED | Task 2. |
| CONTEXT | D-14-09 | ADR path `.planning/adr/ADR-001-upstream-pin.md` | 14-01 | COVERED | Task 2. |
| CONTEXT | D-14-10 | UPST-02 is not applicable when submodule is not selected | 14-01 | COVERED | Task 2 and Task 3. |
| CONTEXT | D-14-11 | Verify path search, smoke tests, shell syntax, and git status | 14-01 | COVERED | Task 3. |
| CONTEXT | Deferred | specs system, Makefile/upstream-status target, broad AGENTS consolidation, actual submodule adoption | 14-01 | EXCLUDED | Explicitly out of scope; no task implements these. |
</source_audit>

<dependency_graph>
Task 1 creates the moved tree and removes active old-path references. Task 2 depends on Task 1 only for final path names that the ADR references, and creates the manifest/ADR. Task 3 depends on Tasks 1-2 and verifies the whole phase.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Move package directory and update active path references</name>
  <files>docs/hermes-dev-orchestra/**, docs/orchestra/**, README.md, AGENTS.md, docs/COVERAGE-MATRIX.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/DIRECTION-CORRECTION.md, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md, docs/orchestra/scripts/tests/*.sh, docs/orchestra/scripts/tests/lib/assert.sh, docs/orchestra/scripts/setup.sh, docs/orchestra/scripts/bin/orch-*, docs/orchestra/scripts/lib/orch-common.sh</files>
  <read_first>
    - `AGENTS.md`
    - `CLAUDE.md`
    - `.planning/STATE.md`
    - `.planning/ROADMAP.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/PROJECT.md`
    - `.planning/DIRECTION-CORRECTION.md`
    - `.planning/phases/14-migration-submodule-adr/14-CONTEXT.md`
    - `.planning/phases/14-migration-submodule-adr/14-VALIDATION.md`
    - `.planning/phases/14-migration-submodule-adr/14-PATTERNS.md`
    - `.planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md`
    - `README.md`
    - `docs/COVERAGE-MATRIX.md`
    - `docs/hermes-dev-orchestra/README.md`
    - `docs/hermes-dev-orchestra/WORKFLOW.md`
    - `docs/hermes-dev-orchestra/scripts/tests/run-all.sh`
    - Every `docs/hermes-dev-orchestra/scripts/tests/test-*.sh` file before editing migrated copies
  </read_first>
  <action>
    Run `git status --short --branch` first. Preserve the pre-existing unrelated staged/deleted backlog paths under `.planning/phases/999.1-*` and `.planning/phases/999.2-*`; do not stage, reset, delete, or include them in Phase 14 work.

    Execute the required dry run and physical move exactly:

    ```bash
    git mv -n docs/hermes-dev-orchestra docs/orchestra
    git mv docs/hermes-dev-orchestra docs/orchestra
    ```

    After the move, replace active literal references from `docs/hermes-dev-orchestra` to `docs/orchestra` in these files only: `README.md`, `AGENTS.md`, `docs/COVERAGE-MATRIX.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/DIRECTION-CORRECTION.md`, `docs/orchestra/README.md`, `docs/orchestra/WORKFLOW.md`, and migrated smoke tests under `docs/orchestra/scripts/tests/`. In smoke tests, keep the existing `REPO_ROOT="$(cd "$TEST_DIR/../../../.." && pwd)"` depth and change only the old docs path literal. Keep runtime names unchanged: `hermes-orchestra`, `~/.hermes-orchestra`, `/tmp/hermes-orchestra`, `ORCHESTRA_HOME`, `RUNTIME_ROOT`, project IDs, and tmux session naming are not part of this migration.

    Verify `CLAUDE.md` has no `docs/hermes-dev-orchestra` match. Do not edit `CLAUDE.md` if the verification stays empty. Do not edit Phase 13 evidence, v1.0/v1.1 archives, discussion logs, task transcript files, or backlog artifacts to satisfy the active gate; those are historical/audit-only and must be handled in Task 3's residual review.
  </action>
  <verify>
    <automated><![CDATA[
# Required one-time commands run in <action>; do not rerun after the source path has moved:
# git status --short --branch
# git mv -n docs/hermes-dev-orchestra docs/orchestra
# git mv docs/hermes-dev-orchestra docs/orchestra
! rg -n "docs/hermes-dev-orchestra" README.md AGENTS.md CLAUDE.md docs .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/DIRECTION-CORRECTION.md
while IFS= read -r f; do bash -n "$f"; done < <(find docs/orchestra/scripts -type f \( -name "*.sh" -o -path "*/scripts/bin/orch-*" \) -print | sort)
]]></automated>
  </verify>
  <acceptance_criteria>
    - `docs/orchestra/README.md` exists and `docs/hermes-dev-orchestra/README.md` does not exist as a tracked working-tree path.
    - `git status --short -- docs/hermes-dev-orchestra docs/orchestra` shows a rename/move from the old tree to the new tree, not a duplicate copy.
    - The strict active gate command returns no matches.
    - `rg -n "docs/orchestra" README.md AGENTS.md docs/COVERAGE-MATRIX.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/DIRECTION-CORRECTION.md docs/orchestra/README.md docs/orchestra/WORKFLOW.md docs/orchestra/scripts/tests` returns at least one match.
    - `rg -n "docs/hermes-dev-orchestra" CLAUDE.md` returns no matches and `CLAUDE.md` is not modified.
    - Shell syntax check over `docs/orchestra/scripts` exits 0.
  </acceptance_criteria>
  <done>The package has been moved with `git mv`, active references point to `docs/orchestra`, `CLAUDE.md` remains untouched, and migrated scripts pass syntax checks.</done>
</task>

<task type="auto">
  <name>Task 2: Create upstream pin manifest and ADR</name>
  <files>.planning/upstream/hermes-agent-pin.json, .planning/adr/ADR-001-upstream-pin.md</files>
  <read_first>
    - `.planning/phases/14-migration-submodule-adr/14-CONTEXT.md`
    - `.planning/phases/14-migration-submodule-adr/14-RESEARCH.md`
    - `.planning/phases/14-migration-submodule-adr/14-PATTERNS.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/ROADMAP.md`
    - `.planning/DIRECTION-CORRECTION.md`
    - `.planning/phases/09-upstream-hermes-agent-baseline/09-01-SUMMARY.md`
    - `.planning/adr/ADR-001-upstream-pin.md` if it already exists
    - `.planning/upstream/hermes-agent-pin.json` if it already exists
  </read_first>
  <action>
    Create `.planning/upstream/` and `.planning/adr/` if missing. Write `.planning/upstream/hermes-agent-pin.json` as valid JSON with two-space indentation and a final newline. Use the following contract and exact values unless execution finds a deliberate upstream update; if that happens, document the deviation in the ADR and summary before changing any value:

    ```json
    {
      "schema_version": "1.0",
      "component": "hermes-agent",
      "upstream": {
        "repository": "https://github.com/NousResearch/hermes-agent",
        "remote": "https://github.com/NousResearch/hermes-agent.git"
      },
      "pin": {
        "commit": "023b1bff11c2a01a435f1956a0e2ac1773a065f3",
        "observed_version": "Hermes Agent v0.11.0 (2026.4.23)",
        "probe_date": "2026-04-28",
        "install_source": "https://github.com/NousResearch/hermes-agent",
        "install_method": "upstream installer with HTTPS-safe Git config when needed",
        "local_install_path": "~/.hermes/hermes-agent"
      },
      "probe_commands": [
        "git -C ~/.hermes/hermes-agent rev-parse HEAD",
        "hermes --version",
        "hermes --help"
      ],
      "update_procedure": [
        "Choose an intentional upstream commit.",
        "Run the upstream installer or update procedure.",
        "Verify hermes --version and hermes --help.",
        "Update this manifest and related documentation in the same change."
      ],
      "phase_16_contract": {
        "repo_pin_json_pointer": "/pin/commit",
        "observed_version_json_pointer": "/pin/observed_version",
        "runtime_pin_probe": "git -C ${HERMES_AGENT_DIR:-$HOME/.hermes/hermes-agent} rev-parse HEAD"
      }
    }
    ```

    Write `.planning/adr/ADR-001-upstream-pin.md` with this decision shape: title `ADR-001: Upstream Hermes Agent Pin Strategy`, date `2026-04-28`, status `Accepted`, decision `Use a repo-local JSON manifest pin for v1.2`, sections `Context`, `Decision`, `Options Considered`, `Consequences`, `UPST-02 Applicability`, and `Verification`. The `Options Considered` section must include the exact option labels `installer/probe pin`, `git submodule`, `manifest pin`, and `vendor snapshot`. Accept `manifest pin`; reject `git submodule` because this repository is an adapter layer and does not need to vendor or checkout upstream core source for normal development. State explicitly: `UPST-02 is not applicable because manifest pin is selected and git submodule is not selected.` Also state this phase must not introduce `.gitmodules` or any gitlink mode `160000`.
  </action>
  <verify>
    <automated><![CDATA[
python3 -m json.tool .planning/upstream/hermes-agent-pin.json >/dev/null
rg -q --fixed-strings "installer/probe pin" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "git submodule" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "manifest pin" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "vendor snapshot" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "UPST-02" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "not applicable" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "manifest pin" .planning/adr/ADR-001-upstream-pin.md
test ! -f .gitmodules
! git ls-files --stage | grep -q '^160000 '
]]></automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m json.tool .planning/upstream/hermes-agent-pin.json >/dev/null` exits 0.
    - `rg -q --fixed-strings '"schema_version": "1.0"' .planning/upstream/hermes-agent-pin.json`, `rg -q --fixed-strings '"component": "hermes-agent"' .planning/upstream/hermes-agent-pin.json`, and `rg -q --fixed-strings '"commit": "023b1bff11c2a01a435f1956a0e2ac1773a065f3"' .planning/upstream/hermes-agent-pin.json` all exit 0.
    - `rg -q --fixed-strings "git -C ~/.hermes/hermes-agent rev-parse HEAD" .planning/upstream/hermes-agent-pin.json`, `rg -q --fixed-strings "hermes --version" .planning/upstream/hermes-agent-pin.json`, and `rg -q --fixed-strings "hermes --help" .planning/upstream/hermes-agent-pin.json` all exit 0.
    - `rg -q --fixed-strings "intentional upstream commit" .planning/upstream/hermes-agent-pin.json`, `rg -q --fixed-strings "upstream installer" .planning/upstream/hermes-agent-pin.json`, `rg -q --fixed-strings "hermes --version" .planning/upstream/hermes-agent-pin.json`, `rg -q --fixed-strings "hermes --help" .planning/upstream/hermes-agent-pin.json`, and `rg -q --fixed-strings "manifest and related documentation" .planning/upstream/hermes-agent-pin.json` all exit 0.
    - `rg -q --fixed-strings "installer/probe pin" .planning/adr/ADR-001-upstream-pin.md`, `rg -q --fixed-strings "git submodule" .planning/adr/ADR-001-upstream-pin.md`, `rg -q --fixed-strings "manifest pin" .planning/adr/ADR-001-upstream-pin.md`, and `rg -q --fixed-strings "vendor snapshot" .planning/adr/ADR-001-upstream-pin.md` all exit 0.
    - `rg -q --fixed-strings "UPST-02" .planning/adr/ADR-001-upstream-pin.md`, `rg -q --fixed-strings "not applicable" .planning/adr/ADR-001-upstream-pin.md`, and `rg -q --fixed-strings "manifest pin" .planning/adr/ADR-001-upstream-pin.md` all exit 0.
    - `test ! -f .gitmodules` passes and `! git ls-files --stage | grep -q '^160000 '` passes.
  </acceptance_criteria>
  <done>The manifest is valid JSON with the required upstream pin contract, the ADR accepts manifest pin after comparing all four options, and no submodule artifact exists.</done>
</task>

<task type="auto">
  <name>Task 3: Run final verification and write execution summary</name>
  <files>.planning/phases/14-migration-submodule-adr/14-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/14-migration-submodule-adr/14-VALIDATION.md`
    - `.planning/phases/14-migration-submodule-adr/14-PATTERNS.md`
    - `.planning/phases/13-evidence-audit-and-discoverability/13-EVIDENCE.md`
    - `.planning/phases/09-upstream-hermes-agent-baseline/09-01-SUMMARY.md`
    - `.planning/phases/14-migration-submodule-adr/14-01-PLAN.md`
    - `.planning/phases/14-migration-submodule-adr/14-01-SUMMARY.md` if it already exists
  </read_first>
  <action>
    Run the full Phase 14 verification matrix in the order below. If the strict active old-path gate finds any match, fix the active reference before continuing. Run the broad hidden old-path review as audit-only; do not rewrite Phase 13 evidence, Phase 14 planning/research/validation/pattern artifacts, old milestone archives, task transcripts, or backlog files solely because they preserve historical old-path evidence.

    Create `.planning/phases/14-migration-submodule-adr/14-01-SUMMARY.md` using the execute-plan summary template. The summary must record command results, the active-vs-historical old-path distinction, the manifest pin values, ADR decision, UPST-02 not-applicable statement, no-submodule verification, and the final `git status --short --branch` output. In the summary, explicitly list pre-existing unrelated backlog changes and state they were preserved and excluded from Phase 14 staging/commit work.
  </action>
  <verify>
    <automated><![CDATA[
git status --short --branch
! rg -n "docs/hermes-dev-orchestra" README.md AGENTS.md CLAUDE.md docs .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/DIRECTION-CORRECTION.md
while IFS= read -r f; do bash -n "$f"; done < <(find docs/orchestra/scripts -type f \( -name "*.sh" -o -path "*/scripts/bin/orch-*" \) -print | sort)
python3 -m json.tool .planning/upstream/hermes-agent-pin.json >/dev/null
bash docs/orchestra/scripts/tests/run-all.sh
rg -q --fixed-strings "installer/probe pin" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "git submodule" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "manifest pin" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "vendor snapshot" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "UPST-02" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "not applicable" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "manifest pin" .planning/adr/ADR-001-upstream-pin.md
test ! -f .gitmodules
! git ls-files --stage | grep -q '^160000 '
rg --hidden -n "docs/hermes-dev-orchestra" --glob '!/.git/*' --glob '!*.zip' || true
test -f .planning/phases/14-migration-submodule-adr/14-01-SUMMARY.md
]]></automated>
  </verify>
  <acceptance_criteria>
    - Strict active old-path gate exits 0 with no matches.
    - Broad hidden old-path review output is recorded in the summary as audit-only residuals, or the summary states it returned no matches.
    - Shell syntax check exits 0.
    - `bash docs/orchestra/scripts/tests/run-all.sh` exits 0.
    - Manifest JSON validation exits 0 and the manifest contains the required Phase 9 commit.
    - ADR grep checks for four options and UPST-02 applicability pass.
    - `.gitmodules` does not exist and no tracked index entry has mode `160000`.
    - Final summary exists and includes `MIGR-02`, `UPST-01`, `UPST-02`, `docs/orchestra`, `.planning/upstream/hermes-agent-pin.json`, and `.planning/adr/ADR-001-upstream-pin.md`.
    - Final `git status --short --branch` is recorded and unrelated 999.x backlog changes are still distinguishable from Phase 14 changes.
  </acceptance_criteria>
  <done>All verification commands have been run, audit-only residuals are documented, no-submodule checks pass, and the Phase 14 summary exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical planning evidence -> active migration gate | Old path strings in audit artifacts must not be treated as current broken links. |
| Migrated shell tests -> repo-local helper paths | Shell tests execute helper paths and can silently miss the moved tree if literals stay stale. |
| ADR prose -> machine-readable manifest -> Phase 16 workflow | Future tooling will parse the manifest, not prose, so drift breaks downstream automation. |
| Git index -> upstream dependency representation | Submodule artifacts in the index would change supply-chain semantics despite manifest pin selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering / Repudiation | Active old-path gate and historical audit artifacts | mitigate | Use the strict active gate only on `README.md AGENTS.md CLAUDE.md docs .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/DIRECTION-CORRECTION.md`; run broad hidden `rg` only as audit-only residual review and record residuals in the summary. |
| T-14-02 | Tampering / Denial of Service | `docs/orchestra/scripts/tests/*.sh` and helper invocations | mitigate | Replace test path literals with `docs/orchestra`, keep relative `REPO_ROOT` depth unchanged, run `bash -n` over migrated scripts, then run `bash docs/orchestra/scripts/tests/run-all.sh`. |
| T-14-03 | Tampering / Repudiation | `.planning/upstream/hermes-agent-pin.json` and ADR | mitigate | Create a parseable JSON manifest with exact Phase 9 commit, observed version, probe date, install source/method, probe commands, and update procedure; validate with `python3 -m json.tool`. |
| T-14-04 | Tampering / Supply Chain Confusion | `.gitmodules`, git index, `hermes-agent` checkout | mitigate | Do not create a submodule; ADR states UPST-02 is not applicable; verify `test ! -f .gitmodules` and `! git ls-files --stage \| grep -q '^160000 '`. |
</threat_model>

<verification>
Run these commands during execution and record the results in `14-01-SUMMARY.md`:

```bash
git status --short --branch
git mv -n docs/hermes-dev-orchestra docs/orchestra
git mv docs/hermes-dev-orchestra docs/orchestra
! rg -n "docs/hermes-dev-orchestra" README.md AGENTS.md CLAUDE.md docs .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/DIRECTION-CORRECTION.md
while IFS= read -r f; do bash -n "$f"; done < <(find docs/orchestra/scripts -type f \( -name "*.sh" -o -path "*/scripts/bin/orch-*" \) -print | sort)
python3 -m json.tool .planning/upstream/hermes-agent-pin.json >/dev/null
bash docs/orchestra/scripts/tests/run-all.sh
rg -q --fixed-strings "installer/probe pin" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "git submodule" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "manifest pin" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "vendor snapshot" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "UPST-02" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "not applicable" .planning/adr/ADR-001-upstream-pin.md
rg -q --fixed-strings "manifest pin" .planning/adr/ADR-001-upstream-pin.md
test ! -f .gitmodules
! git ls-files --stage | grep -q '^160000 '
rg --hidden -n "docs/hermes-dev-orchestra" --glob '!/.git/*' --glob '!*.zip' || true
git status --short --branch
```

The broad hidden `rg` is manual/audit-only residual review, not the strict active gate. It may return historical planning evidence, Phase 14 planning artifacts, archives, task transcripts, or backlog files; classify those in the summary instead of rewriting history.
</verification>

<success_criteria>
- `14-01-SUMMARY.md` proves `git mv` was used and the new tracked package path is `docs/orchestra/`.
- Strict active old-path grep returns no matches.
- Migrated shell syntax check and `bash docs/orchestra/scripts/tests/run-all.sh` pass.
- `.planning/upstream/hermes-agent-pin.json` is valid JSON with the required commit `023b1bff11c2a01a435f1956a0e2ac1773a065f3`.
- `.planning/adr/ADR-001-upstream-pin.md` compares all four required strategies and accepts manifest pin.
- ADR states UPST-02 is not applicable because git submodule is not selected.
- `test ! -f .gitmodules` and `! git ls-files --stage | grep -q '^160000 '` pass.
- Final status review keeps unrelated backlog changes distinct from Phase 14 changes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-migration-submodule-adr/14-01-SUMMARY.md`.
</output>
