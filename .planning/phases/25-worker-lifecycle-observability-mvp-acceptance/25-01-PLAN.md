---
phase: 25-worker-lifecycle-observability-mvp-acceptance
plan: "01"
type: execute
wave: 1
depends_on:
  - 24-01
files_modified:
  - docs/orchestra/scripts/lib/orch-common.sh
  - docs/orchestra/scripts/bin/orch-bus-loop
  - docs/orchestra/scripts/bin/orch-start
  - docs/orchestra/scripts/bin/orch-status
  - docs/orchestra/scripts/setup.sh
  - docs/orchestra/hermes/plugins/observability/__init__.py
  - docs/orchestra/hermes/role-engine-protocol/v1/roles/implementer.md
  - docs/orchestra/scripts/tests/test-worker-lifecycle-timeout.sh
  - docs/orchestra/scripts/tests/test-structured-handoff.sh
  - docs/orchestra/scripts/tests/test-observability-plugin.sh
  - docs/orchestra/scripts/tests/test-env-snapshot.sh
  - docs/orchestra/scripts/tests/test-backpressure-basic.sh
  - docs/orchestra/scripts/tests/test-mvp-acceptance.sh
  - docs/orchestra/README.md
  - docs/orchestra/WORKFLOW.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/25-worker-lifecycle-observability-mvp-acceptance/25-VERIFICATION.md
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - FLOW-01
  - OBS-01
  - OBS-02
must_haves:
  truths:
    - "Phase 25 只收 v1.3 MVP 主链路：timeout/reclaim、isolated cleanup、structured handoff、basic backpressure、hook traces、environment snapshot 和一条端到端验收。"
    - "`expected_duration_max` 继续作为任务级 timeout 契约；任务里显式声明的值优先于任何 profile 默认值。"
    - "timeout/crash/cancel 的回收只允许清理 worker 拥有的隔离工作区或任务运行时产物，不能改写主仓库 checkout。"
    - "`on_session_end` 在硬崩溃路径不可靠，因此 cleanup 和 session summary 不能只靠 hook，必须有 dispatcher-side fallback。"
    - "handoff metadata 的最小结构固定包含 `behaviors`、`regression`、`changed_files`、`decisions`、`pitfalls`，下游必须按 untrusted input 消费。"
    - "basic backpressure 只做 ready 队列比率暂停，不在本 phase 实现死锁升级或自动 SRE 分析任务。"
    - "observability 必须基于 Hermes 官方 `post_tool_call` / `on_session_end` hooks 和 sidecar queryable store；不得修改 Hermes 核心。"
    - "MVP 验收必须证明至少一条 requirement -> implementation -> review/test 主链路在当前 orchestration entrypoints 上跑通。"
  artifacts:
    - path: "docs/orchestra/scripts/lib/orch-common.sh"
      provides: "共享 lifecycle helpers：timeout 解析、run manifest、cleanup baseline、backpressure predicates"
      contains: "expected_duration_max"
    - path: "docs/orchestra/scripts/bin/orch-bus-loop"
      provides: "runtime reclaim、handoff validation、env snapshot capture、backpressure pause 和 acceptance-chain 执行入口"
      contains: "review_handoff"
    - path: "docs/orchestra/hermes/plugins/observability/__init__.py"
      provides: "基于官方 hooks 的 sidecar trace 记录器"
      contains: "register_hook"
    - path: "docs/orchestra/scripts/tests/test-worker-lifecycle-timeout.sh"
      provides: "timeout -> cleanup -> ready/requeue 的可执行回归证明"
      contains: "expected_duration_max"
    - path: "docs/orchestra/scripts/tests/test-structured-handoff.sh"
      provides: "handoff schema、payload safety、untrusted-consumption 边界的回归证明"
      contains: "changed_files"
    - path: "docs/orchestra/scripts/tests/test-mvp-acceptance.sh"
      provides: "一条 requirement -> implementation -> reviewer/qa 主链路验收"
      contains: "qa_handoff"
    - path: ".planning/phases/25-worker-lifecycle-observability-mvp-acceptance/25-VERIFICATION.md"
      provides: "Phase 25 验证证据、残留边界与 v1.4 handoff"
      contains: "Ready for v1.3 closeout"
  key_links:
    - from: ".planning/phases/19-hermes-workflow-design/REQUIREMENTS.md"
      to: "docs/orchestra/scripts/lib/orch-common.sh"
      via: "`R4/R12/R15` 的 timeout/reclaim 契约变成 repo-local shared helper surface，而不是留在设计文档里"
      pattern: "expected_duration_max"
    - from: ".planning/phases/23-stateful-routing-kanban-handoff/23-CONTEXT.md"
      to: "docs/orchestra/scripts/tests/test-structured-handoff.sh"
      via: "Phase 23 的 handoff artifact 语义被 Phase 25 收紧为结构化 schema + untrusted consumption"
      pattern: "handoff_ref"
    - from: ".planning/phases/19-hermes-workflow-design/ascii-observability.md"
      to: "docs/orchestra/hermes/plugins/observability/__init__.py"
      via: "hook-based observability 从叙事变成 repo-shipped plugin asset"
      pattern: "post_tool_call"
    - from: ".planning/phases/24-risk-policy-role-guardrails/24-VERIFICATION.md"
      to: "docs/orchestra/scripts/tests/test-mvp-acceptance.sh"
      via: "Phase 24 风险/角色边界继续作为主链路验收前提，不被 lifecycle logic 绕过"
      pattern: "role guardrails"
---

<objective>
Execute Phase 25 as the lifecycle, observability, and MVP-acceptance closeout for the Hermes-native workflow MVP.

Purpose: satisfy `EXEC-01`, `EXEC-02`, `EXEC-03`, `FLOW-01`, `OBS-01`, and `OBS-02` by adding timeout/reclaim control, isolated cleanup, structured handoff validation, basic ready-queue backpressure, hook-based tracing, environment snapshots, and one end-to-end MVP acceptance path on top of the Phase 22-24 runtime.

Output: lifecycle helpers, runtime reclaim behavior, structured handoff enforcement, observability plugin assets, snapshot capture, acceptance/regression tests, mirrored operator docs, and Phase 25 verification evidence ready for v1.3 milestone closeout.
</objective>

<execution_context>
Use the GSD execute-plan workflow. Keep Phase 25 narrow: land only the lifecycle/observability/handoff/acceptance slice required to close v1.3. Do not implement curator promotion, deadlock escalation, automatic SRE task creation, deployment workflows, or production release approvals in this phase.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/25-worker-lifecycle-observability-mvp-acceptance/25-CONTEXT.md
@.planning/phases/25-worker-lifecycle-observability-mvp-acceptance/25-DISCUSSION-LOG.md
@.planning/phases/24-risk-policy-role-guardrails/24-CONTEXT.md
@.planning/phases/24-risk-policy-role-guardrails/24-VERIFICATION.md
@.planning/phases/23-stateful-routing-kanban-handoff/23-CONTEXT.md
@.planning/phases/23-stateful-routing-kanban-handoff/23-VERIFICATION.md
@.planning/phases/22-external-cli-engine-protocol-role-invocation/22-CONTEXT.md
@.planning/phases/19-hermes-workflow-design/REQUIREMENTS.md
@.planning/phases/19-hermes-workflow-design/DESIGN.md
@.planning/phases/19-hermes-workflow-design/EXTERNAL-CLI-ENGINE.md
@.planning/phases/19-hermes-workflow-design/ascii-observability.md
@.planning/phases/19-hermes-workflow-design/workflow-phase-03-implementation.md
@.planning/phases/19-hermes-workflow-design/workflow-appendix-failure-modes.md
@docs/orchestra/scripts/lib/orch-common.sh
@docs/orchestra/scripts/bin/orch-bus-loop
@docs/orchestra/scripts/bin/orch-start
@docs/orchestra/scripts/bin/orch-status
@docs/orchestra/scripts/setup.sh
@docs/orchestra/hermes/role-engine-protocol/v1/common-envelope.md
@docs/orchestra/hermes/role-engine-protocol/v1/roles/implementer.md
@docs/orchestra/scripts/tests/test-kanban-routing.sh
@docs/orchestra/scripts/tests/test-kanban-handoff.sh
@docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh
@docs/orchestra/README.md
@docs/orchestra/WORKFLOW.md

<interfaces>
Required timeout and reclaim contract:

```text
task metadata field: expected_duration_max
task value overrides profile default
timeout/crash/cancel -> cleanup isolated workspace -> requeue or resume safely
```

Required structured handoff fields:

```text
behaviors
regression
changed_files
decisions
pitfalls
```

Required observability sources:

```text
post_tool_call hook
on_session_end hook
spawn-time environment snapshot
task_id / project_id / handoff_ref correlation anchors
```

Required environment snapshot minimum:

```text
git status
df -h (first 5 lines)
hermes status
```

Required backpressure scope:

```text
implementer -> reviewer
reviewer -> qa-tester
ratio-based pause only
```

Required MVP acceptance chain:

```text
pm requirement handoff
implementer execution
reviewer completion
qa insertion when predicate matches
```
</interfaces>
</context>

<source_audit>
| Source | ID | Required Item | Plan/Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Close v1.3 MVP with lifecycle control, observability, and acceptance evidence. | 25-01 T1,T2,T3,T4 | COVERED | Tasks map directly to the remaining roadmap success criteria. |
| REQ | EXEC-01 | Every worker task declares `expected_duration_max`; dispatcher can reclaim on timeout. | 25-01 T1 | COVERED | Shared manifest/default logic plus timeout tests are explicit. |
| REQ | EXEC-02 | crash/timeout/cancel restore isolated workspace to task-start clean state. | 25-01 T1 | COVERED | Cleanup baseline and reclaim flow are the core of Task 1. |
| REQ | EXEC-03 | `kanban_complete` outputs structured handoff metadata and downstream treats it as untrusted input. | 25-01 T2 | COVERED | Task 2 locks schema, validation, and downstream consumption boundary. |
| REQ | FLOW-01 | Dispatcher applies basic ready-queue backpressure. | 25-01 T4 | COVERED | Task 4 adds the conservative ratio gate only for MVP flow edges. |
| REQ | OBS-01 | Observability plugin uses official hooks without Hermes core changes. | 25-01 T3 | COVERED | Plugin asset, sidecar store, and tests are explicit. |
| REQ | OBS-02 | Spawn-time environment snapshot is attached for later debugging. | 25-01 T3 | COVERED | Task 3 captures and persists the minimum snapshot set. |
| CONTEXT | D-25-03..08 | Task-level timeout, isolated cleanup, dispatcher fallback. | 25-01 T1 | COVERED | Task 1 directly implements the locked lifecycle boundary. |
| CONTEXT | D-25-09..11 | Summary-first handoff and untrusted downstream consumption. | 25-01 T2 | COVERED | Task 2 constrains payload shape and reader behavior. |
| CONTEXT | D-25-12..17 | Basic backpressure, hook traces, snapshots, sidecar observability. | 25-01 T3,T4 | COVERED | Tasks 3 and 4 narrow the MVP implementation to those surfaces only. |
| CONTEXT | D-25-18..19 | One strong end-to-end MVP acceptance path. | 25-01 T4 | COVERED | Acceptance test becomes the main closeout proof. |
| DESIGN | R4/R5/R12/R13/R15/R16/R19/R22 | Lifecycle, backpressure, handoff, trace, and snapshot design targets. | 25-01 T1,T2,T3,T4 | COVERED | Phase 19 design items are translated into executable repo assets. |
| CODE | orch-bus-loop / orch-common / orch-start / orch-status | Current runtime already owns dispatch, state, and status. | 25-01 T1,T3,T4 | COVERED | Plan evolves current entrypoints instead of adding a new runtime. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared worker-lifecycle manifest, timeout parsing, and reclaim cleanup path</name>
  <files>docs/orchestra/scripts/lib/orch-common.sh, docs/orchestra/scripts/bin/orch-bus-loop, docs/orchestra/scripts/bin/orch-start, docs/orchestra/scripts/bin/orch-status, docs/orchestra/scripts/tests/test-worker-lifecycle-timeout.sh</files>
  <read_first>
    Read 25-CONTEXT.md decisions D-25-03 through D-25-08, Phase 19 `R4`, `R12`, and `R15`, then inspect the current `orch-bus-loop`, `orch-start`, `orch-status`, and any existing task/runtime state helpers in `orch-common.sh`.
  </read_first>
  <action>
    Add one shared lifecycle surface for active worker runs and use it to drive timeout detection, cleanup, and safe requeue/resume behavior.

    Required implementation boundaries:

```text
- Define one normalized active-run manifest that captures task_id, role, started_at, expected_duration_max, workspace/cleanup baseline, and the best available runner identity
- Parse `expected_duration_max` from the task envelope first; fall back to conservative per-role defaults only when the task did not declare a value
- Detect timeout, cancel, and stale/crashed execution from the orchestration loop without pretending `on_session_end` is always available
- Reclaim only the worker-owned isolated workspace/runtime artifacts; never mutate the main repo checkout
- Leave auditable lifecycle breadcrumbs in state/audit outputs so operators can see why reclaim happened
```

    If the current session model does not expose a precise per-task child PID, record the best available launcher/session identity and use a conservative reclaim heuristic rather than faking precision.
  </action>
  <verify>
    <automated>
bash docs/orchestra/scripts/tests/test-worker-lifecycle-timeout.sh
    </automated>
    <automated>
bash docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh
    </automated>
  </verify>
  <acceptance_criteria>
    The runtime has one obvious lifecycle manifest surface, timeout is driven by `expected_duration_max`, reclaim stays inside the worker isolation boundary, and operators can see timeout/cleanup status through the existing status surface.
  </acceptance_criteria>
  <done>EXEC-01 and EXEC-02 have an executable timeout/reclaim baseline instead of remaining design-only requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Harden structured handoff metadata and downstream untrusted-input consumption</name>
  <files>docs/orchestra/scripts/lib/orch-common.sh, docs/orchestra/scripts/bin/orch-bus-loop, docs/orchestra/hermes/role-engine-protocol/v1/roles/implementer.md, docs/orchestra/scripts/tests/test-structured-handoff.sh, docs/orchestra/scripts/tests/test-kanban-handoff.sh, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md</files>
  <read_first>
    Read 25-CONTEXT.md decisions D-25-09 through D-25-11, Phase 19 `R13` and `R16`, the current implementer role contract, and the existing handoff creation path in `orch-bus-loop`.
  </read_first>
  <action>
    Turn the current summary-first handoff convention into an explicit structured schema with safety validation and downstream-consumption rules.

    Required behavior:

```text
- Validate `task_complete` handoff payloads against the minimum fields: behaviors, regression, changed_files, decisions, pitfalls
- Reject or sanitize obviously unsafe free-text payload patterns before child-task creation consumes them
- Keep handoff artifacts summary-first; do not turn metadata into a second instruction channel
- Ensure downstream task creation marks inherited handoff content as untrusted/reference-only rather than authoritative instructions
- Mirror the runtime rule in docs and role-contract wording without reopening the Phase 23 routing model
```
  </action>
  <verify>
    <automated>
bash docs/orchestra/scripts/tests/test-structured-handoff.sh
    </automated>
    <automated>
bash docs/orchestra/scripts/tests/test-kanban-handoff.sh
    </automated>
  </verify>
  <acceptance_criteria>
    Handoff payloads have one documented minimum schema, unsafe free-text is not silently propagated, and downstream workers consume upstream metadata as reference-only untrusted input.
  </acceptance_criteria>
  <done>EXEC-03 is enforced in code and tests rather than being a narrative guideline.</done>
</task>

<task type="auto">
  <name>Task 3: Ship hook-based observability and spawn-time environment snapshots as sidecar assets</name>
  <files>docs/orchestra/hermes/plugins/observability/__init__.py, docs/orchestra/scripts/bin/orch-bus-loop, docs/orchestra/scripts/bin/orch-start, docs/orchestra/scripts/setup.sh, docs/orchestra/scripts/tests/test-observability-plugin.sh, docs/orchestra/scripts/tests/test-env-snapshot.sh, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md</files>
  <read_first>
    Read 25-CONTEXT.md decisions D-25-14 through D-25-17, Phase 19 `R19` and `R22`, `ascii-observability.md`, and the current package installer layout in `setup.sh`.
  </read_first>
  <action>
    Add repo-shipped observability assets that rely only on verified Hermes hook surfaces and orchestration-side snapshot capture.

    Required implementation boundaries:

```text
- Add one repo-local observability plugin asset that registers `post_tool_call` and `on_session_end`
- Persist traces to a sidecar queryable store owned by the orchestra layer, not Hermes core
- Capture spawn-time environment snapshots with at least git status, first 5 lines of df -h, and hermes status
- Correlate trace/snapshot records with task_id, project_id, and runtime artifacts already used by the orchestration layer
- When crash paths miss `on_session_end`, keep enough dispatcher-side metadata to diagnose the missing session summary
```
  </action>
  <verify>
    <automated>
bash docs/orchestra/scripts/tests/test-observability-plugin.sh
    </automated>
    <automated>
bash docs/orchestra/scripts/tests/test-env-snapshot.sh
    </automated>
    <automated>
python3 -m py_compile docs/orchestra/hermes/plugins/observability/__init__.py
    </automated>
  </verify>
  <acceptance_criteria>
    The repo ships a concrete observability plugin asset, snapshots are captured at spawn time, and operators have a queryable sidecar trace path without any Hermes core patching.
  </acceptance_criteria>
  <done>OBS-01 and OBS-02 have concrete runtime assets, storage, and verification coverage.</done>
</task>

<task type="auto">
  <name>Task 4: Add conservative ready-queue backpressure and prove one end-to-end MVP acceptance chain</name>
  <files>docs/orchestra/scripts/lib/orch-common.sh, docs/orchestra/scripts/bin/orch-bus-loop, docs/orchestra/scripts/bin/orch-status, docs/orchestra/scripts/tests/test-backpressure-basic.sh, docs/orchestra/scripts/tests/test-mvp-acceptance.sh, docs/orchestra/scripts/tests/test-kanban-routing.sh, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md, .planning/phases/25-worker-lifecycle-observability-mvp-acceptance/25-VERIFICATION.md</files>
  <read_first>
    Read 25-CONTEXT.md decisions D-25-12 through D-25-19, Phase 19 `R5`, `AE3`, and the existing routing/QA predicate behavior from Phase 23 tests.
  </read_first>
  <action>
    Close v1.3 with a minimal backpressure gate and one strong end-to-end acceptance proof.

    Required behavior:

```text
- Add a conservative ready-queue ratio gate for implementer->reviewer and reviewer->qa only
- Surface backpressure pause state in runtime logs/status instead of hiding it
- Reuse the existing PM -> Implementer -> Reviewer -> QA routing path rather than inventing a new acceptance harness
- Ensure the acceptance fixture also proves the new lifecycle side effects that matter here: handoff validation, snapshot presence, and trace storage
- Record v1.4 handoff boundaries explicitly in Phase 25 verification instead of leaking them into MVP scope
```
  </action>
  <verify>
    <automated>
bash docs/orchestra/scripts/tests/test-backpressure-basic.sh
    </automated>
    <automated>
bash docs/orchestra/scripts/tests/test-mvp-acceptance.sh
    </automated>
    <automated>
bash docs/orchestra/scripts/tests/test-kanban-routing.sh
    </automated>
  </verify>
  <acceptance_criteria>
    The runtime can visibly pause an overloaded upstream queue, and at least one requirement->implementation->review/test chain passes end-to-end on the current orchestration entrypoints with lifecycle/observability evidence attached.
  </acceptance_criteria>
  <done>FLOW-01 and the roadmap’s MVP acceptance criterion are both evidenced by executable tests and verification notes.</done>
</task>

</tasks>
