---
phase: 22-external-cli-engine-protocol-role-invocation
plan: "01"
type: execute
wave: 1
depends_on:
  - 21-01
files_modified:
  - docs/orchestra/hermes/profile-distribution/distribution.yaml
  - docs/orchestra/hermes/profile-distribution/profiles/pm/config.yaml
  - docs/orchestra/hermes/profile-distribution/profiles/implementer/config.yaml
  - docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml
  - .hermes/profiles/README.md
  - docs/orchestra/scripts/bin/orch-profile-sync
  - docs/orchestra/hermes/role-engine-protocol/v1/
  - docs/orchestra/scripts/tests/test-profile-packaging.sh
  - docs/orchestra/scripts/tests/test-project-isolation.sh
  - docs/orchestra/scripts/tests/test-role-engine-protocol.sh
  - docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh
  - docs/orchestra/README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/phases/22-external-cli-engine-protocol-role-invocation/22-VERIFICATION.md
autonomous: true
requirements:
  - ENG-01
  - ENG-02
must_haves:
  truths:
    - "Per-role checked-in `config.yaml` remains the only canonical source for default `engine` settings; project overrides may deep-merge `cli`, `mode`, `flags`, and `fallback` without polluting `~/.hermes/profiles/`."
    - "Phase 22 closes the first protocol loop only for `pm`, `implementer`, and `reviewer`; it does not expand scope into full seven-role routing or Orchestrator state-machine behavior."
    - "The repo ships one shared `hermes-role-engine/v1` envelope plus role-specific contracts and fixtures for `pm`, `implementer`, and `reviewer`."
    - "A small shared `next_action` enum is cross-role, while `status` remains role-specific and is documented as such."
    - "Canonical carried context is limited to `conversation_history`, `handoff_from_parent`, `task_summary` / `current_stage`, `last_engine_error`, and `rollback_count`; comments remain audit-only and history compaction uses summary plus recent raw turns."
    - "Default failure handling is retry once, then block; fallback is opt-in and auditable, while `parse_error` and `schema_mismatch` are hard-stop block cases with no auto-fallback."
  artifacts:
    - path: "docs/orchestra/hermes/profile-distribution/profiles/implementer/config.yaml"
      provides: "Checked-in default external CLI engine contract for the Implementer role"
      contains: "engine:"
    - path: "docs/orchestra/scripts/bin/orch-profile-sync"
      provides: "Project-scoped profile compiler that deep-merges `engine` fields into generated Hermes runtime output"
      contains: "fallback"
    - path: "docs/orchestra/hermes/role-engine-protocol/v1/common-envelope.md"
      provides: "Canonical shared request/response envelope for role-engine invocation"
      contains: "next_action"
    - path: "docs/orchestra/hermes/role-engine-protocol/v1/examples/"
      provides: "Golden JSON fixtures for `pm`, `implementer`, and `reviewer` request/response examples"
      contains: "hermes-role-engine/v1"
    - path: "docs/orchestra/scripts/tests/test-role-engine-protocol.sh"
      provides: "Fixture-level proof that shared envelope fields and role-specific contract rules stay in sync"
      contains: "next_action"
    - path: "docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh"
      provides: "Proof that timeout, crash, rate-limit, parse-error, and schema-mismatch normalize to the locked retry/block contract"
      contains: "parse_error"
    - path: ".planning/phases/22-external-cli-engine-protocol-role-invocation/22-VERIFICATION.md"
      provides: "Phase 22 execution evidence and residual blocker classification"
      contains: "Ready for $gsd-execute-phase closeout"
  key_links:
    - from: "docs/orchestra/hermes/profile-distribution/profiles/pm/config.yaml"
      to: "docs/orchestra/scripts/bin/orch-profile-sync"
      via: "checked-in engine defaults are compiled with repo-local overrides into project-scoped runtime output"
      pattern: "engine:"
    - from: "docs/orchestra/hermes/role-engine-protocol/v1/common-envelope.md"
      to: "docs/orchestra/hermes/role-engine-protocol/v1/roles/pm.md"
      via: "shared envelope semantics stay fixed while `status` remains role-specific"
      pattern: "correlation_id"
    - from: "docs/orchestra/hermes/role-engine-protocol/v1/examples/"
      to: "docs/orchestra/scripts/tests/test-role-engine-protocol.sh"
      via: "golden fixtures are validated by shell + Python smoke checks instead of prose-only review"
      pattern: "hermes-role-engine/v1"
    - from: "docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh"
      to: ".planning/phases/22-external-cli-engine-protocol-role-invocation/22-VERIFICATION.md"
      via: "failure-policy evidence is captured before ENG-01 and ENG-02 are marked complete"
      pattern: "retry once"
---

<objective>
Execute Phase 22 as the contract-and-invocation layer for the Hermes-native workflow MVP.

Purpose: satisfy ENG-01 and ENG-02 by extending the Phase 21 profile compiler with explicit external CLI engine config, shipping a first-class `hermes-role-engine/v1` contract package for `pm` / `implementer` / `reviewer`, and proving failure normalization with executable tests instead of design-only prose.
Output: checked-in engine defaults and override merge behavior, a shared protocol package with golden fixtures, failure-policy smoke tests, and Phase 22 verification evidence ready for milestone progression.
</objective>

<execution_context>
Use the GSD execute-plan workflow. Keep Phase 22 narrow: deliver engine config ownership, protocol artifacts, and failure normalization only. Do not implement Kanban state-machine routing, Orchestrator decision logic, risk hook enforcement, worker cleanup, or observability persistence in this phase.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/22-external-cli-engine-protocol-role-invocation/22-CONTEXT.md
@.planning/phases/22-external-cli-engine-protocol-role-invocation/22-DISCUSSION-LOG.md
@.planning/phases/22-external-cli-engine-protocol-role-invocation/22-RESEARCH.md
@.planning/phases/22-external-cli-engine-protocol-role-invocation/22-PATTERNS.md
@.planning/phases/21-profiles-overrides-board-isolation/21-VERIFICATION.md
@.planning/phases/19-hermes-workflow-design/EXTERNAL-CLI-ENGINE.md
@.planning/phases/19-hermes-workflow-design/REQUIREMENTS.md
@.planning/phases/19-hermes-workflow-design/DESIGN.md
@.planning/phases/19-hermes-workflow-design/CONSISTENCY-CHECKLIST.md
@docs/orchestra/scripts/bin/orch-profile-sync
@docs/orchestra/scripts/bin/orch-bus-loop
@docs/orchestra/poc-headless-gsd-execution.md
@docs/orchestra/hermes/profile-distribution/distribution.yaml
@docs/orchestra/hermes/profile-distribution/profiles/pm/config.yaml
@docs/orchestra/hermes/profile-distribution/profiles/implementer/config.yaml
@docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml
@docs/orchestra/scripts/tests/test-profile-packaging.sh
@docs/orchestra/scripts/tests/test-project-isolation.sh
@docs/orchestra/README.md
@.hermes/profiles/README.md

<interfaces>
Required engine keys:

```text
cli
mode
flags
fallback
```

Required closed roles in Phase 22:

```text
pm
implementer
reviewer
```

Required shared `next_action` enum:

```text
continue
wait_for_user
create_tasks
create_research_task
block
complete
defer_to_human
```

Required retained runtime metadata fields:

```text
conversation_history
handoff_from_parent
task_summary
current_stage
last_engine_error
rollback_count
```

Required normalized failure classes:

```text
timeout
crash
rate_limit
parse_error
schema_mismatch
```

Required compaction rule:

```text
summary + recent N raw turns
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend checked-in role configs and `orch-profile-sync` to support `engine` deep-merge</name>
  <files>docs/orchestra/hermes/profile-distribution/distribution.yaml, docs/orchestra/hermes/profile-distribution/profiles/pm/config.yaml, docs/orchestra/hermes/profile-distribution/profiles/implementer/config.yaml, docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml, .hermes/profiles/README.md, docs/orchestra/scripts/bin/orch-profile-sync</files>
  <read_first>
    Read 22-CONTEXT.md decisions D-22-01 through D-22-04, 22-RESEARCH.md findings about `orch-profile-sync`, 22-PATTERNS.md sections for config defaults and override docs, current `orch-profile-sync`, and the three checked-in role configs for `pm`, `implementer`, and `reviewer`.
  </read_first>
  <action>
    Extend the existing Phase 21 profile compiler path so `engine` becomes a first-class compiled field instead of a design-only comment.

    Required implementation boundaries:

```text
- Default engine settings live in each role's checked-in `config.yaml`
- Project overrides may deep-merge only the fields they need: `cli`, `mode`, `flags`, `fallback`
- `model` remains the Hermes routing-layer model and must not be conflated with `engine`
- No project override may leak into `~/.hermes/profiles/`
- `fallback` stays opt-in only; do not invent an implicit fallback for roles that did not declare one
```

    Update `.hermes/profiles/README.md` to document `engine` as a supported override surface, and bump `distribution.yaml` version if the generated runtime contract changes. Reuse the existing shell entry + inline Python structure in `orch-profile-sync`; do not add a second compiler or centralized engine matrix file.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
tmp="$(mktemp -d)"
trap '"'"'rm -rf "$tmp"'"'"' EXIT
export HOME="$tmp/home"
export RUNTIME_ROOT="$tmp/tmp/hermes-orchestra"
export STATE_ROOT="$tmp/state/hermes-orchestra"
export AUDIT_ROOT="$tmp/audit/hermes-orchestra"
export CACHE_ROOT="$tmp/cache/hermes-orchestra"
mkdir -p "$HOME"
project="$tmp/project"
mkdir -p "$project"
git -C "$project" init >/dev/null
"$PWD/docs/orchestra/scripts/bin/orch-init" test-proj "$project" >/dev/null
cat > "$project/.hermes/profiles/implementer.override.yaml" <<'"'"'YAML'"'"'
engine:
  flags: --dangerously-skip-permissions --json
  fallback: claude
YAML
"$PWD/docs/orchestra/scripts/bin/orch-profile-sync" test-proj "$project" >/dev/null
cfg="$project/.hermes/projects/test-proj/profiles/implementer/config.yaml"
rg -F "engine:" "$cfg" >/dev/null
rg -F "cli: codex" "$cfg" >/dev/null
rg -F "mode: exec" "$cfg" >/dev/null
rg -F "flags: --dangerously-skip-permissions --json" "$cfg" >/dev/null
rg -F "fallback: claude" "$cfg" >/dev/null
[ ! -e "$HOME/.hermes/profiles/implementer" ]
'
    </automated>
  </verify>
  <acceptance_criteria>
    Generated project-scoped role configs contain the merged `engine` object, preserve per-role defaults when an override supplies only one field, and never write project-specific engine settings into the user's global Hermes home.
  </acceptance_criteria>
  <done>ENG-01 now has an executable config compilation path instead of a design-only note.</done>
</task>

<task type="auto">
  <name>Task 2: Create the `hermes-role-engine/v1` contract package for `pm`, `implementer`, and `reviewer`</name>
  <files>docs/orchestra/hermes/role-engine-protocol/v1/common-envelope.md, docs/orchestra/hermes/role-engine-protocol/v1/roles/pm.md, docs/orchestra/hermes/role-engine-protocol/v1/roles/implementer.md, docs/orchestra/hermes/role-engine-protocol/v1/roles/reviewer.md, docs/orchestra/hermes/role-engine-protocol/v1/examples/</files>
  <read_first>
    Read 22-CONTEXT.md decisions D-22-05 through D-22-14, EXTERNAL-CLI-ENGINE.md sections 4 through 7, the Phase 19 consistency checklist, 22-PATTERNS.md recommendations for protocol layout, and `orch-bus-loop` only as a migration reference.
  </read_first>
  <action>
    Materialize a checked-in contract package under `docs/orchestra/hermes/role-engine-protocol/v1/` with one shared envelope spec plus role-specific contracts and JSON examples for `pm`, `implementer`, and `reviewer`.

    The package must explicitly lock:

```text
- `protocol = hermes-role-engine/v1`
- one shared `next_action` enum across roles
- role-specific `status` enums and payload expectations
- `correlation_id` as trace-only, not session authority
- canonical carried metadata fields and the "summary + recent N raw turns" compaction rule
- comments as audit-only, never canonical recovery state
```

    Minimum required fixtures:

```text
pm.request.json
pm.response.question.json
implementer.request.json
implementer.response.complete.json
reviewer.request.json
reviewer.response.findings.json
```

    Keep this task contract-first. Do not implement the future dispatcher or state-machine routing here.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
root=docs/orchestra/hermes/role-engine-protocol/v1
test -f "$root/common-envelope.md"
for role in pm implementer reviewer; do
  test -f "$root/roles/$role.md"
done
for fixture in \
  pm.request.json \
  pm.response.question.json \
  implementer.request.json \
  implementer.response.complete.json \
  reviewer.request.json \
  reviewer.response.findings.json; do
  test -f "$root/examples/$fixture"
done
rg -F "hermes-role-engine/v1" "$root/common-envelope.md" "$root/examples" >/dev/null
rg -F "next_action" "$root/common-envelope.md" "$root/roles/pm.md" "$root/roles/implementer.md" "$root/roles/reviewer.md" >/dev/null
rg -F "correlation_id" "$root/common-envelope.md" >/dev/null
'
    </automated>
  </verify>
  <acceptance_criteria>
    The repo contains a reviewable contract surface for the first three closed roles, with one shared envelope and role-specific status/payload rules. The contract package stands on its own without requiring readers to reconstruct the protocol from planning prose.
  </acceptance_criteria>
  <done>ENG-02 has a canonical contract package that downstream routing and adapter work can consume without reopening Phase 19 design debates.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixture-driven tests for engine merge, protocol shape, and failure normalization</name>
  <files>docs/orchestra/scripts/tests/test-profile-packaging.sh, docs/orchestra/scripts/tests/test-project-isolation.sh, docs/orchestra/scripts/tests/test-role-engine-protocol.sh, docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh</files>
  <read_first>
    Read the outputs of Tasks 1 and 2, 22-RESEARCH.md verification recommendations, current `test-profile-packaging.sh`, current `test-project-isolation.sh`, and `orch-bus-loop` failure-handling references for timeout / parse mismatch behavior.
  </read_first>
  <action>
    Extend the existing profile smoke tests and add two new protocol-specific tests so Phase 22 is proven by fixtures rather than manual inspection.

    Required coverage:

```text
- `test-profile-packaging.sh`: assert generated configs include merged `engine` fields
- `test-project-isolation.sh`: assert different projects may diverge on engine overrides without runtime bleed
- `test-role-engine-protocol.sh`: validate shared envelope fields, shared `next_action`, and role-specific `status` / payload expectations against the golden JSON fixtures
- `test-role-engine-failure-policy.sh`: validate timeout / crash / rate_limit => retry once then block, fallback audit requirements, and parse_error / schema_mismatch => immediate block with no fallback
```

    Keep the tests in the existing shell + inline Python style already used in this repo. If a helper is needed, keep it local to the Phase 22 test surface; do not build a general framework.
  </action>
  <verify>
    <automated>
rtk docs/orchestra/scripts/tests/test-profile-packaging.sh
rtk docs/orchestra/scripts/tests/test-project-isolation.sh
rtk docs/orchestra/scripts/tests/test-role-engine-protocol.sh
rtk docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh
    </automated>
  </verify>
  <acceptance_criteria>
    Targeted Phase 22 tests fail when engine merge, protocol fields, or failure taxonomy drift. The locked hard-stop cases (`parse_error`, `schema_mismatch`) are explicitly tested as non-fallback paths.
  </acceptance_criteria>
  <done>Phase 22 contract drift becomes machine-detectable before Phase 23 consumes the protocol.</done>
</task>

<task type="auto">
  <name>Task 4: Update runtime docs, planning traceability, and Phase 22 verification evidence</name>
  <files>docs/orchestra/README.md, .hermes/profiles/README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/phases/22-external-cli-engine-protocol-role-invocation/22-VERIFICATION.md</files>
  <read_first>
    Read the diffs from Tasks 1 through 3, 22-CONTEXT.md decisions D-22-15 through D-22-19, 21-VERIFICATION.md for inherited blocker handling style, and the current roadmap / requirements / project milestone status text.
  </read_first>
  <action>
    Update user-facing docs and planning traceability so they describe the same executable contract as the code and fixtures. At minimum, document:

```text
- `engine.cli/mode/flags/fallback` ownership and override semantics
- the role-engine protocol package location
- the first closed roles (`pm`, `implementer`, `reviewer`)
- the retry-once-then-block recovery model
- fallback audit requirements and hard-stop parse/schema mismatch behavior
```

    Write `22-VERIFICATION.md` with targeted test evidence and residual blocker classification. Only after the targeted Phase 22 checks pass may you mark `ENG-01` and `ENG-02` complete in planning traceability. Run the aggregate repo gate as a final confidence check; if it still fails only because of the inherited Phase 20/21 upstream pin mismatch, record that exactly instead of misclassifying it as a new Phase 22 regression.
  </action>
  <verify>
    <automated>
rtk bash -lc 'set -euo pipefail
rg -F "engine.cli" docs/orchestra/README.md .hermes/profiles/README.md >/dev/null
rg -F "hermes-role-engine/v1" docs/orchestra/README.md >/dev/null
rg -F "ENG-01" .planning/REQUIREMENTS.md >/dev/null
rg -F "ENG-02" .planning/REQUIREMENTS.md >/dev/null
test -f .planning/phases/22-external-cli-engine-protocol-role-invocation/22-VERIFICATION.md
'

rtk make test
    </automated>
  </verify>
  <acceptance_criteria>
    Docs, verification evidence, and milestone traceability describe the same Phase 22 contract as the implementation. Any remaining aggregate-gate failure is either a genuine new regression or the previously documented upstream pin mismatch, with no ambiguity.
  </acceptance_criteria>
  <done>Phase 22 is documented, verifiable, and ready to hand off to Phase 23 without reopening engine-ownership or protocol-baseline decisions.</done>
</task>

</tasks>
