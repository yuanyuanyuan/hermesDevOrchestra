## 五、多项目并行管理子流程（§7）

> 📎 **相关叙事文档**：
> - 多项目上下文 → [`workflow-phase-02-orchestrator.md`](./workflow-phase-02-orchestrator.md) — PM 管理多项目任务分解
> - 跨项目 Skill 进化 → [`workflow-phase-05-fix-evolution.md`](./workflow-phase-05-fix-evolution.md) — Step 5.4 记录经验教训
>
> **能力来源说明：** Board 隔离（独立 SQLite、独立 workspace、独立 dispatcher）属于 `[Hermes 官方]` 能力。跨项目经验共享的分层命名空间、`cross-project` 标记限制（R7e）、查询冲突警告（R7c）、删除传染机制（R7d）、Curator 语义相似度合并（R7b）属于 `[Phase 19 增量]`。

---

### 跨项目经验共享

```
                    ┌─────────────────────────┐
                    │   Hermes Gateway        │
                    │   (统一入口)             │
                    └───────────┬─────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              │                 │                 │
              ▼                 ▼                 ▼
   ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
   │ Board: Alpha    │ │ Board: Beta     │ │ Board: Gamma    │
   │ (项目A任务板)    │ │ (项目B任务板)    │ │ (项目C任务板)    │
   │                 │ │                 │ │                 │
   │ 独立 SQLite     │ │ 独立 SQLite     │ │ 独立 SQLite     │
   │ 独立 workspace  │ │ 独立 workspace  │ │ 独立 workspace  │
   │ 独立 dispatcher │ │ 独立 dispatcher │ │ 独立 dispatcher │
   │                 │ │                 │ │                 │
   │ T1: 实现登录    │ │ T1: 修复Bug     │ │ T1: 优化性能    │
   │ T2: 写测试      │ │ T2: 发布补丁    │ │ T2: 重构代码    │
   │ T3: 审查代码    │ │                 │ │                 │
   └────────┬────────┘ └────────┬────────┘ └────────┬────────┘
            │                   │                   │
            │                   │                   │
            │    Board 完全隔离  │  (任务不可跨板引用) │
            │                   │                   │
            └───────────────────┼───────────────────┘
                                │
                                ▼
                    ┌─────────────────────────┐
                    │     经验共享层          │
                    │   (跨 Board 打通)       │
                    └───────────┬─────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              │                 │                 │
              ▼                 ▼                 ▼
   ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
   │ ① Memory        │ │ ② Skills        │ │ ③ Session       │
   │   全局 MEMORY.md │ │   全局可用       │ │   Search        │
   │   所有项目生效   │ │   ~/.hermes/     │ │   FTS5 搜索     │
   │                 │ │   skills/        │ │   所有历史      │
   └─────────────────┘ └─────────────────┘ └─────────────────┘
                                │
                                ▼
                    ┌─────────────────────────┐
                    │  ④ Profile SOUL.md      │
                    │     全局共享             │
                    │     角色定义统一         │
                    └─────────────────────────┘
                                │
                                ▼
                    ┌─────────────────────────┐
                    │  ⑤ .learnings/ 分层     │  [Phase 19 增量]
                    │     命名空间 (R7)       │
                    │                         │
                    │  project-a/ (默认私有)   │
                    │  _global/   (审核后共享) │
                    │  _pending/  (待审核)     │
                    └───────────┬─────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              │                 │                 │
              ▼                 ▼                 ▼
   ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
   │ R7b: Curator    │ │ R7c: 查询冲突   │ │ R7e: cross-     │
   │ 语义相似度合并   │ │ 显式警告        │ │ project 标记    │
   │ 同主题 learning │ │ 项目 vs 全局    │ │ 仅 orchestrator │
   │ 超阈值触发合并   │ │ 矛盾时两端显示  │ │ 或用户可标注    │
   └─────────────────┘ └─────────────────┘ └─────────────────┘
                                │
                                ▼
                    ┌─────────────────────────┐
                    │  R7d: 删除传染机制      │
                    │  · 仅本项目（默认）      │
                    │  · 触发其他项目复审      │
                    │  · 全局回退（需二次审核）│
                    └─────────────────────────┘

        大白话比喻:
        ┌──────────────────────────────────────────────┐
        │                                              │
        │   10 个独立部门 (Board)                       │
        │   ├── 部门A: 做电商网站                       │
        │   ├── 部门B: 做后台管理                       │
        │   └── 部门C: 做数据分析                       │
        │                                              │
        │   每个部门有自己的任务板、自己的工作区         │
        │   互不干扰                                   │
        │                                              │
        │   但共用一本"公司经验手册" (Memory/Skills)    │
        │   部门A踩的坑，部门B也会记得                   │
        │                                              │
        └──────────────────────────────────────────────┘
```

**大白话：** 你有 10 个项目，每个项目的任务板完全隔离，但经验教训是共享的——A 项目踩的坑，B 项目的工人也会记得。

---

