## 八、端到端完整示例 ASCII 图（§10）

场景：**"给项目 Alpha 加用户认证模块"**

> 📎 **相关叙事文档**：
> - 系统需求 → [`REQUIREMENTS.md`](./REQUIREMENTS.md)（含 F4 故障检测、R12 Worker 存活判定等定义）
> - Phase 1-2 → [`workflow-phase-01-requirements.md`](./workflow-phase-01-requirements.md)、[`workflow-phase-02-orchestrator.md`](./workflow-phase-02-orchestrator.md)
> - Phase 3 → [`workflow-phase-03-implementation.md`](./workflow-phase-03-implementation.md)
> - Phase 4 → [`workflow-phase-04-testing-review.md`](./workflow-phase-04-testing-review.md)
> - Phase 5 / 5.5 → [`workflow-phase-05-fix-evolution.md`](./workflow-phase-05-fix-evolution.md)
> - Phase 6 → [`workflow-phase-06-completion.md`](./workflow-phase-06-completion.md)

---

### Phase 1-2: 需求提交 → 技术发现 → 需求澄清 → 任务拆解

```
   ┌─────────┐                              ┌─────────────────┐
   │  CEO    │                              │       PM        │
   │ (用户)  │                              │  (需求分析/拆解) │
   └────┬────┘                              └────────┬────────┘
        │                                            │
        │ ① 提模糊需求（不带技术细节）                │
        │ "登录体验太差了"                            │
        │─────────────────────────────────────────────►│
        │                                            │
        │  (如果 Board 上有多个需求)                  │
        │  ①.5 多需求优先级排序                       │
        │◄──── "建议顺序: 登录→搜索→首页"            │
        │──── "确认顺序" ───────────────────────────►│
        │                                            │
        │                                            │ ② 初始技术发现（从入口出发，按需触发）
        │                                            │    读 CLAUDE.md/AGENTS.md → 项目入口
        │                                            │    按入口指引 → Cargo.toml → src/ → 按需深入
        │                                            │    输出: 技术发现报告(含证据,文件:行号)
        │                                            │    (后续澄清中按需补充发现)
        │                                            │
        │                                            │ ③ 需求澄清（一次一问，逐步收缩）
        │                                            │
        │                                            │  ┌─────────────────────────────┐
        │                                            │  │ 已知: "改善登录体验"        │
        │                                            │  │ 约束: 什么都可能            │
        │                                            │  └─────────────┬───────────────┘
        │                                            │                │
        │  ④ Q1: 核心目标？                           │                │
        │◄──── clarify(一个问题)                     │                │
        │      ⭐A)7天免登录 ○B)永远 △C)安全超时     │                │
        │──── "A — 7天免登录" ──────────────────────►│                │
        │                                            │  ┌─────────────▼───────────────┐
        │                                            │  │ 收束: 目标=7天免登录        │
        │                                            │  │ 排除: 无限期/短会话         │
        │                                            │  └─────────────┬───────────────┘
        │                                            │                │
        │  ⑤ Q2: 用户群体？                           │                │
        │◄──── clarify(基于Q1优化)                   │                │
        │──── "A — 外部付费客户" ───────────────────►│                │
        │                                            │  ┌─────────────▼───────────────┐
        │                                            │  │ 收束: 安全标准=高           │
        │                                            │  └─────────────┬───────────────┘
        │                                            │                │
        │  ⑥ Q3: 时间压力？                           │                │
        │──── "B — 1-2周" ──────────────────────────►│                │
        │                                            │  ┌─────────────▼───────────────┐
        │                                            │  │ 收束: 复杂度上界确定        │
        │                                            │  └─────────────┬───────────────┘
        │                                            │                │
        │  ... Q4~Q11 一次一问，逐步收束 ...          │                │
        │    (每确认一个关键维度就做可行性检查)        │                │
        │                                            │                │
        │  (如果发现冲突，立即沟通，不等最后)          │                │
        │  ⑦ ⚠️ 冲突沟通                              │                │
        │◄──── "发现冲突: 范围 vs 时间"              │                │
        │      证据: 工作量估算 > 时间约束            │                │
        │      建议: A)缩小范围 B)延长时间            │                │
        │──── "A — 先做核心认证" ───────────────────►│                │
        │                                            │                │
        │                                            │ ⑧ 生成标准化需求文档
        │                                            │    含: 证据索引(文件:行号)
        │                                            │    含: 可行性确认表
        │                                            │
        │                                            │ ⑨ DoR 验证门
        │                                            │    7项逐项检查,不通过则补问
        │                                            │    ✅ 全部通过
        │                                            │
        │  ⑩ 确认需求（显式关卡）                     │
        │◄──── "需求 v1 已生成，确认？"              │
        │      [确认] [修改需求] [其他]               │
        │                                            │
        │  选"确认":                                  │
        │──── "确认 v1" ────────────────────────────►│
        │                                            │    需求版本: v1
        │                                            │
        │  选"修改需求":                              │
        │◄──── "修改哪部分？"                         │
        │──── "功能范围" ───────────────────────────►│ → 回到 Q10 重新澄清
        │                                            │    → 重新 DoR 验证
        │                                            │    → 需求版本: v2
        │                                            │
        │  ⑩.5 质量反馈                               │
        │◄──── "澄清过程清晰吗？有遗漏吗？"          │
        │──── "5分，无遗漏" ────────────────────────►│    记录反馈，持续优化
        │                                            │
        │                                            │ ⑪ 技术不确定性评估
        │                                            │    PM 判断: 技术方案是否确定？
        │                                            │
        │                                            │    ┌──────────┐
        │                                            │    │ 确定?     │
        │                                            │    └────┬─────┘
        │                                            │         │
        │                                            │   ┌─────┴──────┐
        │                                            │   │            │
        │                                            │  低           高
        │                                            │   │            │
        │                                            │   │     ┌──────▼──────────────────────┐
        │                                            │   │     │ 创建 Research 子任务        │
        │                                            │   │     │ assignee: researcher        │
        │                                            │   │     └──────┬──────────────────────┘
        │                                            │   │            │
        │                                            │   │     ┌──────▼──────────────────────┐
        │                                            │   │     │ Researcher 技术调研         │
        │                                            │   │     │ · 代码走读 + 现有方案分析   │
        │                                            │   │     │ · 输出: 技术提案文档        │
        │                                            │   │     │   (方案对比/风险/工作量)    │
        │                                            │   │     │ · 不写代码                  │
        │                                            │   │     └──────┬──────────────────────┘
        │                                            │   │            │
        │                                            │   │     ┌──────▼──────────────┐
        │                                            │   │     │ PM 评审提案:         │
        │                                            │   │     │ 需要 POC 验证?       │
        │                                            │   │     └──────┬──────────────┘
        │                                            │   │            │
        │                                            │   │      ┌─────┴─────┐
        │                                            │   │      │           │
        │                                            │   │     否          是
        │                                            │   │      │           │
        │                                            │   │      │    ┌──────▼──────────────────────┐
        │                                            │   │      │    │ 创建 POC 子任务              │
        │                                            │   │      │    │ assignee: implementer        │
        │                                            │   │      │    │ → 独立 worktree 隔离执行      │
        │                                            │   │      │    │ → 验证技术可行性             │
        │                                            │   │      │    └──────┬──────────────────────┘
        │                                            │   │      │           │
        │                                            │   │      │    ┌──────▼──────────────┐
        │                                            │   │      │    │ POC 结果?            │
        │                                            │   │      │    └──┬──────────┬───────┘
        │                                            │   │      │       │          │
        │                                            │   │      │      通过       不通过
        │                                            │   │      │       │          │
        │                                            │   │      │       │    ┌─────▼────────┐
        │                                            │   │      │       │    │ 重新调研      │
        │                                            │   │      │       │    │ (回 Research) │
        │                                            │   │      │       │    └──────────────┘
        │                                            │   │      │       │
        │◄─────────────────────────────────────────────┼───────┴───────┘
        │                                            │
        │                                            │ ⑫ 任务拆解（含需求追溯）
        │                                            │    每个任务关联 US-1~US-4
        │                                            │
        │                                            │ ⑬ PM → Orchestrator 交接
        │                                            │    Orchestrator 负责:
        │                                            │    · 任务分发 (dispatch)
        │                                            │    · 进度监控 (monitoring)
        │                                            │    · 消息路由 (routing)
        │                                            │    · 状态机驱动，不做分析/拆解
        │                                            │
        │◄─────────────────────────────────────────────│
        │ ⑭ 完成通知                                   │
        │                                            │
        └────────────────────────────────────────────┘
```

**大白话：** 老板说"登录体验太差了"，PM（需求分析角色）先读项目的 CLAUDE.md 入口文件，按指引读代码搞清楚技术现状（只读权限，不能写代码）。如果 Board 上有多个需求，先排优先级让老板确认。然后一次问一个问题，每个问题都有推荐标签和大白话理由，**问题顺序是动态的**——根据老板的回答和可行性约束决定下一个问什么。技术发现不是一次性的，而是与澄清交织进行的（老板选了"Redis"就去查 Redis 相关代码）。如果老板反复选"其他"，系统会在第 2 次合成摘要、第 3 次强制收敛，同一维度最多收敛+修改各 1 次。**证据必须来自外部或实证**（代码引用/搜索结果/POC），不做推理。**可行性检查贯穿全程**，发现冲突或阻塞立即沟通。**澄清是异步的**——老板中途去开会不会导致任务被回收，每轮通过 comments 保存进度，崩溃后可从检查点恢复。所有问题问完后，通过 DoR 验证门逐项检查需求质量。最后**必须经过老板显式确认**才能进入后续流程。确认后还会收集质量反馈用于持续优化。**PM 评估技术不确定性**：如果方案不确定，先创建 Research 子任务交给 Researcher（只调研不写代码），产出技术提案；如果提案仍有风险，再创建 POC 子任务交给 Implementer 在独立 worktree 里验证可行性。POC 通过后才进入任务拆解，拆解时每个子任务都关联到具体的用户故事和验收标准。**PM 完成拆解后，将任务交接给 Orchestrator**——Orchestrator 是纯路由器/调度器，负责任务分发、进度监控和消息路由（状态机驱动），不做需求分析或任务拆解。

---

### Phase 3: 执行（Implementer）

```
   ┌─────────────────────────────────────────────────────────────────┐
   │                     Implementer Worker                          │
   │                     执行任务 T1: 实现 JWT                         │
   └─────────────────────────────────────────────────────────────────┘
        │
        │ ① kanban_show() → 读取 T1 详情
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ② 跑现有测试建立基线                                             │
   │    terminal(command="npm test")                                  │
   │    → exit 0 (基线正常)                                          │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ③ 调用底层 CLI 编码                                              │
   │    ┌─────────────────────────────────────────────────────────┐  │
   │    │  model = codex:                                         │  │
   │    │    terminal("codex exec --full-auto '实现 JWT...'")      │  │
   │    │                                                         │  │
   │    │  model = claude:                                        │  │
   │    │    terminal("claude -p '实现 JWT...' --max-turns 10")    │  │
   │    └─────────────────────────────────────────────────────────┘  │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ④ 进度报告（非存活检测，存活由 R12 PID 探测判定）                   │
   │    kanban_comment(                                              │
   │      body="已实现登录接口，正在实现注册..."                        │
   │    )                                                            │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ⑤ 遇到技术疑问?                                                  │
   │    "RS256 vs HS256?"                                            │
   │         │                                                       │
   │         ├── 是 ──→ 走实时问答流程 (tmux / Kanban block)          │
   │         │         得到答案后继续                                 │
   │         │                                                       │
   │         └── 否 ──→ 继续编码                                      │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ⑥ 完成!                                                         │
   │    kanban_complete(                                             │
   │      summary="JWT 认证已实现",                                   │
   │      metadata={                                                 │
   │        "changed_files": ["src/auth/jwt.rs"],                    │
   │        "tests_run": 12,                                         │
   │        "tests_passed": 12,                                      │
   │        "decisions": ["用 RS256"],                               │
   │        "pitfalls": ["token刷新用滑动窗口"]                        │
   │      }                                                          │
   │    )                                                            │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │  T1 → done                                                     │
   │  T2, T3 (parents=[T1]) → 同时变为 ready                         │
   └─────────────────────────────────────────────────────────────────┘
```

**大白话：** 开发工人接到工单"写 JWT 认证"。先跑一遍现有测试确认"现在一切正常"，然后开始写代码。写到一半不确定用 RS256 还是 HS256，问了一下专家得到"用 RS256"的答案后继续。写完测试全过，填交接单：改了 2 个文件、12 个测试全过、用了 RS256、注意刷新 token 用滑动窗口。

---

### Phase 4: 测试 + 审查（并行）

```
                    T1 done
                      │
                      ▼
        ┌─────────────────────────────┐
        │  T2 和 T3 同时变为 ready     │
        │  (parents=[T1] 已满足)       │
        └─────────────┬───────────────┘
                      │
        ┌─────────────┴─────────────┐
        ▼                           ▼
   ┌─────────────────┐       ┌─────────────────┐
   │  T2: 写测试      │       │  T3: 审查代码    │
   │                 │       │                 │
   │  Implementer    │       │  Reviewer        │
   │  (并行执行)      │       │  (硬门控/只读)    │
   └────────┬────────┘       └────────┬────────┘
            │                         │
            ▼                         ▼
   ┌─────────────────┐       ┌─────────────────┐
   │ 读取 T1 handoff │       │ 读取 T1 handoff │
   │ (改了哪些文件)  │       │ (改了哪些文件)  │
   │                 │       │ (做了什么决策)  │
   └────────┬────────┘       └────────┬────────┘
            │                         │
            ▼                         ▼
   ┌─────────────────┐       ┌─────────────────┐
   │ terminal(       │       │ terminal(       │
   │  "pytest..."    │       │  "claude -p     │
   │ )               │       │   '审查代码...'"│
   │                 │       │ )               │
   │ tests: 14/14 ✓  │       │                 │
   └────────┬────────┘       │ findings:       │
            │                │  1 high severity│
            │                │  "token过期时间  │
            │                │   硬编码"        │
            │                └────────┬────────┘
            │                         │
            ▼                         ▼
   ┌─────────────────┐       ┌─────────────────┐
   │ kanban_complete │       │ kanban_complete │
   │ tests_run: 14   │       │ approved: false │
   │ tests_passed: 14│       │ findings: [...] │
   │ status: done    │       │ status: done    │
   └─────────────────┘       └─────────────────┘
            │                         │
            │                         │ T3 done → T4 ready
            │                         │
            └─────────────────────────┘
                      │
                      ▼
               ┌─────────────┐
               │ T4: 修复问题 │
               │  (等 T3)     │
               └─────────────┘
```

**大白话：** 代码写完了，两个工人同时开工——一个写测试用例（"登录成功/失败/密码错误..."），一个审查代码（"这里安全吗？规范吗？"）。审查员是**严格只读**的——只看代码不改代码，审查结论是**硬门控**：不通过就阻塞，必须修复后重新提交才能继续。这里审查员发现问题："token 过期时间写死在代码里了，应该改成可配置的！"打了"不通过"，流程被阻塞直到修复完成。

---

### Phase 5: 修复 + 自我进化

```
   ┌─────────────────────────────────────────────────────────────────┐
   │                     Implementer Worker                          │
   │                     执行任务 T4: 修复审查问题                      │
   └─────────────────────────────────────────────────────────────────┘
        │
        │ ① kanban_show() → 读取 T3 handoff
        │    (findings: token 过期时间硬编码)
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ② 修复问题                                                       │
   │    terminal(                                                     │
   │      "codex exec --full-auto                                     │
   │       '修复 token 过期时间硬编码问题...'"                         │
   │    )                                                             │
   │                                                                  │
   │    修改:                                                         │
   │    - src/auth/jwt.rs: 过期时间 → 读取环境变量                     │
   │    - config/default.toml: 添加 TOKEN_EXPIRY 配置                │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ③ 验证                                                           │
   │    terminal("pytest") → 14/14 ✓                                 │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ④ 完成任务                                                       │
   │    kanban_complete(                                             │
   │      summary="已修复，token 过期时间改为环境变量配置",             │
   │      metadata={                                                 │
   │        "changed_files": [                                        │
   │          "src/auth/jwt.rs",                                      │
   │          "config/default.toml"                                   │
   │        ],                                                        │
   │        "tests_run": 14,                                         │
   │        "tests_passed": 14                                       │
   │      }                                                          │
   │    )                                                            │
   └─────────────────────────────────────────────────────────────────┘
        │
        │ ⑤ 自我进化（边干边记）
        │
        ├──→ memory add
        │    "JWT token 过期时间应可配置，不要硬编码"
        │
        └──→ skill_manage create
             "jwt-auth-checklist"
             → 新项目直接用 checklist 避免重复踩坑
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │  T4 → done                                                     │
   │  所有任务完成!                                                  │
   └─────────────────────────────────────────────────────────────────┘
```

**大白话：** 开发工人看到审查意见"过期时间硬编码了，得改"，改完提交。同时"长记性"——记下"以后写 JWT 别把过期时间写死"。又发现"每次做认证都要检查这几件事"，干脆写成一张 checklist，以后新项目直接套用。

---

### Phase 5.5: 故障场景 — 部署失败，人工升级触发 SRE-Observer

```
   ┌─────────────────────────────────────────────────────────────────┐
   │                  DevOps-Engineer Worker                         │
   │                  执行任务 T5: 部署发布                           │
   └─────────────────────────────────────────────────────────────────┘
        │
        │ ① 执行部署脚本
        │    terminal("./deploy.sh production")
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ② 返回 exit code 1                                              │
   │    stderr: "DATABASE_URL: parameter not set"                    │
   │    outcome: crashed                                             │
   └─────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ③ Orchestrator 检测到故障 (状态机驱动)                           │
   │    outcome='crashed'                                            │
   │    Hermes 原生: 自动重试/回滚 (crash/timed_out)                  │
   │    人工升级路径: CEO 判断需要深度分析 → 手动触发 SRE-Observer     │
   └─────────────────────────────────────────────────────────────────┘
        │
        │ (CEO 手动触发深度分析)
        │
        ├──→ 步骤 A: 采集环境快照
        │    - git status
        │    - df -h (前5行)
        │    - hermes status
        │    → 关联到 T5 task_run metadata
        │
        └──→ 步骤 B: 创建 SRE 任务
             kanban_create(
               title="根因分析: T5 部署失败",
               assignee="sre-observer",
               parents=[T5],
               priority="high"
             )
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │                  SRE-Observer Worker                            │
   │                  执行任务 T5-sre: 根因分析                        │
   └─────────────────────────────────────────────────────────────────┘
        │
        │ ④ 读取所有证据
        │    ┌─────────────────────────────────────────────────────┐
        │    │ ① kanban_show(T5) → 任务详情                       │
        │    │ ② trace.db → tool call #3: deploy.sh exited 1      │
        │    │ ③ worker logs → stderr: DATABASE_URL not set       │
        │    │ ④ task_events → claim→crashed 时间线               │
        │    │ ⑤ env snapshot → production 分支正常               │
        │    │ ⑥ audit logs → 无策略拦截                          │
        │    │ ⑦ parent handoff → 上游无缺陷                      │
        │    └─────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ⑤ 综合分析 → 根因定位: 环境层                                    │
   │    生产环境缺少 DATABASE_URL 环境变量                            │
   │    DevOps 脚本未检查环境变量即执行                                │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ⑥ 输出根因报告                                                   │
   │    kanban_complete(                                             │
   │      summary="根因: 生产环境缺 DATABASE_URL",                    │
   │      metadata={                                                 │
   │        "root_cause_category": "environment",                    │
   │        "confidence": "high",                                    │
   │        "symptom": "deploy.sh exited 1",                         │
   │        "responsible_profile": "devops-engineer",                │
   │        "recommended_action":                                     │
   │          "补全 DATABASE_URL 后重新部署",                         │
   │        "trace_anchor":                                          │
   │          "tool_call_#3 terminal(deploy.sh) exited 1"            │
   │      }                                                          │
   │    )                                                            │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │ ⑦ Orchestrator 通知 CEO                                          │
   │    Gateway 推送:                                                 │
   │    "项目 Alpha 部署失败，SRE 已定位根因：                         │
   │     类别: environment | 置信度: high                             │
   │     建议: 补全 DATABASE_URL 后重新部署 T5"                       │
   └─────────────────────────────────────────────────────────────────┘
```

**大白话：** 发布工人执行部署脚本，结果报错退出了。Hermes 原生处理 crash/timed_out 自动恢复（重试/回滚），但这次老板判断需要深度分析，手动触发 SRE-Observer 介入。SRE-Observer 翻遍所有证据后发现：生产服务器上没配 DATABASE_URL 环境变量。写了一份报告，同时推送给老板："部署失败，原因找到了——缺个环境变量，补上再跑一次就行。"老板不需要自己翻日志。注意：SRE-Observer 不是自动触发的，只在人工判断需要深度根因分析时才介入。

---

### Phase 6: 完成通知

```
   ┌─────────────────────────────────────────────────────────────────┐
   │                      所有任务 done                               │
   │  T1 ✓  T2 ✓  T3 ✓  T4 ✓                                        │
   │  (T5 ✗ → T5-sre ✓ 根因已定位)                                   │
   └─────────────────────────────┬───────────────────────────────────┘
                                 │
                                 ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │                    Gateway 推送通知                               │
   │                                                                 │
   │  ┌─────────────────────────────────────────────────────────┐    │
   │  │  📬 项目 Alpha - 用户认证模块 已完成                     │    │
   │  │                                                         │    │
   │  │  ✅ 变更文件:                                            │    │
   │  │     - src/auth/jwt.rs                                   │    │
   │  │     - src/auth/routes.rs                                │    │
   │  │     - config/default.toml                               │    │
   │  │                                                         │    │
   │  │  ✅ 测试: 14/14 通过                                     │    │
   │  │                                                         │    │
   │  │  ✅ 审查: 1 个问题已修复                                 │    │
   │  │                                                         │    │
   │  │  🧠 经验沉淀:                                            │    │
   │  │     - 2 条 memory (JWT 最佳实践)                        │    │
   │  │     - 1 个新 skill (jwt-auth-checklist)                 │    │
   │  │                                                         │    │
   │  │  ⚠️  部署状态: 失败 (根因已定位)                         │    │
   │  │      建议: 补全 DATABASE_URL 后重试                      │    │
   │  │                                                         │    │
   │  └─────────────────────────────────────────────────────────┘    │
   │                                                                 │
   │  推送渠道: CLI / Dashboard / Telegram / Discord / 邮件          │
   └─────────────────────────────────────────────────────────────────┘
        │
        ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │                      CEO (Jacky) 决策                            │
   │                                                                 │
   │  [批准归档]  [查看详情]  [处理部署问题]  [追加需求]              │
   └─────────────────────────────────────────────────────────────────┘
```

**大白话：** 所有工单都干完了，系统自动给你发一条消息："Alpha 项目的登录功能做好了，改了 3 个文件，测试全过，还沉淀了 2 条经验和 1 个 checklist。部署那边出了点问题——缺个环境变量，补上再跑一次就行。你看一下，没问题就归档。"


---

