---
phase: 15-specification-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/README.md
  - specs/file-bus.md
  - specs/risk-decisions.md
  - specs/commands.md
  - docs/orchestra/scripts/tests/test-specs.sh
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
user_setup: []
must_haves:
  truths:
    - "User can identify `.planning/SPEC.md` as the only canonical specification before reading any derived spec."
    - "Every derived `specs/*.md` file declares `## Source`, `## Consumers`, `## Drift Check`, and `## Conformance Checks`."
    - "Every derived spec lists only concrete current repository consumer paths, and no consumerless derived spec exists."
    - "`test-specs.sh` fails on missing required sections, missing canonical source, malformed or missing consumer paths, missing drift command, missing conformance checks, and unindexed specs."
    - "The existing smoke runner discovers the new spec checks without Makefile or runtime changes."
  artifacts:
    - path: "specs/README.md"
      provides: "Derived spec index and canonical authority relationship"
      contains: ".planning/SPEC.md"
    - path: "specs/file-bus.md"
      provides: "Narrow file-bus projection sourced from `.planning/SPEC.md` BUS sections"
      contains: "## Source"
    - path: "specs/risk-decisions.md"
      provides: "Narrow risk/decision projection sourced from `.planning/SPEC.md` AUTH/RISK/REMOTE sections"
      contains: "## Source"
    - path: "specs/commands.md"
      provides: "Narrow command projection sourced from `.planning/SPEC.md` CMD sections"
      contains: "## Source"
    - path: "docs/orchestra/scripts/tests/test-specs.sh"
      provides: "Failing conformance checks for derived specs"
      contains: "TEST_NAME=\"specs-contract\""
  key_links:
    - from: "specs/README.md"
      to: "specs/file-bus.md"
      via: "index entry"
      pattern: "file-bus\\.md"
    - from: "specs/README.md"
      to: "specs/risk-decisions.md"
      via: "index entry"
      pattern: "risk-decisions\\.md"
    - from: "specs/README.md"
      to: "specs/commands.md"
      via: "index entry"
      pattern: "commands\\.md"
    - from: "specs/*.md"
      to: ".planning/SPEC.md"
      via: "Source section and canonical conflict rule"
      pattern: "\\.planning/SPEC\\.md"
    - from: "docs/orchestra/scripts/tests/test-specs.sh"
      to: "specs/*.md"
      via: "repo-root spec discovery"
      pattern: "SPECS_DIR=.*specs"
    - from: "docs/orchestra/scripts/tests/run-all.sh"
      to: "docs/orchestra/scripts/tests/test-specs.sh"
      via: "test-*.sh glob discovery"
      pattern: "test-\\*\\.sh"
---

<objective>
Create the Phase 15 derived specification system while preserving `.planning/SPEC.md` as the only canonical specification.

Purpose: SPEC-01 and SPEC-02 require consumer-oriented `specs/*.md` projections with source, consumer, drift, and conformance metadata, plus at least one failing conformance check per derived spec.

Output: `specs/README.md`, `specs/file-bus.md`, `specs/risk-decisions.md`, `specs/commands.md`, and `docs/orchestra/scripts/tests/test-specs.sh`.
</objective>

<execution_context>
@/home/stark/.codex/get-shit-done/workflows/execute-plan.md
@/home/stark/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/SPEC.md
@.planning/phases/15-specification-system/15-CONTEXT.md
@.planning/phases/15-specification-system/15-RESEARCH.md
@.planning/phases/15-specification-system/15-VALIDATION.md
@.planning/phases/15-specification-system/15-PATTERNS.md
@docs/orchestra/scripts/tests/test-docs.sh
@docs/orchestra/scripts/tests/lib/assert.sh
@docs/orchestra/scripts/tests/run-all.sh

<interfaces>
Smoke test pattern from `docs/orchestra/scripts/tests/test-docs.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail

TEST_NAME="docs-contract"
TEST_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$TEST_DIR/../../../.." && pwd)"

# shellcheck source=lib/assert.sh
source "$TEST_DIR/lib/assert.sh"
```

Assertion helper exports from `docs/orchestra/scripts/tests/lib/assert.sh`:
```bash
fail()
assert_eq()
assert_contains()
assert_file_exists()
assert_executable()
assert_jsonl_valid()
assert_exit_code()
make_fake_path()
test_done()
```

Runner discovery from `docs/orchestra/scripts/tests/run-all.sh`:
```bash
for test_script in "$TEST_DIR"/test-*.sh; do
    if bash "$test_script"; then
        echo "PASS $test_script"
        PASSED=$((PASSED + 1))
    else
        echo "FAIL $test_script"
        FAILED=$((FAILED + 1))
    fi
done
```
</interfaces>
</context>

<source_audit>
| Source | ID | Required Item | Plan/Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Establish `specs/` derived documentation system while keeping `.planning/SPEC.md` canonical. | 15-01 T1, T2 | COVERED | T1 creates docs; T2 enforces metadata and canonical source. |
| REQ | SPEC-01 | `.planning/SPEC.md` remains canonical; each derived spec declares source, consumer, and drift check. | 15-01 T1, T2 | COVERED | Frontmatter includes SPEC-01. |
| REQ | SPEC-02 | Each derived spec has a failing conformance check; no spec without current consumer is created. | 15-01 T1, T2 | COVERED | Frontmatter includes SPEC-02. |
| RESEARCH | - | Use Bash smoke tests with existing `assert.sh` and optional Python stdlib parsing. | 15-01 T2 | COVERED | No new dependencies. |
| RESEARCH | - | Use only `specs/README.md`, `file-bus.md`, `risk-decisions.md`, `commands.md`, and `test-specs.sh`. | 15-01 T1, T2 | COVERED | Files match phase scope exactly. |
| RESEARCH | - | Let `run-all.sh` discover `test-specs.sh`; do not add Makefile wiring. | 15-01 T2 | COVERED | Acceptance checks include runner and Makefile guard. |
| RESEARCH | - | Reject absolute or traversal consumer paths and missing files. | 15-01 T2 | COVERED | Python parser requirements are explicit. |
| CONTEXT | D-01 | Create only specs with current repository consumers. | 15-01 T1, T2 | COVERED | T1 inventory is limited; T2 fails on extra specs. |
| CONTEXT | D-02 | Create `specs/file-bus.md` for file-bus consumers. | 15-01 T1 | COVERED | T1 lists exact file-bus consumers. |
| CONTEXT | D-03 | Create `specs/risk-decisions.md` for risk/decision consumers. | 15-01 T1 | COVERED | T1 lists exact risk/decision consumers. |
| CONTEXT | D-04 | Create `specs/commands.md` for command consumers. | 15-01 T1 | COVERED | T1 lists exact command consumers. |
| CONTEXT | D-05 | Create `specs/README.md` index. | 15-01 T1 | COVERED | T1 index content is specified. |
| CONTEXT | D-06 | Use fixed sections `## Source`, `## Consumers`, `## Drift Check`, `## Conformance Checks`. | 15-01 T1, T2 | COVERED | T2 asserts the headings. |
| CONTEXT | D-07 | Point derived specs to `.planning/SPEC.md`; docs are projections only. | 15-01 T1, T2 | COVERED | T1 source text and T2 source assertions. |
| CONTEXT | D-08 | Consumer paths must be concrete file paths. | 15-01 T1, T2 | COVERED | T2 extracts backticked paths from Consumers. |
| CONTEXT | D-09 | Extract only downstream-required, checkable contracts. | 15-01 T1 | COVERED | T1 restricts each spec to listed contracts. |
| CONTEXT | D-10 | Add `docs/orchestra/scripts/tests/test-specs.sh`. | 15-01 T2 | COVERED | T2 creates the test. |
| CONTEXT | D-11 | Rely on existing `run-all.sh` discovery. | 15-01 T2 | COVERED | T2 verifies runner output. |
| CONTEXT | D-12 | Fail if sections/source/consumer/drift/conformance are missing. | 15-01 T2 | COVERED | T2 parser and assertions cover every condition. |
| CONTEXT | D-13 | `specs/README.md` indexes every spec and lists consumers. | 15-01 T1, T2 | COVERED | T2 fails if a spec or consumer is missing from index. |
| CONTEXT | D-14 | Do not add Makefile targets in Phase 15. | 15-01 T2 | COVERED | T2 acceptance includes Makefile diff guard. |
| CONTEXT | D-15 | `.planning/SPEC.md` always wins. | 15-01 T1, T2 | COVERED | T1 writes canonical conflict rule; T2 checks canonical citation. |
| CONTEXT | D-16 | `specs/*.md` are projections, not authorities. | 15-01 T1 | COVERED | T1 writes projection language into index/specs. |
| CONTEXT | D-17 | Implementation/spec conflicts fail fast and point to implementation or canonical-first update. | 15-01 T1, T2 | COVERED | T1 conformance sections say this; T2 fails on consumer drift. |
| CONTEXT | D-18 | Do not broadly rewrite `.planning/SPEC.md` for stale canonical detail. | 15-01 T1, T2 | COVERED | Files modified exclude `.planning/SPEC.md`. |
| CONTEXT | D-19 | Downstream read order is `.planning/SPEC.md`, then `specs/*.md`, then `docs/orchestra/*`. | 15-01 T1 | COVERED | T1 writes exact read order in index and specs. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Create consumer-scoped derived spec documents</name>
  <files>specs/README.md, specs/file-bus.md, specs/risk-decisions.md, specs/commands.md</files>
  <read_first>
    - `specs/README.md`, `specs/file-bus.md`, `specs/risk-decisions.md`, `specs/commands.md` if they already exist; otherwise create them.
    - `.planning/SPEC.md` for canonical sections: BUS-01..BUS-06, AUTH-03, RISK-01..RISK-05, REMOTE-05, Appendix A, Appendix B, CMD-01..CMD-02.
    - `.planning/phases/15-specification-system/15-CONTEXT.md` for D-01..D-19 locked decisions.
    - `.planning/phases/15-specification-system/15-RESEARCH.md` and `15-PATTERNS.md` for exact consumer lists and derived spec shape.
    - `docs/orchestra/README.md`, `docs/orchestra/WORKFLOW.md`, and `docs/COVERAGE-MATRIX.md` as implementation projections only.
  </read_first>
  <action>
    Create the `specs/` directory and exactly four Markdown files: `README.md`, `file-bus.md`, `risk-decisions.md`, and `commands.md`. Do not create recovery, observability, scheduling, remote-adapter, or broad canonical split specs per D-01 and D-09. Do not modify `.planning/SPEC.md`, Makefile, runtime scripts, or `docs/orchestra/*` docs in this task per D-14 and D-18.

    `specs/README.md` must contain:
    - H1: `# Derived Specifications`
    - Exact authority sentence: "`.planning/SPEC.md` is the canonical specification. Files in `specs/` are derived projections for current repository consumers."
    - Exact conflict sentence: "If a derived spec conflicts with `.planning/SPEC.md`, update the derived spec to match `.planning/SPEC.md`."
    - Exact read-order sentence: "Downstream read order: `.planning/SPEC.md`, then the relevant `specs/*.md`, then `docs/orchestra/*` implementation projections."
    - Exact inventory rule sentence: "No current consumer, no derived spec."
    - An index table with rows for `specs/file-bus.md`, `specs/risk-decisions.md`, and `specs/commands.md`.
    - Consumer lists for each indexed spec using backticked repo-relative file paths.

    `specs/file-bus.md` must contain H1 `# File Bus Derived Spec` and fixed sections `## Source`, `## Consumers`, `## Drift Check`, `## Conformance Checks`. In `## Source`, write "Primary: `.planning/SPEC.md` §§BUS-01..BUS-06." and state `docs/orchestra/README.md` and `docs/orchestra/WORKFLOW.md` are projections only. In `## Consumers`, list exactly these paths:
    - `docs/orchestra/scripts/bin/orch-bus-loop`
    - `docs/orchestra/scripts/tests/test-file-bus.sh`
    - `docs/orchestra/README.md`
    - `docs/orchestra/WORKFLOW.md`
    Include a narrow contract section covering only: JSON/JSONL as canonical protocol, Markdown as projection only, required envelope fields `schema_version`, `message_id`, `project_id`, `task_id`, `correlation_id`, `status`, `author`, `authority`, `timestamp`, bus file ownership for `task.md`, `codex-question.md`, `claude-decision.md`, `escalation.md`, `codex-result.md`, `review-result.md`, `*.jsonl`, atomic write plus `flock`, stale-message rejection, correlation checks, schema validation, and audit migration before completion. In `## Drift Check`, include this fenced command exactly:
    ```bash
    bash docs/orchestra/scripts/tests/test-specs.sh && bash docs/orchestra/scripts/tests/test-file-bus.sh
    ```
    In `## Conformance Checks`, name `bash docs/orchestra/scripts/tests/test-specs.sh` and `bash docs/orchestra/scripts/tests/test-file-bus.sh`.

    `specs/risk-decisions.md` must contain H1 `# Risk Decisions Derived Spec` and the same fixed sections. In `## Source`, write "Primary: `.planning/SPEC.md` §§AUTH-03, RISK-01..RISK-05, REMOTE-05, Appendix A, Appendix B." In `## Consumers`, list exactly these paths:
    - `docs/orchestra/scripts/bin/orch-risk-check`
    - `docs/orchestra/config/rules.json`
    - `docs/orchestra/scripts/bin/orch-decisions`
    - `docs/orchestra/scripts/bin/orch-approve`
    - `docs/orchestra/scripts/bin/orch-reject`
    - `docs/orchestra/scripts/tests/test-risk-check.sh`
    - `docs/orchestra/scripts/tests/test-risk-decisions.sh`
    - `docs/orchestra/scripts/tests/test-decision-cli.sh`
    - `docs/orchestra/scripts/tests/test-decision-replay.sh`
    Include a narrow contract section covering only: static rule floors, Claude may upgrade but not downgrade below rulebook floor, L3/L4 block until explicit user approve/reject, no timeout or fallback auto-approval for L3/L4, local fallback commands `orch-decisions`, `orch-approve`, `orch-reject`, request and response JSON files under Runtime decisions, one-time `approval_id`, TTL, project/task binding, Audit write after validation, and the decision envelope field groups `rulebook`, `assessment`, `execution`, `history`. In `## Drift Check`, include this fenced command exactly:
    ```bash
    bash docs/orchestra/scripts/tests/test-specs.sh && bash docs/orchestra/scripts/tests/test-risk-check.sh && bash docs/orchestra/scripts/tests/test-risk-decisions.sh && bash docs/orchestra/scripts/tests/test-decision-cli.sh && bash docs/orchestra/scripts/tests/test-decision-replay.sh
    ```
    In `## Conformance Checks`, name the same five commands.

    `specs/commands.md` must contain H1 `# Commands Derived Spec` and the same fixed sections. In `## Source`, write "Primary: `.planning/SPEC.md` §§CMD-01..CMD-02." and state `docs/orchestra/README.md` and `docs/orchestra/WORKFLOW.md` are projections only. In `## Consumers`, list exactly these paths:
    - `docs/orchestra/scripts/bin/orch-init`
    - `docs/orchestra/scripts/bin/orch-start`
    - `docs/orchestra/scripts/bin/orch-stop`
    - `docs/orchestra/scripts/bin/orch-status`
    - `docs/orchestra/scripts/bin/orch-bus-loop`
    - `docs/orchestra/scripts/bin/orch-risk-check`
    - `docs/orchestra/scripts/bin/orch-decisions`
    - `docs/orchestra/scripts/bin/orch-approve`
    - `docs/orchestra/scripts/bin/orch-reject`
    - `docs/orchestra/scripts/bin/orch-audit`
    - `docs/orchestra/scripts/bin/orch-verify`
    - `docs/orchestra/README.md`
    - `docs/orchestra/WORKFLOW.md`
    - `docs/orchestra/scripts/tests/test-docs.sh`
    Include a narrow contract section covering only: local `orch-*` helper surface, command success/error output expectations from `.planning/SPEC.md` CMD-02, idempotency expectations where applicable, and the package boundary that local entrypoints are limited to `orch-*` helpers. Do not add Makefile targets or command implementations. In `## Drift Check`, include this fenced command exactly:
    ```bash
    for cmd in orch-init orch-start orch-stop orch-status orch-bus-loop orch-risk-check orch-decisions orch-approve orch-reject orch-audit orch-verify; do test -x "docs/orchestra/scripts/bin/$cmd"; done && bash docs/orchestra/scripts/tests/test-specs.sh && bash docs/orchestra/scripts/tests/test-docs.sh
    ```
    In `## Conformance Checks`, name `bash docs/orchestra/scripts/tests/test-specs.sh`, `bash docs/orchestra/scripts/tests/test-docs.sh`, and the executable loop above.
  </action>
  <verify>
    <automated>test -f specs/README.md && test -f specs/file-bus.md && test -f specs/risk-decisions.md && test -f specs/commands.md && rg -q --fixed-strings ".planning/SPEC.md" specs/*.md && rg -q --fixed-strings "No current consumer, no derived spec." specs/README.md</automated>
    <automated>for section in "## Source" "## Consumers" "## Drift Check" "## Conformance Checks"; do rg -q --fixed-strings "$section" specs/file-bus.md specs/risk-decisions.md specs/commands.md; done</automated>
  </verify>
  <acceptance_criteria>
    - `find specs -maxdepth 1 -type f -name '*.md' -printf '%f\n' | sort` outputs exactly `README.md`, `commands.md`, `file-bus.md`, and `risk-decisions.md`.
    - `rg -q --fixed-strings 'If a derived spec conflicts with `.planning/SPEC.md`, update the derived spec to match `.planning/SPEC.md`.' specs/README.md` passes.
    - `rg -q --fixed-strings "docs/orchestra/scripts/bin/orch-bus-loop" specs/file-bus.md specs/commands.md` passes.
    - `rg -q --fixed-strings "docs/orchestra/config/rules.json" specs/risk-decisions.md` passes.
    - `rg -q --fixed-strings "docs/orchestra/scripts/bin/orch-verify" specs/commands.md` passes.
    - `git diff --name-only -- .planning/SPEC.md Makefile docs/orchestra/scripts/bin docs/orchestra/README.md docs/orchestra/WORKFLOW.md` returns no paths from this task.
  </acceptance_criteria>
  <done>SPEC-01 and SPEC-02 documentation artifacts exist, every D-01..D-09 and D-15..D-19 documentation decision is implemented, and no out-of-scope spec or Makefile/runtime/canonical rewrite was introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing derived-spec conformance checks</name>
  <files>docs/orchestra/scripts/tests/test-specs.sh</files>
  <read_first>
    - `docs/orchestra/scripts/tests/test-specs.sh` if it already exists; otherwise create it.
    - `docs/orchestra/scripts/tests/test-docs.sh` for the Bash smoke test prologue.
    - `docs/orchestra/scripts/tests/lib/assert.sh` for `fail`, `assert_contains`, `assert_file_exists`, and `test_done`.
    - `docs/orchestra/scripts/tests/run-all.sh` for discovery behavior.
    - `specs/README.md`, `specs/file-bus.md`, `specs/risk-decisions.md`, `specs/commands.md` created by Task 1.
    - `.planning/phases/15-specification-system/15-VALIDATION.md` for Nyquist verification commands.
  </read_first>
  <behavior>
    - Test 1: Missing `specs/README.md` exits non-zero with an assertion failure.
    - Test 2: Any derived spec missing `## Source`, `## Consumers`, `## Drift Check`, or `## Conformance Checks` exits non-zero.
    - Test 3: Any derived spec that does not cite `.planning/SPEC.md` exits non-zero.
    - Test 4: Any backticked consumer path under `## Consumers` that is absolute, contains `..`, or does not exist exits non-zero.
    - Test 5: Any derived spec without a fenced `bash` command in `## Drift Check` exits non-zero.
    - Test 6: Any derived spec without `bash docs/orchestra/scripts/tests/test-specs.sh` in `## Conformance Checks` exits non-zero.
    - Test 7: Any non-index `specs/*.md` file not listed in `specs/README.md` exits non-zero.
    - Test 8: Any consumer path listed in a derived spec but not listed in `specs/README.md` exits non-zero.
  </behavior>
  <action>
    Create `docs/orchestra/scripts/tests/test-specs.sh` using the existing Bash smoke-test style. Use this prologue exactly, except `TEST_NAME` must be `specs-contract`:
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail

    TEST_NAME="specs-contract"
    TEST_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    REPO_ROOT="$(cd "$TEST_DIR/../../../.." && pwd)"

    # shellcheck source=lib/assert.sh
    source "$TEST_DIR/lib/assert.sh"
    ```

    Define:
    ```bash
    SPECS_DIR="$REPO_ROOT/specs"
    SPEC_INDEX="$SPECS_DIR/README.md"
    EXPECTED_SPECS=("commands.md" "file-bus.md" "risk-decisions.md")
    REQUIRED_SECTIONS=("## Source" "## Consumers" "## Drift Check" "## Conformance Checks")
    ```

    Implement the checks in this order:
    1. `assert_file_exists "$SPEC_INDEX" "specs index missing"`.
    2. `assert_contains ".planning/SPEC.md" "$SPEC_INDEX" "specs index must state canonical source"`.
    3. Assert `specs/README.md`, `specs/commands.md`, `specs/file-bus.md`, and `specs/risk-decisions.md` are the only `specs/*.md` files. Use `find "$SPECS_DIR" -maxdepth 1 -type f -name '*.md' -printf '%f\n' | sort` and compare to the expected newline list; call `fail "unexpected specs inventory"` on mismatch.
    4. For each expected spec, assert the file exists, assert every required section is present, assert `.planning/SPEC.md` is present, assert the basename is present in `SPEC_INDEX`, and assert `bash docs/orchestra/scripts/tests/test-specs.sh` is present.
    5. Use an inline `python3` parser to inspect each derived spec. The parser must:
       - Extract the body between `## Consumers` and the next `## ` heading.
       - Extract only backticked paths from that body.
       - Fail if there are no paths.
       - Reject `os.path.isabs(path)`.
       - Reject paths where any `pathlib.PurePosixPath(path).parts` item equals `..`.
       - Fail if `os.path.exists(os.path.join(repo_root, path))` is false.
       - Fail if any consumer path is absent from `specs/README.md`.
       - Extract `## Drift Check` and require a non-empty fenced `bash` block.
       - Extract `## Conformance Checks` and require `bash docs/orchestra/scripts/tests/test-specs.sh`.
    6. Call `test_done` at the end.

    Make `test-specs.sh` executable with `chmod +x docs/orchestra/scripts/tests/test-specs.sh`. Do not edit `docs/orchestra/scripts/tests/run-all.sh`; it already discovers `test-*.sh` per D-11. Do not create or edit Makefile per D-14.
  </action>
  <verify>
    <automated>bash docs/orchestra/scripts/tests/test-specs.sh</automated>
    <automated>bash docs/orchestra/scripts/tests/run-all.sh</automated>
    <automated>test -x docs/orchestra/scripts/tests/test-specs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash docs/orchestra/scripts/tests/test-specs.sh` exits 0 after Task 1 docs exist.
    - `bash docs/orchestra/scripts/tests/run-all.sh` exits 0 and includes the new script in its output as `test-specs.sh`.
    - `rg -q --fixed-strings "os.path.isabs(path)" docs/orchestra/scripts/tests/test-specs.sh` passes.
    - `rg -q --fixed-strings "PurePosixPath(path).parts" docs/orchestra/scripts/tests/test-specs.sh` passes.
    - `rg -q --fixed-strings "missing Consumers section" docs/orchestra/scripts/tests/test-specs.sh` passes.
    - `rg -q --fixed-strings "missing Drift Check bash block" docs/orchestra/scripts/tests/test-specs.sh` passes.
    - `rg -q --fixed-strings "missing Conformance Checks section" docs/orchestra/scripts/tests/test-specs.sh` passes.
    - `bash -lc 'set -euo pipefail; backup=$(mktemp); cp specs/file-bus.md "$backup"; restore(){ cp "$backup" specs/file-bus.md; rm -f "$backup"; }; trap restore EXIT; perl -0pi -e "s/^## Source\\n/## Source Missing\\n/m" specs/file-bus.md; set +e; bash docs/orchestra/scripts/tests/test-specs.sh >/tmp/test-specs-negative-source.out 2>&1; status=$?; set -e; test "$status" -ne 0'` proves a missing required section fails, then `bash docs/orchestra/scripts/tests/test-specs.sh` passes.
    - `bash -lc 'set -euo pipefail; backup=$(mktemp); cp specs/file-bus.md "$backup"; restore(){ cp "$backup" specs/file-bus.md; rm -f "$backup"; }; trap restore EXIT; perl -0pi -e "s#docs/orchestra/scripts/bin/orch-bus-loop#/tmp/not-a-repo-consumer#" specs/file-bus.md; set +e; bash docs/orchestra/scripts/tests/test-specs.sh >/tmp/test-specs-negative-consumer.out 2>&1; status=$?; set -e; test "$status" -ne 0'` proves an absolute consumer path fails, then `bash docs/orchestra/scripts/tests/test-specs.sh` passes.
    - `git diff --name-only -- Makefile docs/orchestra/scripts/tests/run-all.sh docs/orchestra/scripts/bin` returns no paths from this task.
  </acceptance_criteria>
  <done>SPEC-02 conformance is automated through an executable failing smoke test, D-10..D-14 are implemented, and the existing runner reaches the new checks without Makefile or runtime changes.</done>
</task>

</tasks>

<dependency_graph>
| Task | Needs | Creates | Wave |
|---|---|---|---|
| Task 1 | Phase 15 context, canonical SPEC, current consumer paths | `specs/README.md`, `specs/file-bus.md`, `specs/risk-decisions.md`, `specs/commands.md` | 1 |
| Task 2 | Task 1 docs, existing assert helper and runner | `docs/orchestra/scripts/tests/test-specs.sh` | 1 after Task 1 within the same plan |

Single-plan execution is intentional because Task 2 validates the exact documents created by Task 1, and splitting would add cross-plan dependency without parallelism.
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `.planning/SPEC.md` -> `specs/*.md` | Canonical source is projected into derived docs; projection must never claim authority. |
| `specs/*.md` consumer paths -> filesystem | Untrusted Markdown path strings are interpreted by `test-specs.sh`. |
| Markdown conformance checks -> smoke runner status | Documentation tests must fail with non-zero status, not print reminders. |
| Phase 15 scope -> repository runtime/dev workflow | Documentation/test phase must not introduce runtime scripts, Makefile targets, or remote adapters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-15-01 | Tampering | `specs/*.md` authority language | mitigate | Task 1 writes `.planning/SPEC.md` canonical and conflict-wins wording into index/specs; Task 2 checks `.planning/SPEC.md` appears in every derived spec. |
| T-15-02 | Tampering | `## Consumers` parser in `test-specs.sh` | mitigate | Task 2 extracts backticked paths only, rejects absolute paths, rejects `..` path parts, and fails if the repo-relative target does not exist. |
| T-15-03 | Repudiation | Documentation conformance checks | mitigate | Task 2 uses `set -euo pipefail`, `assert.sh`, inline Python failures, and negative mutation acceptance checks to prove non-zero failure behavior. |
| T-15-04 | Elevation of Privilege | Phase 15 scope boundary | mitigate | Plan `files_modified` excludes Makefile/runtime files; Task 1 and Task 2 acceptance criteria require no Makefile, `docs/orchestra/scripts/bin`, runner, or canonical SPEC diffs. |
| T-15-05 | Information Disclosure | Consumer path listing | accept | Listed paths are repository-relative public project paths, not secrets or user data. No secret/config value is copied into specs. |
</threat_model>

<verification>
Run these commands after both tasks complete:

```bash
bash docs/orchestra/scripts/tests/test-specs.sh
bash docs/orchestra/scripts/tests/run-all.sh
find specs -maxdepth 1 -type f -name '*.md' -printf '%f\n' | sort
git diff --name-only -- .planning/SPEC.md Makefile docs/orchestra/scripts/bin docs/orchestra/scripts/tests/run-all.sh
```

Expected results:
- `test-specs.sh` passes.
- `run-all.sh` passes and includes `test-specs.sh`.
- `find` prints only `README.md`, `commands.md`, `file-bus.md`, `risk-decisions.md`.
- The guarded `git diff --name-only` command prints nothing.
</verification>

<success_criteria>
- `SPEC-01` is satisfied: every derived spec declares source, consumers, and drift check while `.planning/SPEC.md` remains explicitly canonical.
- `SPEC-02` is satisfied: each derived spec has failing conformance coverage, and no consumerless spec is created.
- D-01 through D-19 are implemented or respected; deferred ideas are absent.
- The only execution files created or modified are `specs/README.md`, `specs/file-bus.md`, `specs/risk-decisions.md`, `specs/commands.md`, and `docs/orchestra/scripts/tests/test-specs.sh`.
- Security threats T-15-01 through T-15-04 are mitigated by concrete checks.
</success_criteria>

<output>
After completion, create `.planning/phases/15-specification-system/15-01-SUMMARY.md`.
</output>
