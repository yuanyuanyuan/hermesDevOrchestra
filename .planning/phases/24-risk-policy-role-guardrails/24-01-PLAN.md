---
phase: 24-risk-policy-role-guardrails
plan: "01"
type: execute
wave: 1
depends_on:
  - 23-01
files_modified:
  - docs/orchestra/config/risk-policy.yaml
  - docs/orchestra/hermes/hooks/pre_tool_call-risk-gate.sh
  - docs/orchestra/hermes/profile-distribution/profiles/orchestrator/config.yaml
  - docs/orchestra/hermes/profile-distribution/profiles/orchestrator/SOUL.md
  - docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml
  - docs/orchestra/hermes/profile-distribution/profiles/reviewer/SOUL.md
  - docs/orchestra/hermes/profile-distribution/profiles/implementer/SOUL.md
  - docs/orchestra/hermes/role-engine-protocol/v1/roles/implementer.md
  - docs/orchestra/scripts/bin/orch-risk-check
  - docs/orchestra/scripts/bin/orch-bus-loop
  - docs/orchestra/scripts/bin/orch-profile-sync
  - docs/orchestra/scripts/lib/orch-common.sh
  - docs/orchestra/scripts/setup.sh
  - docs/orchestra/scripts/tests/test-risk-decisions.sh
  - docs/orchestra/scripts/tests/test-risk-policy-loader.sh
  - docs/orchestra/scripts/tests/test-role-guardrails.sh
  - docs/orchestra/scripts/tests/test-implementer-block-contract.sh
  - docs/orchestra/README.md
  - docs/orchestra/WORKFLOW.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/24-risk-policy-role-guardrails/24-VERIFICATION.md
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "Phase 24 保留四层运行时风险语义：`L1` 仅记录，`L2` 可介入任务流，`L3` 必须一次显式批准，`L4` 只保留给事故按钮类操作并要求更强确认。"
    - "`L4` 范围必须明显收窄：删库、删生产资源、关键分支强推/改写历史、`rm -rf` 这类广域不可逆删除才进入 `L4`，其他高风险操作保留在 `L3`。"
    - "`L4` 批准语义必须绑定 `approval_id` 且可审计；本计划锁定固定确认短语格式为 `APPROVE-L4 <approval_id>`。"
    - "Reviewer 与 Orchestrator 护栏必须共用一份 policy source，并按 `toolset/CLI allowlist -> Hermes pre_tool_call hook -> SOUL 提醒` 三层实施，不能只靠 prompt。"
    - "Implementer 的四类强制 block 触发项固定为：架构决策、外部依赖不可用、风险策略拦截、关键测试失败；Implementer 不得自行降级绕过。"
    - "Phase 23 已锁定的 routing metadata 与 block/resume 语义保持不变；Phase 24 只补风险与角色护栏，不重开路由状态机设计。"
    - "Policy 必须保持单一 canonical surface；如需兼容旧 `rules.json` 安装形态，只能由 canonical policy 派生，不允许双写维护。"
  artifacts:
    - path: "docs/orchestra/config/risk-policy.yaml"
      provides: "唯一 canonical 风险与角色护栏策略面，按共享规则、角色分支与 `L1/L2/L3/L4` 语义组织"
      contains: "APPROVE-L4 <approval_id>"
    - path: "docs/orchestra/hermes/hooks/pre_tool_call-risk-gate.sh"
      provides: "对 Reviewer / Orchestrator 越权工具调用和高风险操作做二次硬拦截的 hook 入口"
      contains: "pre_tool_call"
    - path: "docs/orchestra/scripts/bin/orch-risk-check"
      provides: "从 canonical policy 计算有效风险等级、批准模式和匹配规则的统一检查器"
      contains: "APPROVE-L4"
    - path: "docs/orchestra/scripts/tests/test-role-guardrails.sh"
      provides: "Reviewer / Orchestrator allowlist 与 hook 双层护栏的可执行回归证明"
      contains: "Read,Glob,Grep"
    - path: "docs/orchestra/scripts/tests/test-implementer-block-contract.sh"
      provides: "Implementer 四类强制 block 触发与结构化 block metadata 的回归证明"
      contains: "critical-test-failure"
    - path: ".planning/phases/24-risk-policy-role-guardrails/24-VERIFICATION.md"
      provides: "Phase 24 验证证据、残留风险与收口结论"
      contains: "Ready for $gsd-execute-phase closeout"
  key_links:
    - from: ".planning/phases/24-risk-policy-role-guardrails/24-CONTEXT.md"
      to: "docs/orchestra/config/risk-policy.yaml"
      via: "已锁定的 L1/L2/L3/L4、L4 窄范围、共用 policy source 与四类 implementer block 触发在代码侧变成单一可执行策略面"
      pattern: "L4"
    - from: ".planning/phases/19-hermes-workflow-design/REQUIREMENTS.md"
      to: "docs/orchestra/hermes/hooks/pre_tool_call-risk-gate.sh"
      via: "R6/R8 的 hook 拦截与只读护栏从设计说明变成可安装的 runtime 资产"
      pattern: "pre_tool_call"
    - from: ".planning/phases/23-stateful-routing-kanban-handoff/23-CONTEXT.md"
      to: "docs/orchestra/scripts/lib/orch-common.sh"
      via: "风险拦截与 implementer block 继续复用 Phase 23 的 `workflow_state` / `routing_reason` / `resume_target` / `handoff_ref`"
      pattern: "resume_target"
    - from: "docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml"
      to: "docs/orchestra/scripts/tests/test-role-guardrails.sh"
      via: "配置层 allowlist 和 hook 层阻断共同证明 Reviewer 无法越权执行 terminal / file write / code execution"
      pattern: "allowedTools"
---

<objective>
Execute Phase 24 as the risk-policy and role-guardrail layer for the Hermes-native workflow MVP.

Purpose: satisfy SAFE-01, SAFE-02, and SAFE-03 by landing one canonical risk policy surface, wiring Reviewer / Orchestrator hard guardrails through profile allowlists plus `pre_tool_call` interception, and formalizing the Implementer mandatory block contract on top of the Phase 23 routing substrate.

Output: a canonical risk-policy file, runtime guardrail enforcement, updated role prompts/configs, executable regression tests, mirrored operator docs, and Phase 24 verification evidence ready for milestone progression.
</objective>

<execution_context>
Use the GSD execute-plan workflow. Keep Phase 24 narrow: land risk grading, role guardrails, and the Implementer block contract only. Do not implement timeout cleanup, environment snapshots, observability persistence, backpressure, or other Phase 25 lifecycle features here.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/24-risk-policy-role-guardrails/24-CONTEXT.md
@.planning/phases/24-risk-policy-role-guardrails/24-DISCUSSION-LOG.md
@.planning/phases/23-stateful-routing-kanban-handoff/23-CONTEXT.md
@.planning/phases/23-stateful-routing-kanban-handoff/23-VERIFICATION.md
@.planning/phases/22-external-cli-engine-protocol-role-invocation/22-CONTEXT.md
@.planning/phases/19-hermes-workflow-design/REQUIREMENTS.md
@.planning/phases/19-hermes-workflow-design/DESIGN.md
@.planning/phases/19-hermes-workflow-design/EXTERNAL-CLI-ENGINE.md
@.planning/phases/19-hermes-workflow-design/CONSISTENCY-CHECKLIST.md
@.planning/phases/19-hermes-workflow-design/ascii-decision-matrix.md
@.planning/phases/19-hermes-workflow-design/ascii-core-flows.md
@.planning/phases/19-hermes-workflow-design/workflow-appendix-decisions.md
@.planning/phases/19-hermes-workflow-design/workflow-appendix-failure-modes.md
@.planning/phases/19-hermes-workflow-design/workflow-appendix-roles.md
@docs/orchestra/config/rules.json
@docs/orchestra/hermes/profile-distribution/distribution.yaml
@docs/orchestra/hermes/profile-distribution/profiles/orchestrator/config.yaml
@docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml
@docs/orchestra/hermes/profile-distribution/profiles/implementer/config.yaml
@docs/orchestra/scripts/bin/orch-risk-check
@docs/orchestra/scripts/bin/orch-bus-loop
@docs/orchestra/scripts/bin/orch-profile-sync
@docs/orchestra/scripts/lib/orch-common.sh
@docs/orchestra/scripts/setup.sh
@docs/orchestra/scripts/tests/test-risk-decisions.sh
@docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh
@docs/orchestra/README.md
@docs/orchestra/WORKFLOW.md

<interfaces>
Required runtime risk levels:

```text
L1
L2
L3
L4
```

Required L4 classes:

```text
destructive database wipe
destructive production resource / cluster / volume deletion
force-push or history rewrite on critical branches
broad irreversible delete such as rm -rf
```

Required approval semantics:

```text
L1 -> record only
L2 -> intervention without default user escalation
L3 -> one explicit approval
L4 -> fixed phrase: APPROVE-L4 <approval_id>
```

Required guardrail layers:

```text
shared policy source
profile toolsets / CLI --allowedTools primary boundary
Hermes pre_tool_call hook secondary boundary
SOUL wording outer reminder only
```

Required Implementer block categories:

```text
architecture-decision
external-dependency-unavailable
risk-policy-intercepted
critical-test-failure
```

Required routing continuity:

```text
workflow_state
routing_reason
resume_target
handoff_ref
block_category
```
</interfaces>
</context>

<source_audit>
| Source | ID | Required Item | Plan/Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Land role-aware risk policy and guardrails on top of the external CLI engine baseline. | 24-01 T1,T2,T3 | COVERED | Plan is constrained to policy, hook enforcement, and implementer block routing. |
| REQ | SAFE-01 | Risk policy YAML classifies operations and prevents L3 auto-approval. | 24-01 T1 | COVERED | Canonical YAML, loader, L3/L4 approval semantics, and regression tests are explicit. |
| REQ | SAFE-02 | Reviewer / Orchestrator use allowlists and read-only guardrails instead of prompt-only boundaries. | 24-01 T2 | COVERED | Config, CLI flags, shared policy, and `pre_tool_call` interception are all in scope. |
| REQ | SAFE-03 | Implementer must `kanban_block` on fixed high-risk trigger classes. | 24-01 T3 | COVERED | Four mandatory categories, metadata contract, and tests are explicit. |
| CONTEXT | D-24-01..12 | Keep four levels, narrow L4, make L2 intervention-level only, and bind L4 to fixed confirmation. | 24-01 T1 | COVERED | Task 1 converts the locked semantics into a canonical executable policy surface. |
| CONTEXT | D-24-13..15 | Reviewer / Orchestrator use three-layer guardrails with one shared policy source. | 24-01 T2 | COVERED | Task 2 wires primary, secondary, and reminder layers without creating policy drift. |
| CONTEXT | D-24-16..20 | Implementer cannot self-downgrade; one policy file with common + role branches. | 24-01 T1,T3 | COVERED | Task 1 defines the file shape; Task 3 consumes it in routing behavior. |
| DESIGN | R6 / R8 / R10 / R14 / R37 | Declarative YAML policy, read-only reviewer boundary, hook interception, and implementer stop conditions. | 24-01 T1,T2,T3 | COVERED | Phase 19 design items are translated into executable repo assets. |
| CODE | rules.json / orch-risk-check | Current repo still uses JSON rules and broad L4 matches. | 24-01 T1 | COVERED | Task 1 explicitly migrates and narrows the current static rulebook. |
| CODE | orch-bus-loop / orch-common.sh | Existing approval blocking and routing metadata already exist. | 24-01 T2,T3 | COVERED | Tasks 2 and 3 extend the current path instead of creating a parallel dispatcher. |
| TEST | test-risk-decisions.sh | Existing approval tests already cover pending decision creation and no-auto-resume. | 24-01 T1,T3 | COVERED | Existing tests are updated and extended instead of replaced. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Replace the legacy static rulebook with one canonical risk-policy surface</name>
  <files>docs/orchestra/config/risk-policy.yaml, docs/orchestra/scripts/bin/orch-risk-check, docs/orchestra/scripts/setup.sh, docs/orchestra/scripts/tests/test-risk-decisions.sh, docs/orchestra/scripts/tests/test-risk-policy-loader.sh, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md</files>
  <read_first>
    Read 24-CONTEXT.md decisions D-24-01 through D-24-12 and D-24-19 through D-24-20, Phase 19 `R6` and the risk sections in DESIGN.md / ascii-decision-matrix.md, then inspect the current `rules.json`, `orch-risk-check`, and `test-risk-decisions.sh`.
  </read_first>
  <action>
    Replace the current ad hoc JSON rulebook with one canonical YAML policy surface under `docs/orchestra/config/risk-policy.yaml`.

    Required implementation boundaries:

```text
- Keep one canonical repo policy file with shared/common rules plus role-specific branches
- Preserve four runtime levels: L1 record-only, L2 intervention-level, L3 one explicit approval, L4 stronger fixed-phrase approval
- Narrow L4 to accident-button operations only; specifically remove current "everything scary becomes L4" drift
- Lock the deterministic L4 approval phrase to `APPROVE-L4 <approval_id>`
- Teach `orch-risk-check` to return structured match data needed by runtime: effective level, rule id, match text, and approval mode
- If a compatibility artifact for installed homes is still needed, generate it from the canonical YAML during setup; do not maintain YAML and JSON by hand
- Do not introduce a new external runtime dependency just to parse policy; stay within repo-local, auditable implementation choices
```

    Update README/WORKFLOW only enough to mirror the executable policy semantics and the L3/L4 difference. Preserve the existing approval flow shape from Phase 23 rather than inventing a second approval subsystem.
  </action>
  <verify>
    <automated>
rtk docs/orchestra/scripts/tests/test-risk-policy-loader.sh
    </automated>
    <automated>
rtk docs/orchestra/scripts/tests/test-risk-decisions.sh
    </automated>
  </verify>
  <acceptance_criteria>
    The repo has one obvious policy source, `orch-risk-check` consumes it directly, `L4` is visibly rare and accident-button only, and the approval contract distinguishes `L3` from `L4` in a way tests can assert.
  </acceptance_criteria>
  <done>SAFE-01 has an executable policy surface instead of a broad static rule list.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce Reviewer and Orchestrator boundaries through shared policy, allowlists, and hook interception</name>
  <files>docs/orchestra/hermes/hooks/pre_tool_call-risk-gate.sh, docs/orchestra/hermes/profile-distribution/profiles/orchestrator/config.yaml, docs/orchestra/hermes/profile-distribution/profiles/orchestrator/SOUL.md, docs/orchestra/hermes/profile-distribution/profiles/reviewer/config.yaml, docs/orchestra/hermes/profile-distribution/profiles/reviewer/SOUL.md, docs/orchestra/scripts/bin/orch-profile-sync, docs/orchestra/scripts/bin/orch-bus-loop, docs/orchestra/scripts/lib/orch-common.sh, docs/orchestra/scripts/setup.sh, docs/orchestra/scripts/tests/test-role-guardrails.sh, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md</files>
  <read_first>
    Read 24-CONTEXT.md decisions D-24-13 through D-24-15, Phase 19 `R8`, `R10`, and `R37`, `EXTERNAL-CLI-ENGINE.md` sections on layered guardrails, current reviewer/orchestrator profile configs, and any existing setup/install paths for profile distribution and hook templates.
  </read_first>
  <action>
    Wire Reviewer and Orchestrator guardrails through the three locked layers while keeping policy ownership centralized.

    Required behavior:

```text
- Reviewer and Orchestrator consume the same canonical policy file as the risk checker
- Primary boundary stays in checked-in profile toolsets and CLI `--allowedTools`
- Secondary boundary is a repo-local Hermes `pre_tool_call` hook that blocks write-equivalent tool use or unsafe shell execution for these roles
- SOUL wording remains present as the outer reminder layer, but never as the only enforcement
- Project-scoped generated output must inherit the same restrictions; do not rely on global manual setup
- Guardrails may escalate or block; they must not silently downgrade an action that truly belongs in L3/L4
```

    Keep this phase focused on Reviewer and Orchestrator. Do not expand guardrail work into full multi-role policy optimization, observability traces, or Phase 25 queue/backpressure behavior.
  </action>
  <verify>
    <automated>
rtk docs/orchestra/scripts/tests/test-role-guardrails.sh
    </automated>
    <automated>
rtk docs/orchestra/scripts/tests/test-profile-packaging.sh
    </automated>
  </verify>
  <acceptance_criteria>
    Reviewer and Orchestrator remain read-only by default at the profile layer, cannot regain write powers through generated project output, and still get a second hard stop from `pre_tool_call` if CLI flags or prompts drift.
  </acceptance_criteria>
  <done>SAFE-02 becomes an enforced runtime boundary instead of a prompt convention.</done>
</task>

<task type="auto">
  <name>Task 3: Formalize the Implementer mandatory block contract on top of Phase 23 routing</name>
  <files>docs/orchestra/hermes/profile-distribution/profiles/implementer/SOUL.md, docs/orchestra/hermes/role-engine-protocol/v1/roles/implementer.md, docs/orchestra/scripts/bin/orch-bus-loop, docs/orchestra/scripts/lib/orch-common.sh, docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh, docs/orchestra/scripts/tests/test-implementer-block-contract.sh, docs/orchestra/scripts/tests/test-risk-decisions.sh, docs/orchestra/README.md, docs/orchestra/WORKFLOW.md, .planning/phases/24-risk-policy-role-guardrails/24-VERIFICATION.md</files>
  <read_first>
    Read 24-CONTEXT.md decisions D-24-16 through D-24-20, Phase 23 routing decisions around `workflow_state` / `routing_reason` / `resume_target`, the Implementer role-engine contract, and the current failure-policy / risk-decision tests.
  </read_first>
  <action>
    Make the Implementer mandatory block contract explicit in prompts, protocol docs, runtime normalization, and regression tests.

    Required implementation boundaries:

```text
- Lock the four block categories exactly: architecture-decision, external-dependency-unavailable, risk-policy-intercepted, critical-test-failure
- Keep Phase 23 routing prefixes and state-machine continuity; add structured block metadata instead of inventing a second routing grammar
- When one of the four categories fires, runtime must normalize to a structured `kanban_block` path; no "best effort" silent workaround is allowed
- Under-classified decisions or local retries must not bypass the mandatory block categories
- Risk-policy interception must feed the same routing/audit path as other explicit blocks
```

    Update the human-facing docs only enough to explain the enforced contract and the interaction with reviewer handoff / resume behavior already shipped in Phase 23.
  </action>
  <verify>
    <automated>
rtk docs/orchestra/scripts/tests/test-implementer-block-contract.sh
    </automated>
    <automated>
rtk docs/orchestra/scripts/tests/test-role-engine-failure-policy.sh
    </automated>
    <automated>
rtk docs/orchestra/scripts/tests/test-risk-decisions.sh
    </automated>
  </verify>
  <acceptance_criteria>
    Implementer has one auditable block contract across prompt, protocol, runtime, and tests; the four categories are machine-visible, preserved through routing metadata, and cannot be silently side-stepped by the worker.
  </acceptance_criteria>
  <done>SAFE-03 plugs into the existing Kanban routing layer with explicit, testable stop conditions.</done>
</task>

</tasks>
